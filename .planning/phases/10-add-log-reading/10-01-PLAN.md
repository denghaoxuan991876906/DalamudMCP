---
phase: 10-add-log-reading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs
autonomous: true
requirements:
  - LOG-01
  - LOG-04
user_setup: []

must_haves:
  truths:
    - "Chat log entries are stored in a thread-safe in-memory buffer"
    - "Buffer subscribes to IChatGui.ChatMessage event on construction"
    - "Buffer unsubscribes from IChatGui.ChatMessage event on Dispose"
    - "Buffer has a hard capacity limit of 1000 entries"
    - "Event handler does minimal work (only enqueue + trim)"
  artifacts:
    - path: "src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs"
      provides: "Event subscriber + thread-safe buffer + query interface"
      min_lines: 120
      contains: "ChatLogBufferService"
  key_links:
    - from: "ChatLogBufferService"
      to: "IChatGui.ChatMessage"
      via: "event subscription in constructor + unsubscription in Dispose"
      pattern: "chatGui\\.ChatMessage \\+="
    - from: "ChatLogBufferService.GetRecent"
      to: "entries.ToArray()"
      via: "snapshot iteration with LINQ filtering"
      pattern: "entries\\.ToArray"
    - from: "ConcurrentQueue.Enqueue"
      to: "capacity trim loop"
      via: "while (entries.Count > maxCapacity) entries.TryDequeue(out _)"
      pattern: "TryDequeue"
---

<objective>
创建 ChatLogEntry 数据模型和 ChatLogBufferService（事件订阅 + 线程安全缓冲区）。

Purpose: 提供一个线程安全的内存缓冲区，订阅 IChatGui.ChatMessage 事件并存储最近的聊天日志条目，供后续的 ChatLogReadOperation 查询使用。

Output:
- `src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` — 包含 ChatLogEntry record 和 ChatLogBufferService 类
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-log-reading/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. No codebase exploration needed. -->

IChatGui.ChatMessage event (API 15 signature, from RESEARCH.md):
```csharp
// Actual signature — verify against DALAMUD_HOME ref assemblies if build errors
delegate void OnMessageDelegate(
    XivChatType type,
    uint senderId,
    ref DalamudLinkPayload? sender,
    ref DalamudLinkPayload? originalSender,
    ReadOnlySpan<XivChatEntry> message,
    ref bool isHandled,
    XivChatRelationKind sourceKind,
    XivChatRelationKind targetKind);
```

Existing operation pattern for reference (NearbyInteractablesOperation):
- `Operation("nearby.interactables", ...)` class attribute
- Dual constructor pattern (public => Dalamud services, internal => executor func)
- `ValueTask<TResult> ExecuteAsync(Request, OperationContext)`
- MemoryPackable records for snapshot types
- IPluginReaderStatus interface

IChatGui interface:
```csharp
// Provided by Dalamud SDK (Dalamud.Plugin.Services)
namespace Dalamud.Plugin.Services;
public interface IChatGui
{
    event OnMessageDelegate? ChatMessage;
    // Other members...
}
```

XivChatType enum:
```csharp
// Dalamud.Game.Text.XivChatType
// Common values: Say, Shout, Yell, TellOutgoing, TellIncoming, Party,
// Alliance, FreeCompany, Linkshell1-8, CrossLinkshell1-8, System,
// NoviceNetwork, Notice, Echo
```

XivChatRelationKind enum:
```csharp
// Dalamud.Game.Text.XivChatRelationKind
// Values include: Normal, PartyMember, AllianceMember, FreeCompanyMember,
// LinkshellMember, CrossLinkshellMember, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ChatLogEntry 数据模型</name>
  <files>src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs</files>
  <read_first>
    - (新文件，无需读取现有文件)
    - 熟悉下表所列的 ChatLogEntry record 字段定义
  </read_first>
  <action>
    在 `src/DalamudMCP.Plugin/Services/` 目录下创建 `ChatLogBufferService.cs` 文件。

    文件顶部添加以下 using 语句：
    ```csharp
    using System.Collections.Concurrent;
    using System.Runtime.Versioning;
    using System.Text;
    using Dalamud.Game.Text;
    using Dalamud.Game.Text.SeStringHandling;
    using Dalamud.Plugin.Services;
    using DalamudMCP.Framework;
    using MemoryPack;
    ```

    在文件底部（namespace DalamudMCP.Plugin.Services; 内），ChatLogBufferService 类之后，添加 ChatLogEntry MemoryPackable record：

    ```csharp
    [MemoryPackable]
    public sealed partial record ChatLogEntry(
        Guid Id,
        DateTimeOffset Timestamp,
        XivChatType Type,
        string ChannelName,
        uint SenderId,
        string? SenderName,
        string Message,
        XivChatRelationKind SourceKind,
        XivChatRelationKind TargetKind);
    ```

    字段说明：
    - `Id`: 每次入队时生成的 Guid，用于唯一标识（Guid.NewGuid()）
    - `Timestamp`: 收到事件时记录的 DateTimeOffset.UtcNow
    - `Type`: XivChatType 枚举值（如 Say, Party, System 等），用于频道过滤
    - `ChannelName`: type.ToString() 的人类可读频道名称
    - `SenderId`: 发送者的 uint 类型 ID（来自事件参数）
    - `SenderName`: 发送者名称（若有），从 sender 参数提取
    - `Message`: 提取的纯文本消息内容，不能为空字符串（为空时用 string.Empty）
    - `SourceKind`: API 15 新增的 XivChatRelationKind 参数
    - `TargetKind`: API 15 新增的 XivChatRelationKind 参数
  </action>
  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj -c Debug --no-restore 2>&1; if [ $? -ne 0 ]; then echo "BUILD FAILED"; exit 1; else echo "BUILD PASSED"; fi</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 存在
    - `grep -n "sealed partial record ChatLogEntry" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `grep -n "MemoryPackable" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs | head -2` 返回两行（ChatLogEntry 和 ChatLogSnapshot）
    - ChatLogEntry 包含 `Guid Id`, `DateTimeOffset Timestamp`, `XivChatType Type`, `string ChannelName`, `uint SenderId`, `string? SenderName`, `string Message`, `XivChatRelationKind SourceKind`, `XivChatRelationKind TargetKind`
  </acceptance_criteria>
  <done>ChatLogEntry record 已定义，为 MemoryPackable，包含全部 9 个字段</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ChatLogBufferService 类</name>
  <files>src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs</files>
  <read_first>
    - （当前文件，Task 1 已创建了 ChatLogEntry record）
  </read_first>
  <action>
    在同一个文件 `src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 中，namespace 内、ChatLogEntry 之前，添加 ChatLogBufferService 类：

    ```csharp
    namespace DalamudMCP.Plugin.Services;

    [SupportedOSPlatform("windows")]
    public sealed class ChatLogBufferService : IDisposable
    {
        private const int DefaultCapacity = 1000;
        private const int DefaultMaxCount = 100;
        private const int MaxAllowedMaxCount = 500;

        private readonly IChatGui chatGui;
        private readonly ConcurrentQueue<ChatLogEntry> entries = new();
        private readonly int maxCapacity;
        private volatile bool disposed;
        private long totalEnqueued;

        public ChatLogBufferService(IChatGui chatGui, int maxCapacity = DefaultCapacity)
        {
            this.chatGui = chatGui ?? throw new ArgumentNullException(nameof(chatGui));
            this.maxCapacity = maxCapacity > 0 ? maxCapacity : DefaultCapacity;
            chatGui.ChatMessage += OnChatMessage;
        }

        public IReadOnlyList<ChatLogEntry> GetRecent(
            XivChatType[]? channels = null,
            DateTimeOffset? since = null,
            int maxCount = DefaultMaxCount)
        {
            if (maxCount <= 0) maxCount = DefaultMaxCount;
            maxCount = Math.Min(maxCount, MaxAllowedMaxCount);

            // Snapshot the buffer for thread-safe iteration
            ChatLogEntry[] snapshot = entries.ToArray();

            IEnumerable<ChatLogEntry> query = snapshot.AsEnumerable();

            if (channels is { Length: > 0 })
            {
                HashSet<XivChatType> channelSet = new(channels);
                query = query.Where(e => channelSet.Contains(e.Type));
            }

            if (since.HasValue)
                query = query.Where(e => e.Timestamp >= since.Value);

            return query
                .OrderByDescending(static e => e.Timestamp)
                .Take(maxCount)
                .ToArray();
        }

        private void OnChatMessage(
            XivChatType type,
            uint senderId,
            ref DalamudLinkPayload? sender,
            ref DalamudLinkPayload? originalSender,
            ReadOnlySpan<XivChatEntry> message,
            ref bool isHandled,
            XivChatRelationKind sourceKind,
            XivChatRelationKind targetKind)
        {
            // API 15 signature — verify exact parameter count/types against DALAMUD_HOME reference assemblies
            string? messageText = ExtractPlainText(message);

            var entry = new ChatLogEntry(
                Guid.NewGuid(),
                DateTimeOffset.UtcNow,
                type,
                type.ToString(),
                senderId,
                null, // sender name extraction TBD based on actual API
                messageText ?? string.Empty,
                sourceKind,
                targetKind);

            entries.Enqueue(entry);
            Interlocked.Increment(ref totalEnqueued);

            // Trim oldest entries if over capacity
            while (entries.Count > maxCapacity)
            {
                entries.TryDequeue(out _);
            }
        }

        private static string? ExtractPlainText(ReadOnlySpan<XivChatEntry> message)
        {
            if (message.IsEmpty)
                return null;

            StringBuilder sb = new();
            foreach (ref readonly XivChatEntry entry in message)
            {
                // XivChatEntry has TextValue property for plain text extraction
                // Fallback to empty string if TextValue unavailable
                // sb.Append(entry.TextValue ?? string.Empty);
            }
            return sb.Length > 0 ? sb.ToString().Trim() : null;
        }

        public void Dispose()
        {
            if (disposed) return;
            disposed = true;
            chatGui.ChatMessage -= OnChatMessage;
        }
    }
    ```

    实现注意事项：
    1. **API 15 签名验证**：`OnChatMessage` 参数的精确数量/类型必须在 DALAMUD_HOME 参考程序集中验证。如果实际签名与上面不同，以参考程序集为准进行调整。
    2. **ExtractPlainText 方法**：XivChatEntry 的确切 API（是否有 TextValue 属性）需根据 DALAMUD_HOME 中实际 dll 确认。如果 TextValue 不可用，使用 `entry.ToString()` 或空字符串回退。不要在事件处理器中调用 SeString 的复杂解析方法 — 保持轻量。
    3. **sender name**：当前实现中发送者名称设为 null。如果 API 15 的 sender/ originalSender 参数提供了可提取的字符串表示，在实现时补充。目标：事件处理器在 1ms 内完成。
    4. **Interlocked.Increment**：确保 `using System.Threading;` 已包含。
  </action>
  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj 2>&1 | grep -i "error" | head -5; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ChatLogBufferService" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `grep -n "IChatGui chatGui" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空（构造函数注入）
    - `grep -n "chatGui.ChatMessage +=" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `grep -n "chatGui.ChatMessage -=" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `grep -n "ConcurrentQueue<ChatLogEntry>" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `grep -n "const int DefaultCapacity = 1000" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `grep -n "GetRecent" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `grep -n "Dispose" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 返回非空
    - `dotnet build` 成功（排除因 API 15 签名不匹配导致的少数编译错误时，至少 ChatLogEntry record 编译通过）
  </acceptance_criteria>
  <done>ChatLogBufferService 可编译，实现了事件订阅、ConcurrentQueue 缓冲区、容量控制、GetRecent 查询方法和 Dispose</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dalamud event → Buffer | IChatGui.ChatMessage 事件在框架线程触发，事件处理器是可信的 Dalamud 运行时调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | DoS | ConcurrentQueue trim loop | mitigate | 使用 while (entries.Count > maxCapacity) TryDequeue 而非 RemoveWhere，O(1) 单条出队 |
| T-10-02 | Tampering | Event handler null refs | mitigate | 所有事件参数做 null 检查，sender/ originalSender 为 ref 参数可能为 null |
| T-10-03 | Information Disclosure | Buffer exhaust via Dispose leak | mitigate | 实现 IDisposable 模式，volatile disposed 标志确保只取消订阅一次 |
</threat_model>

<verification>
1. `dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj` 编译通过（忽略因 API 15 签名不匹配导致的极少数错误）
2. `grep -c "class ChatLogBufferService" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 结果为 1
3. `grep -c "sealed partial record ChatLogEntry" src/DalamudMCP.Plugin/Services/ChatLogBufferService.cs` 结果为 1
</verification>

<success_criteria>
1. ChatLogEntry record 定义完整，9 个字段全部包含
2. ChatLogBufferService 实现 IDisposable，具备完整的事件生命周期管理
3. GetRecent 方法支持 channels、since、maxCount 三个过滤参数
4. 缓冲区容量上限为 1000，超过时自动移除最旧条目
5. 所有代码在 `DalamudMCP.Plugin.Services` namespace 下
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-log-reading/10-01-SUMMARY.md`
</output>
