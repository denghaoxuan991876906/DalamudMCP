---
phase: 11-ipc-infra
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcCallGateSubscriber.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginIpcGatewayTests.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/ReflectionPluginCallGateSubscriberTests.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "FakeGateway 和 FakeSubscriber 可作为 public 测试桩被其他测试项目复用"
    - "PluginIpcGateway 有独立单元测试覆盖其 TryCreate 路径"
    - "ReflectionPluginCallGateSubscriber 有独立单元测试覆盖 HasFunction 和 InvokeFunc"
    - "现有 UnsafeInvokePluginIpcOperationTests 全部通过（无回归）"
  artifacts:
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs"
      provides: "FakeIpcGateway public 测试桩，实现 IPluginIpcGateway"
      min_lines: 25
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcCallGateSubscriber.cs"
      provides: "FakeIpcCallGateSubscriber public 测试桩，实现 IPluginCallGateSubscriber"
      min_lines: 15
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/PluginIpcGatewayTests.cs"
      provides: "PluginIpcGateway 独立测试 — 覆盖 TryCreate 成功/失败路径"
      min_lines: 40
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/ReflectionPluginCallGateSubscriberTests.cs"
      provides: "ReflectionPluginCallGateSubscriber 独立测试 — 覆盖 HasFunction 和 InvokeFunc"
      min_lines: 30
  key_links:
    - from: "UnsafeInvokePluginIpcOperationTests"
      to: "FakeIpcGateway"
      pattern: "new FakeIpcGateway\\(\\)"
    - from: "FakeIpcGateway.TryCreate()"
      to: "FakeIpcCallGateSubscriber"
      via: "returns subscriber from dictionary"
    - from: "PluginIpcGatewayTests"
      to: "IPluginIpcGateway.TryCreate()"
      pattern: "gateway\\.TryCreate\\(callgate"
---

<objective>
从 `UnsafeInvokePluginIpcOperationTests.cs` 中提取 `FakeGateway` 和 `FakeSubscriber` 为 `TestShared/Ipc/` 下的公共测试桩（per D-06）；创建 `PluginIpcGateway` 和 `ReflectionPluginCallGateSubscriber` 的独立单元测试。

Purpose: 测试桩可被 Phase 12/14/15 测试复用；新增独立服务测试确保提取后的接口实现行为正确。
Output: 2 个测试桩文件 + 2 个新测试文件 + 1 个修改后的现有测试文件
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ipc-infra/11-CONTEXT.md
@tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs
@src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs
@src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs
@src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs
@src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs

<interfaces>
<!-- Plan 01 创建的接口，测试桩需实现这些接口 -->
```csharp
// IPluginIpcGateway (public)
public bool TryCreate(string callgate, IReadOnlyList<Type> typeArguments, out IPluginCallGateSubscriber? subscriber);

// IPluginCallGateSubscriber (public)
public bool HasFunction { get; }
public object? InvokeFunc(IReadOnlyList<object?> arguments);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 提取 FakeGateway 和 FakeSubscriber 为公共测试桩</name>
  <read_first>
    - tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs (第 73-96 行：嵌套 FakeGateway 和 FakeSubscriber 类)
    - src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs (接口定义)
    - src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs (接口定义)
  </read_first>
  <files>
    tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs,
    tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcCallGateSubscriber.cs,
    tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs
  </files>
  <action>
**步骤 1: 创建测试桩文件**

创建 `tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/` 目录和两个 public 测试桩文件：

**文件 `FakeIpcGateway.cs`：**
```csharp
using DalamudMCP.Plugin.Ipc;

namespace DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;

public sealed class FakeIpcGateway : IPluginIpcGateway
{
    private readonly Dictionary<string, IPluginCallGateSubscriber> subscribers;

    public FakeIpcGateway(params (string Callgate, IPluginCallGateSubscriber Subscriber)[] entries)
    {
        subscribers = entries.ToDictionary(
            static entry => entry.Callgate,
            static entry => entry.Subscriber,
            StringComparer.Ordinal);
    }

    public bool TryCreate(string callgate, IReadOnlyList<Type> typeArguments, out IPluginCallGateSubscriber? subscriber)
    {
        _ = typeArguments;
        return subscribers.TryGetValue(callgate, out subscriber);
    }
}
```

**文件 `FakeIpcCallGateSubscriber.cs`：**
```csharp
using DalamudMCP.Plugin.Ipc;

namespace DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;

public sealed class FakeIpcCallGateSubscriber : IPluginCallGateSubscriber
{
    public FakeIpcCallGateSubscriber(bool hasFunction, object? result = null)
    {
        HasFunction = hasFunction;
        _result = result;
    }

    public bool HasFunction { get; }

    private readonly object? _result;

    public object? InvokeFunc(IReadOnlyList<object?> arguments)
    {
        _ = arguments;
        return _result;
    }
}
```

注意：类名从 `FakeGateway` → `FakeIpcGateway`，`FakeSubscriber` → `FakeIpcCallGateSubscriber`，更明确地表达其用途。

**步骤 2: 修改 UnsafeInvokePluginIpcOperationTests.cs**
- 添加 `using DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;`
- 删除嵌套的 `FakeGateway` 类（第 73-84 行）和 `FakeSubscriber` 类（第 86-95 行）
- 更新测试方法中所有引用：
  - `new FakeGateway()` → `new FakeIpcGateway()`
  - `new FakeGateway(("MyPlugin.Test", new FakeSubscriber(true, true)))` → `new FakeIpcGateway(("MyPlugin.Test", new FakeIpcCallGateSubscriber(true, true)))`
  - `UnsafeInvokePluginIpcOperation.IPluginCallGateSubscriber` → `IPluginCallGateSubscriber`（使用 Plan 01 的 public 接口）
  - `UnsafeInvokePluginIpcOperation.IPluginIpcGateway` → `IPluginIpcGateway`（使用 Plan 01 的 public 接口）
  </action>
  <verify>
    <automated>grep -c "FakeIpcGateway" "tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs" && grep -c "FakeIpcCallGateSubscriber" "tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs` 存在，`FakeIpcGateway` 为 `public` 类，实现 `IPluginIpcGateway`
    - `FakeIpcGateway` 构造函数接受 `params (string Callgate, IPluginCallGateSubscriber Subscriber)[]` — 与原 `FakeGateway` 签名一致
    - `tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcCallGateSubscriber.cs` 存在，`FakeIpcCallGateSubscriber` 为 `public` 类，实现 `IPluginCallGateSubscriber`
    - `FakeIpcCallGateSubscriber` 构造函数接受 `(bool hasFunction, object? result = null)` — 与原 `FakeSubscriber` 签名一致
    - `UnsafeInvokePluginIpcOperationTests.cs` 不包含 `FakeGateway` 或 `FakeSubscriber` 嵌套类
    - 所有测试方法引用已更新为 `FakeIpcGateway` 和 `FakeIpcCallGateSubscriber`
    - 所有类型引用从 `UnsafeInvokePluginIpcOperation.IPluginIpcGateway` 改为 `IPluginIpcGateway`
  </acceptance_criteria>
  <done>测试桩已提取为公共类，现有测试文件已更新引用，无嵌套类残留</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 PluginIpcGateway 和 ReflectionPluginCallGateSubscriber 的独立单元测试</name>
  <read_first>
    - src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs (被测类实现)
    - src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs (被测类实现)
    - src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs (接口约定)
    - src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs (接口约定)
    - tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs (测试模式参考)
    - tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj (项目引用)
  </read_first>
  <files>
    tests/DalamudMCP.Plugin.Operations.Tests/PluginIpcGatewayTests.cs,
    tests/DalamudMCP.Plugin.Operations.Tests/ReflectionPluginCallGateSubscriberTests.cs
  </files>
  <behavior>
    为两个已提取的服务类创建独立单元测试：

    **PluginIpcGatewayTests 至少覆盖：**
    - Test 1: `TryCreate` 对不存在的 callgate 返回 `false`，`subscriber` 为 `null`
    - Test 2: `TryCreate` 对 `IDalamudPluginInterface` 中注册的有效 callgate 返回 `true`，`subscriber` 不为 `null`
    - Test 3: `TryCreate` 在 `callgate` 为 null/whitespace 时抛出 `ArgumentException`
    - Test 4: `TryCreate` 在 `typeArguments` 为 null 时抛出 `ArgumentNullException`
    - Test 5: 构造函数在 `pluginInterface` 为 null 时抛出 `ArgumentNullException`

    **ReflectionPluginCallGateSubscriberTests 至少覆盖：**
    - Test 1: 构造函数对有效 subscriber 对象成功缓存 `HasFunction` 和 `InvokeFunc`
    - Test 2: `HasFunction` 返回 subscriber 对象的实际 `HasFunction` 值
    - Test 3: `InvokeFunc` 调用 subscriber 的 `InvokeFunc` 并返回其结果
    - Test 4: 构造函数在 `subscriber` 为 null 时抛出 `ArgumentNullException`
    - Test 5: 构造函数在 subscriber 不包含 `HasFunction` 属性时抛出 `InvalidOperationException`
    - Test 6: 构造函数在 subscriber 不包含 `InvokeFunc` 方法时抛出 `InvalidOperationException`
    - Test 7: `InvokeFunc` 在 `arguments` 为 null 时抛出 `ArgumentNullException`
  </behavior>
  <action>
**文件 1: `PluginIpcGatewayTests.cs`**

使用 xUnit v3 (`[Fact]`)，参照现有测试模式。`PluginIpcGateway` 依赖 `IDalamudPluginInterface`（Dalamud 原生接口），测试需要 mock 该接口。使用 `Substitute` 或手动实现假 `IDalamudPluginInterface` 来模拟 `GetIpcSubscriber<T>` 泛型方法。

测试策略：
- 正向测试：用 Substitute 创建 fake `IDalamudPluginInterface`，配置其 `GetIpcSubscriber<bool>` 泛型方法返回有效对象
- 负向测试：使用 Substitute 创建 fake `IDalamudPluginInterface`，不配置任何方法 → `GetSubscriberMethods` 无匹配导致 `TryCreate` 返回 false

**文件 2: `ReflectionPluginCallGateSubscriberTests.cs`**

`ReflectionPluginCallGateSubscriber` 使用纯反射，不依赖 Dalamud。测试使用简单的 dummy 对象。

定义一个测试用的 dummy subscriber 类：
```csharp
private sealed class DummySubscriber
{
    public bool HasFunction { get; set; } = true;
    public string InvokeFunc(string name, int count) => $"{name}:{count}";
}
```

测试覆盖构造、HasFunction、InvokeFunc、null 参数、无效对象等路径。
  </action>
  <verify>
    <automated>./build/test.ps1 -NoBuild -Filter "PluginIpcGatewayTests|ReflectionPluginCallGateSubscriberTests"</automated>
  </verify>
  <acceptance_criteria>
    - `PluginIpcGatewayTests.cs` 存在，包含至少 5 个 `[Fact]` 测试方法
    - `ReflectionPluginCallGateSubscriberTests.cs` 存在，包含至少 7 个 `[Fact]` 测试方法（覆盖构造、HasFunction、InvokeFunc、异常路径）
    - 所有新测试与现有 UnsafeInvokePluginIpcOperationTests 一起运行通过
    - 测试使用 xUnit v3 + `[Fact]`，命名空间为 `DalamudMCP.Plugin.Operations.Tests`
  </acceptance_criteria>
  <done>PluginIpcGateway 和 ReflectionPluginCallGateSubscriber 的独立单元测试已创建并通过</done>
</task>

</tasks>

<verification>
整体验证：
1. 所有测试（现有 + 新增）通过：`./build/test.ps1`
2. `FakeIpcGateway` 和 `FakeIpcCallGateSubscriber` 为 `public` 类，可被其他测试项目引用
3. 新增测试覆盖了提取后两个服务类的核心路径和异常路径
</verification>

<success_criteria>
- `FakeIpcGateway` 和 `FakeIpcCallGateSubscriber` 作为 `public` 测试桩可被 Phase 12/14/15 复用
- `PluginIpcGatewayTests` 和 `ReflectionPluginCallGateSubscriberTests` 全部通过
- `UnsafeInvokePluginIpcOperationTests` 全部通过（功能无回归）
- `./build/test.ps1` 整体通过
</success_criteria>

<output>
执行完成后创建 `.planning/phases/11-ipc-infra/11-03-SUMMARY.md`
</output>
