---
phase: 15-data-relay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs
  - src/DalamudMCP.Plugin/Relay/RelayChannel.cs
  - src/DalamudMCP.Plugin/Relay/PluginDataRelayService.cs
  - src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs
autonomous: true
requirements:
  - RELAY-01

must_haves:
  truths:
    - "PluginDataRelayService 作为 DI 单例注册，可通过 IPluginDataRelayService 接口注入到操作类"
    - "Subscribe() 创建有界 Channel<string>（容量 1000，DropOldest）并注册 Dalamud IPC Provider（CallGate: DalamudMCP.Relay.{pluginName}.{channel}）"
    - "Unsubscribe() 注销 IPC Provider、关闭 Channel Writer、从注册表移除"
    - "TryPoll() 非阻塞读取 Channel 中所有可用数据"
    - "IFramework.Update 每 60 帧检测一次已卸载插件，自动退订对应的通道"
    - "PluginDataRelayService 实现 IDisposable，在 Dispose 时清理所有活跃 Provider"
  artifacts:
    - path: "src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs"
      provides: "IPluginDataRelayService 公开接口"
      min_lines: 30
    - path: "src/DalamudMCP.Plugin/Relay/RelayChannel.cs"
      provides: "RelayChannel 内部记录类型——封装 Channel + IPC Provider + 元数据"
      min_lines: 10
    - path: "src/DalamudMCP.Plugin/Relay/PluginDataRelayService.cs"
      provides: "PluginDataRelayService 实现——管理 Channel 映射表 + IPC Provider + 自动清理"
      min_lines: 140
    - path: "src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs"
      provides: "DI 注册——AddSingleton<IPluginDataRelayService, PluginDataRelayService>()"
      contains: "IPluginDataRelayService"
  key_links:
    - from: "PluginDataRelayService 构造函数"
      to: "IDalamudPluginInterface + IFramework"
      via: "DI 构造注入"
      pattern: "PluginDataRelayService.*IDalamudPluginInterface pluginInterface.*IFramework framework"
    - from: "PluginDataRelayService.Subscribe"
      to: "DdalMCP.Relay callgate"
      via: "pluginInterface.GetIpcProvider<string, object>(callGate).RegisterAction(handler)"
      pattern: "GetIpcProvider.*Relay"
    - from: "PluginDataRelayService.Unsubscribe"
      to: "provider.Dispose()"
      via: "IPC Provider 注销"
      pattern: "provider\\.Dispose\\(\\)"
    - from: "PluginServiceCollectionExtensions.BuildDalamudServiceProvider"
      to: "IPluginDataRelayService"
      via: "services.AddSingleton"
      pattern: "AddSingleton.*IPluginDataRelayService"
---

<objective>
创建 PluginDataRelayService 基础设施——公开接口、实现、DI 注册。管理目标插件通过 Dalamud IPC 向 DalamudMCP 推送数据的完整生命周期（订阅→缓存→退订→自动清理）。

Purpose: 为 Phase 15 的三个操作类（subscribe/unsubscribe/poll）提供共享的 DI 单例服务。这是 Phase 15 与 Phase 14 的关键区别——Phase 14 使用 IPluginIpcGateway（Subscriber 方向），Phase 15 需要 Provider 方向（目标插件 → DalamudMCP）。
Output: 新建 3 个 Relay/ 源文件，修改 1 个 DI 注册文件。
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-data-relay/15-RESEARCH.md

<interfaces>
<!-- 从现有代码库提取的关键接口——执行器无需探索代码库 -->

来自 src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs — DI 注册模式:
```csharp
// PluginIpcGateway 通过构造注入 IDalamudPluginInterface，作为单例注册
internal sealed class PluginIpcGateway : IPluginIpcGateway
{
    private readonly IDalamudPluginInterface pluginInterface;
    public PluginIpcGateway(IDalamudPluginInterface pluginInterface) { ... }
}
// 注册: services.AddSingleton<IPluginIpcGateway, PluginIpcGateway>();
```

来自 src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs — DI 注册位置:
```csharp
// 行 51: services.AddSingleton<IPluginIpcGateway, PluginIpcGateway>();
// 在行 51 之后添加: services.AddSingleton<IPluginDataRelayService, PluginDataRelayService>();
```

来自 src/DalamudMCP.Plugin/PluginCompositionRoot.cs — Dispose 链:
```csharp
// PluginCompositionRoot.DisposeAsync() 调用 serviceProvider.DisposeAsync()
// ServiceProvider 会调用所有已注册 IDisposable/IAsyncDisposable 单例的 Dispose
```

来自 src/DalamudMCP.Plugin/PluginEntryPoint.cs — IFramework 来源:
```csharp
// PluginEntryPoint 构造函数接收 IFramework framework（Dalamud 注入）
// framework 被传入 BuildDalamudServiceProvider 并注册为单例
// services.AddSingleton(framework);
```

Dalamud SDK IFramework.Update 事件（假定基于 Dalamud API 15）:
```csharp
// IFramework.Update 是一个事件，在每游戏帧触发
// 签名: event Action? Update;
// PluginDataRelayService 在构造函数中订阅: framework.Update += OnFrameworkUpdate;
```

Dalamud SDK IDalamudPluginInterface 公开 IPC Provider API（假定基于 API 15）:
```csharp
// 获取 IPC Provider（服务端方向——注册后供其他插件调用）
// ICallGateProvider<T1, TRet> GetIpcProvider<T1, TRet>(string name)
// 返回的 provider 有方法: void RegisterAction(Action<T1> action)
// provider 实现 IDisposable —— 调用 Dispose() 即可注销 IPC 端点
```

来自 src/DalamudMCP.Plugin/Operations/SafeInvokePluginIpcOperation.cs — 操作类将注入此服务:
```csharp
// 操作类的 public 构造器通过 DI 注入所需服务:
public SomeOperation(IPluginDataRelayService relay, IFramework framework) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 IPluginDataRelayService 接口和 RelayChannel record</name>
  <files>src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs, src/DalamudMCP.Plugin/Relay/RelayChannel.cs</files>

  <read_first>
  - src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs（当前不存在，将新建）
  - src/DalamudMCP.Plugin/Relay/RelayChannel.cs（当前不存在，将新建）
  - src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs（接口命名和 null 返回约定——用于参考接口设计风格）
  - .planning/phases/15-data-relay/15-RESEARCH.md §2（IPluginDataRelayService 接口设计）、§3（RelayChannel 封装设计）
  </read_first>

  <action>
创建 `src/DalamudMCP.Plugin/Relay/` 目录（如不存在），然后创建以下两个文件：

**文件 1: `src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs`**

```csharp
using System.Threading.Channels;

namespace DalamudMCP.Plugin.Relay;

/// <summary>
/// 管理目标插件通过 IPC 向 DalamudMCP 推送数据的服务。
/// 每个订阅创建一个有界 Channel + IPC Provider，AI 通过 MCP 工具轮询数据。
/// </summary>
public interface IPluginDataRelayService
{
    /// <summary>
    /// 订阅数据回传通道——注册 IPC Provider 并创建有界 Channel。
    /// </summary>
    /// <param name="pluginName">目标插件的内部名称（InternalName）</param>
    /// <param name="channelName">通道名（不含插件名前缀）</param>
    /// <param name="capacity">Channel 容量，默认 1000</param>
    /// <returns>订阅成功返回 true；通道已存在返回 false（幂等——不覆盖已有通道）</returns>
    bool Subscribe(string pluginName, string channelName, int capacity = 1000);

    /// <summary>
    /// 退订数据回传通道——注销 IPC Provider 并关闭 Channel。
    /// </summary>
    /// <param name="fullChannelName">完整通道名（{PluginName}.{ChannelName}，如 "MyPlugin.status"）</param>
    /// <returns>退订成功返回 true；通道不存在返回 false</returns>
    bool Unsubscribe(string fullChannelName);

    /// <summary>
    /// 非阻塞轮询通道中的所有可用数据。
    /// </summary>
    /// <param name="fullChannelName">完整通道名（{PluginName}.{ChannelName}）</param>
    /// <param name="data">输出参数：所有可用的数据项（JSON 字符串列表）。通道不存在或为空时为空列表。</param>
    /// <returns>通道存在返回 true；不存在返回 false</returns>
    bool TryPoll(string fullChannelName, out IReadOnlyList<string> data);

    /// <summary>
    /// 检查通道是否已订阅。
    /// </summary>
    bool IsSubscribed(string fullChannelName);

    /// <summary>
    /// 获取所有活跃通道的完整名称。
    /// </summary>
    IReadOnlyCollection<string> ActiveChannels { get; }
}
```

**文件 2: `src/DalamudMCP.Plugin/Relay/RelayChannel.cs`**

```csharp
using System.Threading.Channels;

namespace DalamudMCP.Plugin.Relay;

/// <summary>
/// 封装一个数据回传通道的全部资源。
/// </summary>
/// <param name="Channel">有界 Channel，用于暂存目标插件推送的 JSON 数据</param>
/// <param name="IpcProvider">IPC Provider 的 IDisposable 引用——调用 Dispose() 即可注销 IPC 端点</param>
/// <param name="PluginName">目标插件内部名称（用于自动清理检测）</param>
/// <param name="FullChannelName">完整通道名（{PluginName}.{ChannelName}）</param>
/// <param name="CreatedAt">创建时间戳</param>
internal sealed record RelayChannel(
    Channel<string> Channel,
    IDisposable IpcProvider,
    string PluginName,
    string FullChannelName,
    DateTime CreatedAt);
```

两个文件都使用命名空间 `DalamudMCP.Plugin.Relay`，文件编码 UTF-8 with BOM。
  </action>

  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj -c Debug 2>&1 | Select-String "Error" -NotMatch "warning"</automated>
  </verify>

  <acceptance_criteria>
    <criterion value="1">文件 src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs 存在</criterion>
    <criterion value="2">文件 src/DalamudMCP.Plugin/Relay/RelayChannel.cs 存在</criterion>
    <criterion value="3">grep -c "interface IPluginDataRelayService" src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs → 输出 >= 1</criterion>
    <criterion value="4">grep -c "bool Subscribe" src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs → 输出 >= 1</criterion>
    <criterion value="5">grep -c "bool Unsubscribe" src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs → 输出 >= 1</criterion>
    <criterion value="6">grep -c "bool TryPoll" src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs → 输出 >= 1</criterion>
    <criterion value="7">grep -c "record RelayChannel" src/DalamudMCP.Plugin/Relay/RelayChannel.cs → 输出 >= 1</criterion>
    <criterion value="8">grep -c "Channel<string> Channel" src/DalamudMCP.Plugin/Relay/RelayChannel.cs → 输出 >= 1</criterion>
    <criterion value="9">grep -c "IDisposable IpcProvider" src/DalamudMCP.Plugin/Relay/RelayChannel.cs → 输出 >= 1</criterion>
    <criterion value="10">dotnet build 零错误</criterion>
  </acceptance_criteria>

  <done>IPluginDataRelayService.cs 和 RelayChannel.cs 创建完成，编译通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 PluginDataRelayService 实现（Channel 管理 + IPC Provider + 自动清理）</name>
  <files>src/DalamudMCP.Plugin/Relay/PluginDataRelayService.cs</files>

  <read_first>
  - src/DalamudMCP.Plugin/Relay/PluginDataRelayService.cs（当前不存在，将新建）
  - src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs（单例服务模式——构造注入 + 内部类 + IDisposable 注册模式）
  - src/DalamudMCP.Plugin/PluginCompositionRoot.cs（Dispose 链——ServiceProvider 自动 Dispose 所有 IDisposable 单例）
  - .planning/phases/15-data-relay/15-RESEARCH.md §1（完整数据流）、§2（接口设计）、§3（Channel 设计）、§4（自动清理）、§5 陷阱（线程安全、内存泄漏、性能开销、幂等性）
  </read_first>

  <action>
创建 `src/DalamudMCP.Plugin/Relay/PluginDataRelayService.cs`。

**命名空间：** `DalamudMCP.Plugin.Relay`

**类声明和特性：**
```csharp
internal sealed class PluginDataRelayService : IPluginDataRelayService, IDisposable
```
使用 `internal sealed`——遵循 PluginIpcGateway 的可见性模式（接口公开，实现 internal）。

**字段：**
```csharp
private readonly IDalamudPluginInterface pluginInterface;
private readonly IFramework framework;
private readonly ConcurrentDictionary<string, RelayChannel> channels = new(StringComparer.OrdinalIgnoreCase);
private int frameCounter;
private bool disposed;
```
- `channels`：key 是完整通道名（`"{PluginName}.{ChannelName}"`），不区分大小写匹配
- `frameCounter`：用于 IFramework.Update 节流

**构造函数：**
```csharp
public PluginDataRelayService(IDalamudPluginInterface pluginInterface, IFramework framework)
{
    this.pluginInterface = pluginInterface ?? throw new ArgumentNullException(nameof(pluginInterface));
    this.framework = framework ?? throw new ArgumentNullException(nameof(framework));
    framework.Update += OnFrameworkUpdate;
}
```
注意：直接在构造函数中订阅 `framework.Update`（非延迟）。服务作为单例，仅在 DI 容器解析时创建一次。

**Dispose 方法：**
```csharp
public void Dispose()
{
    if (disposed) return;
    disposed = true;

    framework.Update -= OnFrameworkUpdate;

    foreach (RelayChannel channel in channels.Values)
    {
        channel.IpcProvider.Dispose();
        channel.Channel.Writer.TryComplete();
    }
    channels.Clear();
}
```
Dispose 被 DI 容器在 `ServiceProvider.DisposeAsync()` 时自动调用（IAsyncDisposable → sync IDisposable 均会处理）。

**Subscribe 方法：**
```csharp
public bool Subscribe(string pluginName, string channelName, int capacity = 1000)
{
    ObjectDisposedException.ThrowIf(disposed, this);
    ArgumentException.ThrowIfNullOrWhiteSpace(pluginName);
    ArgumentException.ThrowIfNullOrWhiteSpace(channelName);

    string fullName = $"{pluginName}.{channelName}";

    // 幂等检查——通道已存在时返回 false（不覆盖）
    if (channels.ContainsKey(fullName))
        return false;

    string callGate = $"DalamudMCP.Relay.{pluginName}.{channelName}";

    // 创建有界 Channel
    BoundedChannelOptions options = new(capacity)
    {
        FullMode = BoundedChannelFullMode.DropOldest
    };
    Channel<string> channel = Channel.CreateBounded<string>(options);

    // 注册 IPC Provider
    // [ASSUMED: IDalamudPluginInterface.GetIpcProvider<string, object>() API 存在于 Dalamud SDK 15.0.0]
    var provider = pluginInterface.GetIpcProvider<string, object>(callGate);
    provider.RegisterAction(jsonData =>
    {
        // IPC 回调——将目标插件推送的 JSON 数据写入 Channel
        // Channel<T>.Writer.TryWrite() 是线程安全的，无需额外同步
        channel.Writer.TryWrite(jsonData);
    });

    RelayChannel entry = new(channel, provider, pluginName, fullName, DateTime.UtcNow);
    return channels.TryAdd(fullName, entry);
}
```

**Unsubscribe 方法：**
```csharp
public bool Unsubscribe(string fullChannelName)
{
    ObjectDisposedException.ThrowIf(disposed, this);
    ArgumentException.ThrowIfNullOrWhiteSpace(fullChannelName);

    if (!channels.TryRemove(fullChannelName, out RelayChannel? entry))
        return false;

    // 先注销 IPC Provider（阻止新数据进入）
    entry.IpcProvider.Dispose();
    // 再关闭 Channel Writer（使后续 TryWrite 安全失败）
    // TryComplete 不会抛异常，即使 Channel 已有数据或已被读取完毕
    entry.Channel.Writer.TryComplete();

    return true;
}
```

**TryPoll 方法：**
```csharp
public bool TryPoll(string fullChannelName, out IReadOnlyList<string> data)
{
    ObjectDisposedException.ThrowIf(disposed, this);
    ArgumentException.ThrowIfNullOrWhiteSpace(fullChannelName);

    if (!channels.TryGetValue(fullChannelName, out RelayChannel? entry))
    {
        data = [];
        return false;
    }

    List<string> items = [];
    while (entry.Channel.Reader.TryRead(out string? item))
    {
        items.Add(item);
    }

    data = items;
    return true;
}
```

**IsSubscribed 方法：**
```csharp
public bool IsSubscribed(string fullChannelName)
{
    ObjectDisposedException.ThrowIf(disposed, this);
    return channels.ContainsKey(fullChannelName);
}
```

**ActiveChannels 属性：**
```csharp
public IReadOnlyCollection<string> ActiveChannels
{
    get
    {
        ObjectDisposedException.ThrowIf(disposed, this);
        return channels.Keys.ToArray();
    }
}
```

**自动清理（IFramework.Update 钩子）：**
```csharp
private void OnFrameworkUpdate()
{
    // 节流：每 60 帧检测一次（≈每秒一次，基于 ~60fps）
    if (++frameCounter % 60 != 0)
        return;

    // 仅在存在活跃通道时执行
    if (channels.IsEmpty)
        return;

    // 获取当前已安装插件列表
    // [ASSUMED: IDalamudPluginInterface.InstalledPlugins 属性存在于 Dalamud SDK 15.0.0]
    HashSet<string> installedNames = pluginInterface.InstalledPlugins
        .Select(static plugin => plugin.InternalName)
        .ToHashSet(StringComparer.OrdinalIgnoreCase);

    // 检测已卸载的插件并自动退订
    foreach (KeyValuePair<string, RelayChannel> kvp in channels)
    {
        if (!installedNames.Contains(kvp.Value.PluginName))
        {
            Unsubscribe(kvp.Key);
        }
    }
}
```

**Using 语句：**
```csharp
using System.Collections.Concurrent;
using System.Threading.Channels;
using Dalamud.Plugin;
using Dalamud.Plugin.Services;
```

**文件编码：** UTF-8 with BOM
  </action>

  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj -c Debug 2>&1 | Select-String "Error" -NotMatch "warning"</automated>
  </verify>

  <acceptance_criteria>
    <criterion value="1">文件 src/DalamudMCP.Plugin/Relay/PluginDataRelayService.cs 存在</criterion>
    <criterion value="2">grep -c "class PluginDataRelayService" → 输出 >= 1</criterion>
    <criterion value="3">grep -c "IPluginDataRelayService, IDisposable" → 输出 >= 1</criterion>
    <criterion value="4">grep -c "public bool Subscribe" → 输出 >= 1</criterion>
    <criterion value="5">grep -c "public bool Unsubscribe" → 输出 >= 1</criterion>
    <criterion value="6">grep -c "public bool TryPoll" → 输出 >= 1</criterion>
    <criterion value="7">grep -c "ConcurrentDictionary" → 输出 >= 1</criterion>
    <criterion value="8">grep -c "BoundedChannelFullMode.DropOldest" → 输出 >= 1</criterion>
    <criterion value="9">grep -c "Channel.CreateBounded" → 输出 >= 1</criterion>
    <criterion value="10">grep -c "GetIpcProvider" → 输出 >= 1（IPC Provider 注册）</criterion>
    <criterion value="11">grep -c "RegisterAction" → 输出 >= 1（IPC 回调注册）</criterion>
    <criterion value="12">grep -c "OnFrameworkUpdate" → 输出 >= 1（自动清理钩子）</criterion>
    <criterion value="13">grep -c "frameCounter" → 输出 >= 1（节流计数器）</criterion>
    <criterion value="14">grep -c "InstalledPlugins" → 输出 >= 1（插件安装检测）</criterion>
    <criterion value="15">grep -c "public void Dispose" → 输出 >= 1（批量清理）</criterion>
    <criterion value="16">grep -c "provider.Dispose" src/DalamudMCP.Plugin/Relay/PluginDataRelayService.cs → 输出 >= 2（Unsubscribe + Dispose 中均调用）</criterion>
    <criterion value="17">dotnet build 零错误</criterion>
  </acceptance_criteria>

  <done>PluginDataRelayService.cs 编译通过，实现完整的 Subscribe/Unsubscribe/TryPoll/主动清理/自动清理生命周期管理</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 注册 IPluginDataRelayService 到 DI 容器</name>
  <files>src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs</files>

  <read_first>
  - src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs（行 48-68，IPluginIpcGateway 单例注册 + 其他单例注册模式）
  </read_first>

  <action>
在 `PluginServiceCollectionExtensions.cs` 的 `BuildDalamudServiceProvider` 方法中，在 `services.AddSingleton<IPluginIpcGateway, PluginIpcGateway>();` 之后添加一行：

```csharp
services.AddSingleton<IPluginDataRelayService, PluginDataRelayService>();
```

精确位置：行 51（`services.AddSingleton<IPluginIpcGateway, PluginIpcGateway>();`）之后，行 52（`services.AddSingleton<IUiLocalization, JsonLocalization>();`）之前。

同时需要在文件顶部添加 using 语句：
```csharp
using DalamudMCP.Plugin.Relay;
```

将此 using 添加到现有 using 块中（在 `using DalamudMCP.Plugin.Ipc;` 之后）。

完整修改逻辑：
1. 在 using 区域添加 `using DalamudMCP.Plugin.Relay;`（行 6 附近，`using DalamudMCP.Plugin.Ipc;` 之后）
2. 在 `services.AddSingleton<IPluginIpcGateway, PluginIpcGateway>();` 行之后插入 `services.AddSingleton<IPluginDataRelayService, PluginDataRelayService>();`
3. 文件其余部分不作任何更改
  </action>

  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj -c Debug 2>&1 | Select-String "Error" -NotMatch "warning"</automated>
  </verify>

  <acceptance_criteria>
    <criterion value="1">grep -c "AddSingleton.*IPluginDataRelayService.*PluginDataRelayService" src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs → 输出 >= 1</criterion>
    <criterion value="2">grep -c "using DalamudMCP.Plugin.Relay" src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs → 输出 >= 1</criterion>
    <criterion value="3">dotnet build 零错误</criterion>
  </acceptance_criteria>

  <done>IPluginDataRelayService 已注册为 DI 单例，所有接受 DI 注入的操作类可通过构造器获取该服务</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 目标插件 → DalamudMCP（IPC Provider 回调） | 不可信 JSON 数据从目标插件通过 Dalamud IPC 进入 PluginDataRelayService 的 RegisterAction 回调 |
| DalamudMCP → Channel 缓存 | 内存中 Channel 作为数据缓冲区，所有数据由 PluginDataRelayService 管理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Spoofing | IPC Provider CallGate 名称 | accept | CallGate 命名约定 `DalamudMCP.Relay.{pluginName}.{channel}` 在 Dalamud 进程内执行——只有已安装插件可调用；v1.1 不考虑插件冒充 |
| T-15-02 | Tampering | 通道名注入（如 `../`） | accept | Subscribe 的 pluginName/channelName 参数由 AI 通过 MCP 工具传入，AI 本身是可信客户端；底层 IPC CallGate 由 Dalamud 框架管理 |
| T-15-03 | Denial of Service | 高频推送导致内存耗尽 | mitigate | `BoundedChannelOptions(capacity=1000, FullMode=DropOldest)` 确保 Channel 内存有界；旧数据自动丢弃，永不增长 |
| T-15-04 | Information Disclosure | 僵尸 Provider 残留 | mitigate | `IFramework.Update` 自动清理钩子（每 60 帧检测已卸载插件）；`PluginDataRelayService.Dispose()` 批量清理所有活跃 Provider |
| T-15-05 | Denial of Service | 恶意插件推送超大 JSON | mitigate | `Channel<string>` 每条消息存储为单个字符串；`TryPoll` 可配合 `max-items` 限制（操作层实现）防止 MCP 响应过大 |
| T-15-06 | Elevation of Privilege | 未授权 MCP 工具暴露 | mitigate | 三个 relay 操作归类为 unsafe，受 `PluginOperationExposurePolicy` 的 `enableUnsafeOperations` UI 开关控制 |

**注意：** 由于 `plugin.data.subscribe`/`plugin.data.unsubscribe`/`plugin.data.poll` 被归类为 unsafe 操作，只有在 UI 中启用 unsafe 操作后才会暴露给 AI 客户端。
</threat_model>

<verification>
- `dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj -c Debug` 零错误
- `grep -c "IPluginDataRelayService" PluginServiceCollectionExtensions.cs` 输出 >= 1
- Relay/ 目录下三个新文件存在且完整
</verification>

<success_criteria>
1. PluginDataRelayService 编译通过，通过 DI 构造注入 `IDalamudPluginInterface` 和 `IFramework`
2. Subscribe() 使用 `BoundedChannelFullMode.DropOldest`（容量 1000），通过 `GetIpcProvider<string, object>(callGate).RegisterAction()` 注册 IPC Provider
3. Unsubscribe() 调用 `provider.Dispose()` + `Channel.Writer.TryComplete()` + 从 ConcurrentDictionary 移除
4. TryPoll() 非阻塞循环 `channel.Reader.TryRead()` 读取所有可用数据
5. IFramework.Update 每 60 帧检测 `InstalledPlugins`，自动退订已卸载插件的通道
6. PluginDataRelayService 实现 IDisposable，在 Dispose 中取消事件订阅并清理所有活跃 Provider
7. Subscribe() 幂等——对已存在的通道返回 false（不覆盖）
8. 服务作为 DI 单例注册，在 ServiceProvider Dispose 时自动清理
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-relay/15-01-SUMMARY.md`
</output>
