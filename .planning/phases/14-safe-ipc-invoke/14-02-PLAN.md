---
phase: 14-safe-ipc-invoke
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs
autonomous: true
requirements:
  - IPC-01

must_haves:
  truths:
    - "所有 5 种状态码均有测试覆盖：ipc_success、ipc_missing、ipc_not_ready、ipc_type_mismatch、ipc_plugin_error"
    - "参数类型自动推断（int/double/bool/string/null/JSON信封）有独立测试验证"
    - "Framework 线程编排（在线程上/不在线程上）均有测试覆盖"
    - "构造函数 null 参数验证有测试覆盖"
    - "InvalidCastException 被正确路由到 ipc_type_mismatch（而非 ipc_plugin_error）"
  artifacts:
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs"
      provides: "SafeInvokePluginIpcOperation 的完整单元测试"
      min_lines: 300
  key_links:
    - from: "SafeInvokePluginIpcOperationTests 测试"
      to: "FakeIpcGateway"
      via: "构造函数 params 注入"
      pattern: "new FakeIpcGateway\\(\\("
    - from: "异常路径测试"
      to: "NSubstitute Substitute.For<IPluginCallGateSubscriber>()"
      via: "mock InvokeFunc 抛异常"
      pattern: "Substitute\\.For<IPluginCallGateSubscriber>"
    - from: "线程测试"
      to: "NSubstitute mock IFramework"
      via: "IsInFrameworkUpdateThread + RunOnFrameworkThread"
      pattern: "framework\\.IsInFrameworkUpdateThread"
---

<objective>
为 SafeInvokePluginIpcOperation 创建完整的单元测试，覆盖成功路径（10 个）、错误路径（7 个）、Framework 线程编排（2 个）和构造函数验证（5 个），共计 24 个测试。

Purpose: 确保安全 IPC 调用操作类的所有代码路径（5 种状态码、参数推断、线程编排、异常处理）均被 xUnit 测试验证。
Output: 新建 `SafeInvokePluginIpcOperationTests.cs`，约 450 行，24 个测试。
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/14-safe-ipc-invoke/14-RESEARCH.md §6 测试策略

<interfaces>
<!-- 测试复用资产 -->

来自 tests/.../TestShared/Ipc/FakeIpcGateway.cs:
```csharp
public sealed class FakeIpcGateway : IPluginIpcGateway
{
    public FakeIpcGateway(params (string Callgate, IPluginCallGateSubscriber Subscriber)[] entries)
    // TryCreate 使用字典查找：subscribers.TryGetValue(callgate, out subscriber)
    // 忽略 typeArguments 参数（测试桩不验证类型）
}
```

来自 tests/.../TestShared/Ipc/FakeIpcCallGateSubscriber.cs:
```csharp
public sealed class FakeIpcCallGateSubscriber : IPluginCallGateSubscriber
{
    public FakeIpcCallGateSubscriber(bool hasFunction, object? result = null)
    public bool HasFunction { get; }
    public object? InvokeFunc(IReadOnlyList<object?> arguments) => _result; // 忽略 arguments，返回固定结果
    // 注意：FakeIpcCallGateSubscriber 无法抛出异常——需要异常模拟时使用 NSubstitute mock IPluginCallGateSubscriber
}
```

来自测试模式（PluginReloadOperationTests.cs 行 20-36）:
```csharp
// Framework mock 模式：
private static IFramework CreateFramework(bool isInFrameworkThread = false)
{
    var framework = Substitute.For<IFramework>();
    framework.IsInFrameworkUpdateThread.Returns(isInFrameworkThread);
    framework.RunOnFrameworkThread(Arg.Any<Action>())
        .Returns(callInfo => {
            ((Action)callInfo[0])();
            return Task.CompletedTask;
        });
    return framework;
}
// 操作实例化：var operation = new PluginReloadOperation(...)
// 请求构建：var request = new PluginReloadOperation.Request { PluginName = "..." };
// 上下文构建：var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);
// 断言：Assert.True/False, Assert.Equal, Assert.Contains
```

来自 UnsafeInvokePluginIpcOperationTests.cs 行 31-44:
```csharp
// FakeIpcGateway 使用模式：
new FakeIpcGateway(("MyPlugin.Test", new FakeIpcCallGateSubscriber(true, true)))
// 按 callgate 名称匹配
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建测试基础设施和成功路径测试（约 10 个测试）</name>
  <files>tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs</files>

  <read_first>
  - tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs（当前不存在，将新建）
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginReloadOperationTests.cs（测试模式：CreateFramework、CreateCommandManager、测试结构）
  - tests/DalamudMCP.Plugin.Operations.Tests/UnsafeInvokePluginIpcOperationTests.cs（FakeIpcGateway + FakeIpcCallGateSubscriber 使用模式）
  - tests/.../TestShared/Ipc/FakeIpcGateway.cs（构造函数签名）
  - tests/.../TestShared/Ipc/FakeIpcCallGateSubscriber.cs（构造函数签名：hasFunction, result）
  - src/DalamudMCP.Plugin/Operations/SafeInvokePluginIpcOperation.cs（操作类完整实现——Plan 01 将已创建）
  </read_first>

  <action>
创建 `tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs`。

**命名空间：** `DalamudMCP.Plugin.Operations.Tests`

**Using 语句：**
```csharp
using Dalamud.Plugin.Services;
using DalamudMCP.Framework;
using DalamudMCP.Plugin.Ipc;
using DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;
using NSubstitute;
```

**测试类：**
```csharp
public sealed class SafeInvokePluginIpcOperationTests
```

**私有辅助方法（放在类顶部，测试方法之前）：**

1. `CreateFramework(bool isInFrameworkThread = false)` — 返回 `IFramework`：
   - 使用 `Substitute.For<IFramework>()`
   - `framework.IsInFrameworkUpdateThread.Returns(isInFrameworkThread)`
   - `framework.RunOnFrameworkThread(Arg.Any<Action>()).Returns(callInfo => { ((Action)callInfo[0])(); return Task.CompletedTask; })`

2. `CreateOperation(Func<SafeInvokePluginIpcOperation.Request, CancellationToken, ValueTask<SafeInvokePluginIpcResult>> executor)` — 返回 `SafeInvokePluginIpcOperation`：
   - 调用 internal 构造函数：`new SafeInvokePluginIpcOperation(executor)`

3. `CreateOperation(IPluginIpcGateway gateway, IFramework framework)` — 返回 `SafeInvokePluginIpcOperation`：
   - 调用 public 构造函数：`new SafeInvokePluginIpcOperation(gateway, framework)`

4. 所有测试使用 `var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);`

5. 所有测试方法使用 `[Fact]` 属性和 `async Task` 返回类型。

**测试 1-4：成功路径 — 无参数调用**

| # | 测试名称 | 设置 | 断言 |
|---|---------|------|------|
| 1 | `InvokeSafeIpc_ReturnsSuccess_WithNoArguments` | FakeIpcGateway 包含 `("TestPlugin.MCP.Ping", new FakeIpcCallGateSubscriber(true, true))`；调用 `InvokeSafeIpc(gateway, request)` 且 `PluginName="TestPlugin" Method="Ping" ArgumentsJson=null` | `result.Success == true`, `result.Status == "ipc_success"`, `result.ReturnValue == "true"`, `result.Method == "Ping"`, `result.PluginName == "TestPlugin"`, `result.ErrorMessage == null`, `result.SummaryText` 包含 `"succeeded"` |

| 2 | `InvokeSafeIpc_ReturnsSuccess_WithSingleIntArgument` | FakeIpcGateway 包含 `("TestPlugin.MCP.AddOne", new FakeIpcCallGateSubscriber(true, 43))`；`ArgumentsJson="[42]"` | `Success == true`, `Status == "ipc_success"`, `ReturnValue == "43"` |

| 3 | `InvokeSafeIpc_ReturnsSuccess_WithMultipleArguments` | FakeIpcGateway 包含 callgate 且 subscriber `HasFunction=true`；`ArgumentsJson="[42,\"hello\",true]"` — 验证三种不同类型的自动推断 | `result.Success == true`, `Status == "ipc_success"` |

| 4 | `InvokeSafeIpc_ReturnsSuccess_WithJsonEnvelopeArgument` | FakeIpcGateway 包含 callgate，subscriber result = `"processed"`（string）；`ArgumentsJson="[{\"key\":\"value\"}]"` — 验证 JSON 对象作为字符串信封传递 | `result.Success == true`, `Status == "ipc_success"`, `ReturnValue` 不为 null |

**测试 5-7：成功路径 — 类型推断**

| 5 | `InvokeSafeIpc_InfersIntType_ForIntegerJsonNumber` | `ArgumentsJson="[42]"` — 验证 42 被推断为 int（非 double）。使用 FakeIpcCallGateSubscriber 且 `hasFunction=true`。 | `result.Success == true`, `Status == "ipc_success"` |

| 6 | `InvokeSafeIpc_InfersDoubleType_ForDecimalJsonNumber` | `ArgumentsJson="[3.14]"` — 验证 3.14 被推断为 double。 | `result.Success == true`, `Status == "ipc_success"` |

| 7 | `InvokeSafeIpc_InfersObjectType_ForNullArgument` | `ArgumentsJson="[null]"` — 验证 null 被推断为 object 类型。 | `result.Success == true`, `Status == "ipc_success"` |

**测试 8-10：成功路径 — 布尔值 + 变长参数**

| 8 | `InvokeSafeIpc_ReturnsSuccess_WithBoolArgument` | `ArgumentsJson="[true]"`；FakeIpcCallGateSubscriber result = `false` | `result.Success == true`, `ReturnValue == "false"` |

| 9 | `InvokeSafeIpc_ReturnsSuccess_WithFalseBoolArgument` | `ArgumentsJson="[false]"` | `result.Success == true`, `Status == "ipc_success"` |

| 10 | `InvokeSafeIpc_ReturnsSuccess_WithStringArgument` | `ArgumentsJson="[\"test\"]"`；FakeIpcCallGateSubscriber result = `"response"` | `result.Success == true`, `ReturnValue` 包含 `"response"` |
  </action>

  <verify>
    <automated>dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "SafeInvokePluginIpcOperationTests" --no-restore 2>&1</automated>
  </verify>

  <acceptance_criteria>
    <criterion value="1">文件 `tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs` 存在</criterion>
    <criterion value="2">grep -c "public sealed class SafeInvokePluginIpcOperationTests" → 输出 1</criterion>
    <criterion value="3">grep -c "\[Fact\]" → 输出 >= 10</criterion>
    <criterion value="4">grep -c "FakeIpcGateway" → 输出 >= 3</criterion>
    <criterion value="5">grep -c "FakeIpcCallGateSubscriber" → 输出 >= 3</criterion>
    <criterion value="6">grep -c "ipc_success" → 输出 >= 10</criterion>
    <criterion value="7">grep -c "CreateFramework" → 输出 >= 1（辅助方法存在）</criterion>
    <criterion value="8">grep -c "Substitute.For<IFramework>" → 输出 >= 1</criterion>
    <criterion value="9">测试编译且前 10 个全部通过</criterion>
  </acceptance_criteria>

  <done>10 个成功路径 + 类型推断测试编译通过并全部绿灯</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建错误路径、线程编排和构造函数验证测试（约 10 个测试）</name>
  <files>tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs</files>

  <read_first>
  - tests/DalamudMCP.Plugin.Operations.Tests/SafeInvokePluginIpcOperationTests.cs（Task 1 将已创建包含辅助方法和前 10 个测试）
  - tests/.../TestShared/Ipc/FakeIpcCallGateSubscriber.cs（确认其不支持异常——需 NSubstitute mock）
  - src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs（接口：HasFunction + InvokeFunc）
  </read_first>

  <action>
向现有 `SafeInvokePluginIpcOperationTests.cs` 文件中添加以下测试方法。使用 Edit 工具在类的最后一个测试方法之后、闭合 `}` 之前插入。

**测试 11-12：ipc_missing 错误路径**

| 11 | `InvokeSafeIpc_ReturnsIpcMissing_WhenGatewayHasNoMatchingCallgate` | `FakeIpcGateway()` 不包含任何条目（空字典）；callgate = `"Missing.MCP.Action"` | `result.Success == false`, `result.Status == "ipc_missing"`, `result.ErrorMessage` 不为 null, `result.SummaryText` 包含 `"ipc_missing"` |

| 12 | `InvokeSafeIpc_ReturnsIpcMissing_WhenCallgateDifferent` | `FakeIpcGateway(("Other.MCP.X", new FakeIpcCallGateSubscriber(true)))`；请求的 PluginName=`"TestPlugin"` Method=`"Ping"` → callgate=`"TestPlugin.MCP.Ping"` | `result.Status == "ipc_missing"` — 因为 callgate 名称不匹配 |

**测试 13：ipc_not_ready 错误路径**

| 13 | `InvokeSafeIpc_ReturnsIpcNotReady_WhenHasFunctionIsFalse` | `FakeIpcGateway(("TestPlugin.MCP.Ping", new FakeIpcCallGateSubscriber(false)))` — subscriber 存在但 `HasFunction == false` | `result.Success == false`, `result.Status == "ipc_not_ready"`, `result.ErrorMessage` 包含 `"not registered"`, `result.SummaryText` 包含 `"ipc_not_ready"` |

**测试 14：ipc_type_mismatch 错误路径**

| 14 | `InvokeSafeIpc_ReturnsIpcTypeMismatch_WhenInvalidCastExceptionThrown` | 使用 NSubstitute: `var sub = Substitute.For<IPluginCallGateSubscriber>(); sub.HasFunction.Returns(true); sub.InvokeFunc(Arg.Any<IReadOnlyList<object?>>()).Returns(x => throw new InvalidCastException("Type mismatch"));` 创建 `FakeIpcGateway(("TestPlugin.MCP.Ping", sub))`。同时验证被 `TargetInvocationException` 包装的 `InvalidCastException` 场景：`throw new TargetInvocationException(new InvalidCastException("Type mismatch wrapped"))` | `result.Status == "ipc_type_mismatch"`, `result.ErrorMessage` 包含 `"Type mismatch"`, `result.SummaryText` 包含 `"type mismatch"`

**测试 15-16：ipc_plugin_error 错误路径**

| 15 | `InvokeSafeIpc_ReturnsIpcPluginError_WhenTargetPluginThrows` | 使用 NSubstitute mock subscriber：`InvokeFunc` 抛出 `new InvalidOperationException("Plugin logic error")` | `result.Status == "ipc_plugin_error"`, `result.ErrorMessage` 包含 `"Plugin logic error"`, `result.SummaryText` 包含 `"plugin error"` |

| 16 | `InvokeSafeIpc_ReturnsIpcPluginError_WhenTargetInvocationExceptionWithoutInnerException` | 使用 NSubstitute mock subscriber：`InvokeFunc` 抛出 `new TargetInvocationException(new InvalidOperationException("Inner error"))` — 注意 `TargetInvocationException` 非 `InvalidCastException` 时会进入 `when (ex.InnerException is not null)` 分支 | `result.Status == "ipc_plugin_error"`, `result.ErrorMessage` 包含 `"Inner error"` |

**测试 17：JSON 解析错误 → ipc_plugin_error**

| 17 | `InvokeSafeIpc_ReturnsIpcPluginError_WhenArgumentsJsonIsInvalid` | `ArgumentsJson="not json"` | 使用 try-catch 通过 `ExecuteAsync` 调用：`var result = await operation.ExecuteAsync(request, context)` 且 Framework 线程设为 true。由于 JSON 解析在 InvokeSafeIpc 内部进行，异常应被最外层 `catch (Exception ex)` 捕获 → `Status == "ipc_plugin_error"` |

**测试 18-19：Framework 线程编排**

| 18 | `ExecuteAsync_CallsInvokeDirectly_WhenAlreadyOnFrameworkThread` | `var framework = CreateFramework(isInFrameworkThread: true)`; `var gateway = new FakeIpcGateway(("TestPlugin.MCP.Ping", new FakeIpcCallGateSubscriber(true, "ok")))`; `var operation = CreateOperation(gateway, framework)`; 调用 `operation.ExecuteAsync(...)` | `result.Success == true`; 验证 `framework.DidNotReceive().RunOnFrameworkThread(Arg.Any<Action>())` |

| 19 | `ExecuteAsync_CallsRunOnFrameworkThread_WhenNotOnFrameworkThread` | `var framework = CreateFramework(isInFrameworkThread: false)`; 同上 setup | `result.Success == true`; 验证 `await framework.Received(1).RunOnFrameworkThread(Arg.Any<Action>())` |

**测试 20-22：构造函数验证**

| 20 | `Constructor_RejectsNullGateway` | `var framework = CreateFramework()`; `Assert.Throws<ArgumentNullException>(() => new SafeInvokePluginIpcOperation(null!, framework))` | `ex.ParamName == "gateway"` |

| 21 | `Constructor_RejectsNullFramework` | `var gateway = new FakeIpcGateway()`; `Assert.Throws<ArgumentNullException>(() => new SafeInvokePluginIpcOperation(gateway, null!))` | `ex.ParamName == "framework"` |

| 22 | `ExecuteAsync_RejectsNullRequest` | `var operation = CreateOperation((r, ct) => ...)`; `Assert.ThrowsAsync<ArgumentNullException>(() => ...)` — 实际上 `ArgumentNullException` 不是 async 异常，用 `Assert.Throws<ArgumentNullException>` | `ex.ParamName == "request"` |

**测试 23：空 PluginName 和 Method 验证**

| 23 | `ExecuteAsync_ThrowsArgumentException_WhenPluginNameIsEmpty` | `var framework = CreateFramework(true)`; `var gateway = new FakeIpcGateway(...)`; `var request = new Request { PluginName = "", Method = "Ping" }` | 应抛出 `ArgumentException`（来自 `CreateDalamudExecutor` 中的 `ThrowIfNullOrWhiteSpace`） |

| 24 | `ExecuteAsync_ThrowsArgumentException_WhenMethodIsEmpty` | `PluginName = "Test"`, `Method = ""` | 应抛出 `ArgumentException` |
  </action>

  <verify>
    <automated>dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "SafeInvokePluginIpcOperationTests" --no-restore 2>&1</automated>
  </verify>

  <acceptance_criteria>
    <criterion value="1">grep -c "\[Fact\]" → 输出 >= 24（Task 1 的 10 个 + Task 2 的 14 个）</criterion>
    <criterion value="2">grep -c "ipc_missing" → 输出 >= 2</criterion>
    <criterion value="3">grep -c "ipc_not_ready" → 输出 >= 1</criterion>
    <criterion value="4">grep -c "ipc_type_mismatch" → 输出 >= 1</criterion>
    <criterion value="5">grep -c "ipc_plugin_error" → 输出 >= 3</criterion>
    <criterion value="6">grep -c "Substitute.For<IPluginCallGateSubscriber>" → 输出 >= 1（NSubstitute mock 接口用于异常路径）</criterion>
    <criterion value="7">grep -c "InvalidCastException" → 输出 >= 1</criterion>
    <criterion value="8">grep -c "RunOnFrameworkThread" → 输出 >= 2</criterion>
    <criterion value="9">grep -c "ArgumentNullException" → 输出 >= 3</criterion>
    <criterion value="10">grep -c "ArgumentException" → 输出 >= 2</criterion>
    <criterion value="11">所有 24 个测试编译并全部通过</criterion>
  </acceptance_criteria>

  <done>全部 24 个测试编译通过并绿灯——覆盖所有 5 种状态码、参数类型推断、线程编排和构造函数验证</done>
</task>

</tasks>

<verification>
- `dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "SafeInvokePluginIpcOperationTests"` — 全部 24 个测试通过
- `grep -c "\[Fact\]" SafeInvokePluginIpcOperationTests.cs` → >= 24
- 验证所有 5 种状态码字符串在测试文件中出现
- 验证 NSubstitute mock 模式用于异常路径（FakeIpcCallGateSubscriber 无法抛异常）
</verification>

<success_criteria>
1. 成功路径：无参数、单参数、多参数、JSON 信封参数 — 均返回 `ipc_success`
2. 类型推断：int / double / bool / string / null → 6 种类型自动推断均有测试验证
3. ipc_missing：网关不包含匹配 callgate → `ipc_missing`
4. ipc_not_ready：callgate 存在但 `HasFunction == false` → `ipc_not_ready`
5. ipc_type_mismatch：`InvalidCastException` 内部异常 → `ipc_type_mismatch`（非 ipc_plugin_error）
6. ipc_plugin_error：`InvalidOperationException` / `TargetInvocationException`（非 InvalidCast） / JSON 解析异常 → 均 `ipc_plugin_error`
7. 线程编排：Framework 线程上直接调用，非 Framework 线程通过 RunOnFrameworkThread 编排
8. 构造函数：null gateway / null framework / null request → 全部 `ArgumentNullException`
9. 输入验证：空 PluginName / 空 Method → `ArgumentException`
10. 所有测试使用项目标准：xUnit v3 `[Fact]` + NSubstitute + `OperationContext.ForMcp()` + `TestContext.Current.CancellationToken`
</success_criteria>

<output>
After completion, create `.planning/phases/14-safe-ipc-invoke/14-02-SUMMARY.md`
</output>
