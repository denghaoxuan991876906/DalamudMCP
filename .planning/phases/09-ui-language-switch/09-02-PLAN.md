---
phase: 09-ui-language-switch
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/DalamudMCP.Plugin/Ui/PluginConfigWindowModel.cs
  - src/DalamudMCP.Plugin/Ui/PluginConfigWindow.cs
  - src/DalamudMCP.Plugin/PluginEntryPoint.cs
autonomous: true
requirements:
  - REQ-01 (配置窗口语言切换选项)
  - REQ-02 (切换语言后即时更新)
  - REQ-04 (操作结果和状态信息跟随语言切换)
  - REQ-05 (CLI 帮助文本随语言切换更新)

must_haves:
  truths:
    - "配置窗口中所有状态文本（服务器状态、操作启用状态、读取器状态）随语言切换即时更新"
    - "切换语言后无需关闭或重新打开配置窗口，所有标签、按钮、状态行立即刷新"
    - "操作表中的 CLI 命令前缀和 MCP 工具前缀随语言切换更新"
    - "用户可以在配置窗口标题栏区域通过下拉框在中文和 English 之间切换"
    - "选择语言后配置窗口内容即时刷新，所有文本立刻变为目标语言"
    - "插件启动时自动加载用户上次选择的语言偏好"
    - "操作表中的 CLI 命令和 MCP 工具名称前缀随语言切换正确显示"
    - "窗口标题和面板标题等所有 ImGui 文本均通过本地化服务获取"
  artifacts:
    - path: "src/DalamudMCP.Plugin/Ui/PluginConfigWindowModel.cs"
      provides: "本地化感知的模型，所有状态文本通过 loc 计算"
      contains: "IUiLocalization"
    - path: "src/DalamudMCP.Plugin/Ui/PluginConfigWindow.cs"
      provides: "本地化感知的配置窗口，包含语言选择器"
      contains: "DrawLanguageSelector"
    - path: "src/DalamudMCP.Plugin/PluginEntryPoint.cs"
      provides: "启动时从配置初始化的语言设置"
      contains: "SetLanguage"
  key_links:
    - from: "PluginConfigWindow.LanguageChanged event handler"
      to: "RefreshModel(force: true)"
      via: "事件订阅 → 强制刷新"
      pattern: "LanguageChanged"
    - from: "PluginConfigWindow.DrawOperations"
      to: "localization[\"label.cli_prefix\"]"
      via: "实时字符串前缀拼接"
      pattern: "label.cli_prefix"
    - from: "PluginEntryPoint constructor"
      to: "localization.SetLanguage(config.SelectedLanguage)"
      via: "启动初始化"
      pattern: "SetLanguage"

---

<objective>
将本地化服务集成到 UI 层的所有组件中：模型、操作行、配置窗口和插件入口点。

Purpose: 确保所有 UI 文本通过 IUiLocalization 服务获取，语言切换后即时更新，语言选择持久化并在启动时恢复。
Output: 本地化后的 PluginConfigWindowModel、PluginConfigOperationRow、PluginConfigWindow、PluginEntryPoint。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-language-switch/09-01-PLAN.md
@.planning/phases/09-ui-language-switch/09-RESEARCH.md
@.planning/phases/09-ui-language-switch/09-UI-SPEC.md

注意: 本计划依赖于 Plan 01 创建的以下文件:
- `IUiLocalization` 接口 (命名空间 `DalamudMCP.Plugin.Ui.Localization`)
- `JsonLocalization` 实现 (DI 已注册为单例)
- `en.json` / `zh.json` (EmbeddedResource)
- `PluginUiConfiguration.SelectedLanguage` (配置属性)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 PluginConfigWindowModel — 注入 IUiLocalization，所有状态文本改为 computed getter</name>
  <files>
    src/DalamudMCP.Plugin/Ui/PluginConfigWindowModel.cs
  </files>
  <read_first>
    src/DalamudMCP.Plugin/Ui/PluginConfigWindowModel.cs — 读取完整的模型类（~306 行）和 PluginConfigOperationRow 类（~120 行）
  </read_first>
  <action>
    修改 `PluginConfigWindowModel` 类:

    1. **添加字段:**
    ```csharp
    private readonly IUiLocalization loc;
    ```
    在文件顶部添加 using: `using DalamudMCP.Plugin.Ui.Localization;`

    2. **移除所有静态字符串字段（约 4 行）:**
    - 删除 `ProtocolServerRunningText = "服务器状态: 运行中"`
    - 删除 `ProtocolServerStoppedText = "服务器状态: 已停止"`
    - 删除 `McpServerRunningText = "状态: 运行中"`
    - 删除 `McpServerStoppedText = "状态: 已停止"`

    3. **修改私有构造函数:**
    ```csharp
    private PluginConfigWindowModel(
        IUiLocalization loc,
        string pipeName,
        string pipeNameText,
        string cliCommand,
        string mcpCommand,
        PluginConfigOperationRow[] operations,
        IPluginReaderStatus?[] readerStatuses)
    {
        this.loc = loc;
        PipeName = pipeName;
        PipeNameText = pipeNameText;
        // ... rest unchanged
    }
    ```

    4. **将所有文本属性改为 computed getter（不缓存）:**

    ```csharp
    // ProtocolServerStatusText — 从字段改为 computed getter
    public string ProtocolServerStatusText =>
        loc[ProtocolServerRunning ? "status.server_running" : "status.server_stopped"];

    // ActionOperationsStatusText — 移除 actionOperationsStatusText 字段，改为 computed getter
    public string ActionOperationsStatusText =>
        loc[ActionOperationsEnabled ? "status.actions_enabled" : "status.actions_disabled"];

    // UnsafeOperationsStatusText — 移除 unsafeOperationsStatusText 字段
    public string UnsafeOperationsStatusText =>
        loc[UnsafeOperationsEnabled ? "status.unsafe_enabled" : "status.unsafe_disabled"];

    // McpServerStatusText — 从字段改为 computed getter
    public string McpServerStatusText =>
        loc[McpServerRunning ? "status.http_running" : "status.http_stopped"];
    ```

    5. **PipeNameText 在构造函数中使用 loc:**
    ```csharp
    PipeNameText = loc["label.pipe_name"] + pipeName;
    ```
    构造函数参数 `pipeNameText` 可以移除（因为现在在构造时计算）。更新 `Create` 方法中的调用: `new PluginConfigWindowModel(loc, options.PipeName, ...)` 省略 `pipeNameText` 参数。

    6. **简化 UpdateEndpointText:**
    ```csharp
    private void UpdateEndpointText(string endpoint)
    {
        if (string.Equals(McpServerEndpoint, endpoint, StringComparison.Ordinal))
            return;
        McpServerEndpoint = endpoint;
    }
    ```
   移除 `McpServerEndpointText` 字段。将其变为 computed getter:
    ```csharp
    public string McpServerEndpointText => loc["label.endpoint"] + McpServerEndpoint;
    ```

    7. **简化 UpdateError:**
    ```csharp
    private void UpdateError(string? error)
    {
        if (string.Equals(mcpServerError, error, StringComparison.Ordinal))
            return;
        mcpServerError = error;
    }
    ```
    移除 `McpServerErrorText` 字段。将其变为 computed getter:
    ```csharp
    public string? McpServerErrorText => string.IsNullOrWhiteSpace(mcpServerError)
        ? null
        : loc["label.last_error"] + mcpServerError;
    ```

    8. **ReaderStatusText 改为 computed getter:**
    ```csharp
    public string? ReaderStatusText
    {
        get
        {
            if (readerCount <= 0) return null;
            return string.Format(loc["status.reader_format"], readyReaderCount, readerCount);
        }
    }
    ```
    移除 `RefreshReaderStatuses` 中对 `ReaderStatusText` 的赋值，因为现在是 computed getter。

    9. **ApplyStatus 简化:** 移除所有硬编码字符串赋值:
    - 移除 `ProtocolServerStatusText = ...`（现为 computed getter）
    - 移除 `actionOperationsStatusText = ...`（现为 computed getter）
    - 移除 `unsafeOperationsStatusText = ...`（现为 computed getter）
    - 移除 `McpServerStatusText = ...`（现为 computed getter）
    ApplyStatus 只保留对 bool 字段的赋值：`ProtocolServerRunning`, `ActionOperationsEnabled`, `UnsafeOperationsEnabled`, `McpServerRunning`，以及调用 `UpdateEndpointText`, `UpdateCommand`, `UpdateError`, `RefreshExposureStatuses`, `RefreshReaderStatuses`。

    10. **静态 Create 方法添加 IUiLocalization 参数:**
    ```csharp
    public static PluginConfigWindowModel Create(
        IUiLocalization loc,
        PluginRuntimeOptions options,
        // ... rest unchanged
    )
    ```
    在方法内:
    ```csharp
    PipeNameText: loc["label.pipe_name"] + options.PipeName
    ```
    **注意**: 需要在调用处更新（由 Task 3 处理）。
  </action>
  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj --no-restore 2>&1 | grep -c "error CS" || true</automated>
  </verify>
  <acceptance_criteria>
    - `PluginConfigWindowModel` 包含 `private readonly IUiLocalization loc` 字段
    - 移除了所有静态字符串字段（ProtocolServerRunningText 等 4 个）
    - `ProtocolServerStatusText` 是 computed property getter: `loc[ProtocolServerRunning ? "status.server_running" : "status.server_stopped"]`
    - `ActionOperationsStatusText` 是 computed getter，无后备字段
    - `UnsafeOperationsStatusText` 是 computed getter，无后备字段
    - `McpServerStatusText` 是 computed getter，无后备字段
    - `ReaderStatusText` 是 computed getter: `string.Format(loc["status.reader_format"], ...)`
    - `McpServerEndpointText` 是 computed getter: `loc["label.endpoint"] + McpServerEndpoint`
    - `McpServerErrorText` 是 computed getter: `loc["label.last_error"] + mcpServerError`
    - `ApplyStatus` 方法不再包含对上述文本属性的直接字符串赋值
    - `Create` 静态方法的第一个参数是 `IUiLocalization loc`
    - 编译通过
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 重构 PluginConfigOperationRow — 移除缓存的前缀文本，使用 loc 生成行内字符串</name>
  <files>
    src/DalamudMCP.Plugin/Ui/PluginConfigWindowModel.cs
    src/DalamudMCP.Plugin/Ui/PluginConfigWindow.cs
  </files>
  <read_first>
    src/DalamudMCP.Plugin/Ui/PluginConfigWindowModel.cs — 读取 PluginConfigOperationRow 类（第 308-426 行）
    src/DalamudMCP.Plugin/Ui/PluginConfigWindow.cs — 读取 DrawOperations 方法（第 272-358 行），关注 CliCommandText/McpToolText 的访问方式
  </read_first>
  <action>
    分两步修改 PluginConfigOperationRow 类（在 PluginConfigWindowModel.cs 中）:

    **步骤 A: PluginConfigOperationRow 添加 IUiLocalization 字段并重构:**

    1. 添加 `IUiLocalization` 字段和构造函数参数:
    ```csharp
    private readonly IUiLocalization loc;

    public PluginConfigOperationRow(
        IUiLocalization loc,
        string operationId,
        string summary,
        string? cliCommand,
        string? mcpToolName)
    {
        this.loc = loc;
        OperationId = operationId;
        Summary = summary;
        CliCommand = cliCommand;
        McpToolName = mcpToolName;
        // 移除 CliCommandText 和 McpToolText 的赋值
        IsActionOperation = Hosting.PluginOperationExposurePolicy.IsActionOperation(operationId);
        IsUnsafeOperation = Hosting.PluginOperationExposurePolicy.IsUnsafeOperation(operationId);
    }
    ```

    2. **移除 `CliCommandText` 属性**（第 338 行附近）和 **`McpToolText` 属性**（第 340 行附近）:
    删除两对属性声明及其代码。

    3. **UpdateExposureStatus 中使用 loc:**
    ```csharp
    internal void UpdateExposureStatus(bool actionOperationsEnabled, bool unsafeOperationsEnabled)
    {
        ExposureStatusText = IsUnsafeOperation && !unsafeOperationsEnabled
            ? loc["status.exposure_unsafe_pending"]
            : IsActionOperation && !actionOperationsEnabled
                ? loc["status.exposure_action_pending"]
                : null;
        IsExposed = ExposureStatusText is null;
    }
    ```
    移除原有的早期返回优化（因为语言切换后即使状态不变，文本也可能变化）。方法始终保持更新。

    4. **简化 UpdateReaderStatus:** 移除文本生成逻辑：
    ```csharp
    internal void UpdateReaderStatus(IPluginReaderStatus? readerStatus)
    {
        bool? isReady = null;
        string? detail = null;
        if (readerStatus is not null)
            TryReadReaderStatus(readerStatus, out isReady, out detail);

        if (IsReaderReady == isReady &&
            string.Equals(ReaderDetail, detail, StringComparison.Ordinal))
        {
            return;
        }

        IsReaderReady = isReady;
        ReaderDetail = detail;
        // ReaderStatusText 现在是 computed getter — 不需要在这里设置了
    }
    ```

    5. **ReaderStatusText 改为 computed getter:**
    ```csharp
    public string? ReaderStatusText
    {
        get
        {
            if (IsReaderReady is null) return null;
            if (string.IsNullOrWhiteSpace(ReaderDetail))
                return IsReaderReady.Value ? loc["status.reader_ready"] : loc["status.reader_not_ready"];
            string readiness = IsReaderReady.Value
                ? loc["status.reader_ready_word"]
                : loc["status.reader_not_ready_word"];
            return string.Format(loc["status.reader_detail"], readiness, ReaderDetail);
        }
    }
    ```

    6. **移除 `ReaderStatusText` 的私有 setter**（原来是 public 属性的 private set）。现在 `ReaderStatusText` 变为只有 get 的 computed property。

    7. **不需要再存储 `ExposureStatusText` 和 `ReaderStatusText` 的 cached field** — 它们已经变为 computed getter。

    8. **UpdateReaderStatus 中的 `ReaderStatusText = CreateReaderStatusText(...)` 不再需要**，`CreateReaderStatusText` 静态方法可以删除或保留为空操作。建议保留为行内逻辑在 computed getter 中。

    **步骤 B: 更新 PluginConfigWindowModel.CreateRows 方法:**
    ```csharp
    rows[index] = new PluginConfigOperationRow(
        loc,  // 新增参数
        operation.OperationId,
        operation.Summary ?? operation.Description ?? operation.OperationId,
        operation.CliCommandPath is { Count: > 0 } cliPath ? string.Join(' ', cliPath) : null,
        operation.McpToolName);
    ```

    **步骤 C: 更新 PluginConfigWindow.DrawOperations():**
    在 `DrawOperations()` 方法中，将:
    ```csharp
    if (!string.IsNullOrWhiteSpace(operation.CliCommandText))
        ImGui.TextUnformatted(operation.CliCommandText);
    if (!string.IsNullOrWhiteSpace(operation.McpToolText))
        ImGui.TextUnformatted(operation.McpToolText);
    ```
    改为:
    ```csharp
    string cliPrefix = localization["label.cli_prefix"];
    if (!string.IsNullOrWhiteSpace(operation.CliCommand))
        ImGui.TextUnformatted(cliPrefix + operation.CliCommand);
    string mcpPrefix = localization["label.mcp_prefix"];
    if (!string.IsNullOrWhiteSpace(operation.McpToolName))
        ImGui.TextUnformatted(mcpPrefix + operation.McpToolName);
    ```
    这里 `localization` 是窗口的 `IUiLocalization` 实例（尚不存在，由 Task 3 添加）。为编译通过，先在窗口类中添加字段声明:
    ```csharp
    private readonly IUiLocalization localization;
    ```
  </action>
  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj --no-restore 2>&1 | grep -c "error CS" || true</automated>
  </verify>
  <acceptance_criteria>
    - `PluginConfigOperationRow` 构造函数第一个参数为 `IUiLocalization loc`
    - `CliCommandText` 和 `McpToolText` 属性已删除
    - `ReaderStatusText` 是 computed getter，使用 `loc["status.reader_READY"]` 等
    - `ExposureStatusText` 是 computed getter，使用 `loc["status.exposure_*"]`
    - `UpdateExposureStatus` 中早期返回已移除（总是更新文本）
    - `PluginConfigWindow.DrawOperations` 使用 `localization["label.cli_prefix"]` 和 `localization["label.mcp_prefix"]` 实时拼接
    - 编译通过（注意：窗口的 `localization` 字段将在 Task 3 中赋值）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 重构 PluginConfigWindow — 替换所有字符串字面量 + 添加语言选择器 + PluginEntryPoint 启动初始化</name>
  <files>
    src/DalamudMCP.Plugin/Ui/PluginConfigWindow.cs
    src/DalamudMCP.Plugin/PluginEntryPoint.cs
  </files>
  <read_first>
    src/DalamudMCP.Plugin/Ui/PluginConfigWindow.cs — 完整读取（447 行），了解所有 ImGui.Text/Button/Checkbox/TableSetupColumn 调用中的字符串
    src/DalamudMCP.Plugin/PluginEntryPoint.cs — 读取构造函数中创建 PluginConfigWindow 的部分（第 78-84 行）
  </read_first>
  <action>
    **修改 PluginConfigWindow.cs:**

    1. **添加 using:**
    ```csharp
    using DalamudMCP.Plugin.Ui.Localization;
    ```

    2. **添加字段:**
    ```csharp
    private readonly IUiLocalization localization;
    private bool languageSwitchPending;
    ```

    3. **修改构造函数，添加 IUiLocalization 参数:**
    ```csharp
    public PluginConfigWindow(
        IUiLocalization localization,
        PluginRuntimeOptions options,
        // ... rest unchanged
    )
    {
        this.localization = localization ?? throw new ArgumentNullException(nameof(localization));
        // 在构造函数末尾订阅 LanguageChanged 事件:
        this.localization.LanguageChanged += OnLanguageChanged;

        model = PluginConfigWindowModel.Create(
            localization,  // 新增第一个参数
            options,
            // ... rest unchanged
        );
    ```

    4. **添加 LanguageChanged 处理器:**
    ```csharp
    private void OnLanguageChanged()
    {
        languageSwitchPending = true;
        RefreshModel(force: true);
    }
    ```

    5. **替换所有字符串字面量** — 将 ImGui 调用中的每个硬编码中文字符串替换为 `localization["key"]`:

    具体替换映射（按 UI-SPEC 定义，总共约 60 处替换）:

    **DrawHeader 方法:**
    - `"DalamudMCP 设置"` (ImGui.Begin) → `localization["window.title"]`
    - `"FFXIV 观察、动作与 MCP 暴露的实时桥接。"` → `localization["header.subtitle"]`
    - `"管道在线"` / `"管道离线"` → `localization["badge.pipe_online"]` / `localization["badge.pipe_offline"]`
    - `"HTTP 在线"` / `"HTTP 已停"` → `localization["badge.http_online"]` / `localization["badge.http_stopped"]`
    - `$"{model.ExposedOperationCount}/{model.OperationCount} 已暴露"` → `string.Format(localization["badge.exposed"], model.ExposedOperationCount, model.OperationCount)`
    - `"顶行为运行时健康状态，下半部分用于操作浏览和可复制命令。"` → `localization["header.hint"]`

    **DrawRuntimePanel 方法:**
    - `"运行时"` → `localization["runtime.panel_title"]`
    - `"连接健康、发现与暴露状态。"` → `localization["runtime.panel_subtitle"]`
    - `"发现"` → `localization["runtime.discovery"]`
    - `"CLI 自动发现已启用"` → `localization["runtime.discovery_value"]`
    - `"命名管道"` → `localization["runtime.pipe"]`
    - `"读取器"` → `localization["runtime.reader"]`
    - `"动作工具"` → `localization["runtime.action_tools"]`
    - `"非安全工具"` → `localization["runtime.unsafe_tools"]`
    - `"操作"` → `localization["runtime.operations"]`
    - `$"{model.OperationCount} 总计  |  {model.ExposedOperationCount} 已暴露  |  {model.BlockedOperationCount} 已限制"` → `string.Format(localization["runtime.operations_value"], model.OperationCount, model.ExposedOperationCount, model.BlockedOperationCount)`
    - `"启用 CLI/MCP 动作操作"` → `localization["runtime.enable_actions"]`
    - `"观察工具保持在线。动作默认关闭，需在此处显式暴露后才能使用。"` → `localization["runtime.enable_actions_hint"]`
    - `"启用非安全集成工具（仅开发者）"` → `localization["runtime.enable_unsafe"]`
    - `"非安全工具可调用任意插件 IPC 功能。除非正在调试其他插件，否则请保持关闭。"` → `localization["runtime.enable_unsafe_hint"]`

    **DrawQuickStart 方法:**
    - `"命令台"` → `localization["quickstart.panel_title"]`
    - `"复制最常用的两个入口点命令。"` → `localization["quickstart.panel_subtitle"]`
    - `"CLI 快速检查"` → `localization["quickstart.cli_card_title"]`
    - `"从活动插件实例读取实时玩家快照。"` → `localization["quickstart.cli_card_desc"]`
    - `"复制玩家上下文命令"` → `localization["quickstart.cli_card_button"]`
    - `"MCP 服务"` → `localization["quickstart.mcp_card_title"]`
    - `"通过插件发现的管道启动本地 MCP 桥接。"` → `localization["quickstart.mcp_card_desc"]`
    - `"复制 MCP 服务命令"` → `localization["quickstart.mcp_card_button"]`

    **DrawAdvancedDetails 方法:**
    - `"高级详情"` → `localization["advanced.header"]`
    - `"管道"` → `localization["advanced.pipe"]`
    - `"CLI 命令"` → `localization["advanced.cli_command"]`
    - `"MCP 服务"` → `localization["advanced.mcp_service"]`
    - `"HTTP 命令"` → `localization["advanced.http_command"]`

    **DrawServerPanel 方法:**
    - `"HTTP 服务器"` → `localization["server.panel_title"]`
    - `"稳定的 MCP 端点，供无需关心管道名的客户端使用。"` → `localization["server.panel_subtitle"]`
    - `"端点"` → `localization["server.endpoint"]`
    - `"HTTP 状态"` → `localization["server.http_status"]`
    - `"插件加载时自动启动 MCP HTTP 服务器"` → `localization["server.auto_start"]`
    - `"启动 MCP HTTP 服务器"` → `localization["server.start_button"]`
    - `"停止 MCP HTTP 服务器"` → `localization["server.stop_button"]`
    - `"复制 MCP 端点"` → `localization["server.copy_endpoint"]`
    - `"复制 MCP 服务器命令"` → `localization["server.copy_command"]`

    **DrawOperations 方法:**
    - `"操作"` (panel_title) → `localization["operations.panel_title"]`
    - `"在将插件交给其他客户端前，筛选暴露的接口。"` → `localization["operations.panel_subtitle"]`
    - `"目录"` → `localization["operations.catalog"]`
    - `$"{model.OperationCount} 总计  |  {model.ActionOperationCount} 动作  |  {model.UnsafeOperationCount} 非安全  |  {model.BlockedOperationCount} 已限制"` → `string.Format(localization["operations.catalog_value"], model.OperationCount, model.ActionOperationCount, model.UnsafeOperationCount, model.BlockedOperationCount)`
    - `"搜索"` → `localization["operations.search"]`
    - `"仅显示已限制"` → `localization["operations.show_blocked"]`
    - `"仅显示有读取器"` → `localization["operations.show_reader"]`
    - `"操作"` (table column) → `localization["operations.table_header_operation"]`
    - `"访问"` → `localization["operations.table_header_access"]`
    - `"状态"` → `localization["operations.table_header_status"]`
    - `"摘要"` → `localization["operations.table_header_summary"]`
    - `"动作"` (tag) → `localization["operations.tag_action"]`
    - `"观察"` (tag) → `localization["operations.tag_observe"]`
    - `"非安全"` (tag) → `localization["operations.tag_unsafe"]`
    - `"可暴露"` (fallback text) → `localization["operations.tag_exposed"]`
    - `"无操作匹配当前筛选条件。"` → `localization["operations.empty"]`

    **注意**: 对于 DrawHeader 方法中标签行的格式字符串 `$"{model.ExposedOperationCount}/{model.OperationCount} 已暴露"`, 应改为使用 `string.Format`:
    ```csharp
    string.Format(localization["badge.exposed"], model.ExposedOperationCount, model.OperationCount)
    ```

    **检查是否遗漏**: 确保 Replace All 替换后的代码中没有任何残留的中文字符串字面量（除了颜色值 `AccentColor` 等保持不变外，所有 ImGui.Text / Button / Checkbox / TableSetupColumn 中的文本参数都应改为 `localization["key"]`）。

    6. **添加 DrawLanguageSelector 方法:**
    ```csharp
    private void DrawLanguageSelector()
    {
        string current = localization.CurrentLanguage;
        string[] languages = ["zh", "en"];
        string[] labels = ["中文", "English"];
        int selectedIndex = Array.IndexOf(languages, current);
        if (selectedIndex < 0) selectedIndex = 0;

        ImGui.SetNextItemWidth(120f);
        if (ImGui.Combo("##lang", ref selectedIndex, labels, labels.Length))
        {
            configurationStore.Update(config =>
                config.SelectedLanguage = languages[selectedIndex]);
            localization.SetLanguage(languages[selectedIndex]);
        }
    }
    ```

    7. **在 DrawHeader 中调用 DrawLanguageSelector:**
    将 `DrawLanguageSelector()` 调用添加到 `DrawHeader()` 方法末尾，在 `ImGui.Separator();` 之前:
    ```csharp
    private void DrawHeader()
    {
        // ... existing title, subtitle, badges ...
        DrawLanguageSelector();
        ImGui.Separator();
    }
    ```

    8. **操作摘要文本本地化**: 在 `DrawOperations()` 中，将 `ImGui.TextWrapped(operation.Summary)` 改为:
    ```csharp
    string opSummary = localization.GetString($"op.{operation.OperationId}.summary");
    ImGui.TextWrapped(opSummary ?? operation.Summary);
    ```

    **修改 PluginEntryPoint.cs:**

    1. **获取 IUiLocalization 服务并初始化语言:**
    在 `PluginEntryPoint` 构造函数中，`compositionRoot.StartAsync()` 之后，`configWindow` 创建之前:
    ```csharp
    var localization = compositionRoot.GetRequiredService<IUiLocalization>();
    if (!string.IsNullOrWhiteSpace(configurationStore.Current.SelectedLanguage))
        localization.SetLanguage(configurationStore.Current.SelectedLanguage);
    ```
    在文件顶部添加 using: `using DalamudMCP.Plugin.Ui.Localization;`

    2. **将 IUiLocalization 传递给 PluginConfigWindow:**
    修改 `configWindow = new PluginConfigWindow(...)` 调用，在第一个参数位置添加 `localization`:
    ```csharp
    configWindow = new PluginConfigWindow(
        localization,
        compositionRoot.Options,
        compositionRoot.ProtocolServer,
        configurationStore,
        mcpServerController,
        operations,
        compositionRoot.GetServices<IPluginReaderStatus>());
    ```

    确保 `localization` 变量在编译器可以看到的作用域内。所有修改都在构造函数中，`localization` 是本地变量，传递给 `configWindow`。
  </action>
  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj --no-restore 2>&1 | grep -i "error" | head -10; test ${PIPESTATUS[0]} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `PluginConfigWindow` 构造函数第一个参数为 `IUiLocalization localization`
    - 所有中文字符串字面量已替换为 `localization["key"]` — grep 确认没有剩余的 `"中文"` 字符串（在 ImGui 调用上下文中）
    - `DrawHeader` 方法包含对 `DrawLanguageSelector()` 的调用
    - `DrawLanguageSelector` 方法包含 `ImGui.Combo("##lang", ...)` 和 `localization.SetLanguage(...)` 调用
    - `LanguageChanged` 事件已订阅，处理器调用 `RefreshModel(force: true)`
    - `PluginEntryPoint` 构造函数使用 `compositionRoot.GetRequiredService<IUiLocalization>()` 并传递到 `PluginConfigWindow`
    - `PluginEntryPoint` 在启动时根据 `configurationStore.Current.SelectedLanguage` 调用 `localization.SetLanguage`
    - 编译通过（零 CS 错误）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 本地化查找 → UI | 所有 UI 文本通过 IUiLocalization 查找，查找失败回退到键名自身 |

## STRIDE 威胁登记表

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | PluginConfigWindow loc["key"] | accept | 缺失键回退为显示键名本身（如 `"runtime.panel_title"`），这对开发者调试有助益，但可能暴露内部键名给终端用户。影响极小：无敏感数据。 |
| T-09-04 | Denial of Service | LanguageChanged event → RefreshModel | accept | 语言切换触发强制刷新，但在单帧内完成。无拒绝服务风险（事件仅由用户交互触发）。 |
</threat_model>

<verification>
- `dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj` 编译成功，退出码 0
- 所有模型文本属性改为 computed getter (验证: grep 确认 `=> loc[` 出现在所有 status 属性中)
- PluginConfigWindow 中无残留中文字符串字面量
- 语言选择器 combo box 代码存在于 DrawHeader 中
- PluginEntryPoint 中检查 SelectedLanguage 初始化代码
</verification>

<success_criteria>
- PluginConfigWindowModel 所有状态文本支持语言切换（computed getter）
- PluginConfigWindow 中所有 ImGui 文本使用实时本地化查找
- 语言选择器 combo box (中文/English) 出现在配置窗口标题栏
- 切换语言后所有 UI 文本立即更新（通过 LanguageChanged → RefreshModel）
- 启动时语言偏好从持久化配置恢复
- 操作摘要支持 loc 键覆盖（回退到英文属性值）
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-language-switch/09-02-SUMMARY.md`
</output>
