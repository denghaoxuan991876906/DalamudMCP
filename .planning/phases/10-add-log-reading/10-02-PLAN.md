---
phase: 10-add-log-reading
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs
  - src/DalamudMCP.Plugin/PluginCompositionRoot.cs
  - src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs
  - src/DalamudMCP.Plugin/PluginEntryPoint.cs
autonomous: true
requirements:
  - LOG-02
  - LOG-03
  - LOG-05
  - LOG-06
  - LOG-07
  - LOG-08
  - LOG-09
user_setup: []

must_haves:
  truths:
    - "ChatLogReadOperation 带有正确的操作属性（Operation, CliCommand, McpTool, ResultFormatter）"
    - "ChatLogReadOperation.Request 带有正确的协议属性（ProtocolOperation, LegacyBridgeRequest）"
    - "ExecuteAsync 通过 ChatLogBufferService 返回 ChatLogSnapshot"
    - "Operation 实现 IPluginReaderStatus，始终为 ready"
    - "IChatGui 通过 DI 链正确注入：PluginEntryPoint → PluginCompositionRoot → PluginServiceCollectionExtensions"
    - "ChatLogBufferService 在 DI 中注册为 singleton"
    - "CLI 命令 `chat read` 可调用此操作"
    - "MCP 工具 `get_chat_log` 可调用此操作"
  artifacts:
    - path: "src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs"
      provides: "Chat log query IOperation implementation"
      min_lines: 150
      exports: ["ChatLogReadOperation", "ChatLogSnapshot", "ChatLogReadOperation.Request"]
    - path: "src/DalamudMCP.Plugin/PluginCompositionRoot.cs"
      provides: "IChatGui parameter added to CreateFromDalamud"
      contains: "IChatGui chatGui"
    - path: "src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs"
      provides: "IChatGui + ChatLogBufferService DI registration"
      contains: "chatGui"
    - path: "src/DalamudMCP.Plugin/PluginEntryPoint.cs"
      provides: "IChatGui constructor parameter and forwarding"
      contains: "IChatGui chatGui"
  key_links:
    - from: "PluginEntryPoint"
      to: "PluginCompositionRoot.CreateFromDalamud"
      via: "IChatGui chatGui parameter"
      pattern: "chatGui"
    - from: "PluginCompositionRoot.CreateFromDalamud"
      to: "PluginServiceCollectionExtensions.BuildDalamudServiceProvider"
      via: "IChatGui chatGui parameter"
      pattern: "chatGui"
    - from: "PluginServiceCollectionExtensions.BuildDalamudServiceProvider"
      to: "services.AddSingleton(chatGui)"
      via: "IChatGui singleton registration"
      pattern: "AddSingleton.*chatGui"
    - from: "PluginServiceCollectionExtensions.BuildDalamudServiceProvider"
      to: "services.AddSingleton<ChatLogBufferService>()"
      via: "ChatLogBufferService registration"
      pattern: "ChatLogBufferService"
    - from: "ChatLogReadOperation"
      to: "ChatLogBufferService"
      via: "constructor injection"
      pattern: "ChatLogBufferService logBuffer"
---

<objective>
创建 ChatLogReadOperation（标准 IOperation 拉取实现）并完成 DI 链中 IChatGui 和 ChatLogBufferService 的注册。

Purpose: 实现 MCP/CLI 可调用的 `chat.read` 操作，通过 ChatLogBufferService 查询最近的聊天日志。同时将 IChatGui 和 ChatLogBufferService 注册到 DI 容器。

Output:
- `src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` — 新操作文件
- 修改 3 个现有文件完成 DI 链
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-log-reading/10-RESEARCH.md
@.planning/phases/10-add-log-reading/10-01-SUMMARY.md

<interfaces>
From plan 10-01 (ChatLogBufferService):
```csharp
// namespace DalamudMCP.Plugin.Services
public sealed class ChatLogBufferService : IDisposable
{
    public ChatLogBufferService(IChatGui chatGui, int maxCapacity = 1000);
    public IReadOnlyList<ChatLogEntry> GetRecent(
        XivChatType[]? channels = null,
        DateTimeOffset? since = null,
        int maxCount = 100);
    public void Dispose();
}

[MemoryPackable]
public sealed partial record ChatLogEntry(
    Guid Id,
    DateTimeOffset Timestamp,
    XivChatType Type,
    string ChannelName,
    uint SenderId,
    string? SenderName,
    string Message,
    XivChatRelationKind SourceKind,
    XivChatRelationKind TargetKind);
```

Existing PluginCompositionRoot.CreateFromDalamud signature:
```csharp
public static PluginCompositionRoot CreateFromDalamud(
    IDalamudPluginInterface pluginInterface,
    Configuration.PluginUiConfigurationStore configurationStore,
    IFramework framework,
    IClientState clientState,
    ICondition condition,
    IObjectTable objectTable,
    IPlayerState playerState,
    IGameInventory gameInventory,
    IFateTable fateTable,
    IDataManager dataManager,
    IGameGui gameGui,
    ITargetManager targetManager,
    string? pipeName = null)
```

Existing PluginServiceCollectionExtensions.BuildDalamudServiceProvider signature:
```csharp
public static ServiceProvider BuildDalamudServiceProvider(
    IDalamudPluginInterface pluginInterface,
    Configuration.PluginUiConfigurationStore configurationStore,
    PluginRuntimeOptions options,
    IFramework framework,
    IClientState clientState,
    ICondition condition,
    IObjectTable objectTable,
    IPlayerState playerState,
    IGameInventory gameInventory,
    IFateTable fateTable,
    IDataManager dataManager,
    IGameGui gameGui,
    ITargetManager targetManager)
```

Existing PluginEntryPoint constructor signature:
```csharp
public PluginEntryPoint(
    IDalamudPluginInterface pluginInterface,
    IFramework framework,
    IClientState clientState,
    ICondition condition,
    IObjectTable objectTable,
    IPlayerState playerState,
    IGameInventory gameInventory,
    IFateTable fateTable,
    IDataManager dataManager,
    IGameGui gameGui,
    ITargetManager targetManager)
```

Existing IPluginReaderStatus interface:
```csharp
namespace DalamudMCP.Plugin.Readers;
public interface IPluginReaderStatus
{
    public string ReaderKey { get; }
    public bool IsReady { get; }
    public string Detail { get; }
}
```

Existing NearbyInteractablesOperation pattern (dual-constructor):
```csharp
[Operation("nearby.interactables", Description = "...", Summary = "...")]
[ResultFormatter(typeof(NearbyInteractablesOperation.TextFormatter))]
[CliCommand("nearby", "interactables")]
[McpTool("get_nearby_interactables")]
public sealed partial class NearbyInteractablesOperation
    : IOperation<NearbyInteractablesOperation.Request, NearbyInteractablesSnapshot>, IPluginReaderStatus
{
    // Public constructor: takes Dalamud services, creates real executor
    // Internal constructor: takes executor func + readiness, for tests
    // ExecuteAsync: delegates to executor func
    // IPluginReaderStatus: ReaderKey, IsReady, Detail
    // TextFormatter: nested class implementing IResultFormatter<TResult>
}
```

Option attribute usage:
```csharp
[Option("channels", Description = "Chat channels to filter by...", Required = false)]
public string[]? Channels { get; init; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ChatLogReadOperation</name>
  <files>src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs</files>
  <read_first>
    - src/DalamudMCP.Plugin/Operations/NearbyInteractablesOperation.cs（参考现有 dual-constructor 模式）
    - src/DalamudMCP.Plugin/Readers/IPluginReaderStatus.cs（IPluginReaderStatus 接口）
  </read_first>
  <action>
    在 `src/DalamudMCP.Plugin/Operations/` 下创建 `ChatLogReadOperation.cs`。

    使用以下 using 语句：
    ```csharp
    using System.Runtime.Versioning;
    using Dalamud.Game.Text;
    using DalamudMCP.Framework;
    using DalamudMCP.Plugin.Readers;
    using DalamudMCP.Plugin.Services;
    using DalamudMCP.Protocol;
    using MemoryPack;
    ```

    创建完整的 ChatLogReadOperation 类（partial），包含以下内容：

    **类属性：**
    ```csharp
    [Operation("chat.read", Description = "Reads recent chat/combat/system log entries.", Summary = "Gets recent chat log entries.")]
    [ResultFormatter(typeof(ChatLogReadOperation.TextFormatter))]
    [CliCommand("chat", "read")]
    [McpTool("get_chat_log")]
    public sealed partial class ChatLogReadOperation
        : IOperation<ChatLogReadOperation.Request, ChatLogSnapshot>, IPluginReaderStatus
    ```

    **内部字段：**
    - `private readonly Func<Request, CancellationToken, ValueTask<ChatLogSnapshot>> executor;`
    - `private readonly Func<bool>? isReadyProvider;`
    - `private readonly Func<string>? detailProvider;`
    - `private readonly string unavailableDetail;`

    **公共构造函数**（接收 ChatLogBufferService，标记 `[SupportedOSPlatform("windows")]`）：
    ```csharp
    [SupportedOSPlatform("windows")]
    public ChatLogReadOperation(ChatLogBufferService logBuffer)
    {
        ArgumentNullException.ThrowIfNull(logBuffer);
        executor = CreateExecutor(logBuffer);
        isReadyProvider = () => true; // 缓冲区存在即 ready
        detailProvider = () => "ready";
        unavailableDetail = "ready";
    }
    ```

    **内部构造函数**（用于测试）：
    ```csharp
    internal ChatLogReadOperation(
        Func<Request, CancellationToken, ValueTask<ChatLogSnapshot>> executor,
        bool isReady = true,
        string detail = "ready")
    {
        this.executor = executor ?? throw new ArgumentNullException(nameof(executor));
        isReadyProvider = () => isReady;
        detailProvider = () => string.IsNullOrWhiteSpace(detail) ? "ready" : detail;
        unavailableDetail = string.IsNullOrWhiteSpace(detail) ? "ready" : detail;
    }
    ```

    **IPluginReaderStatus 实现：**
    ```csharp
    public string ReaderKey => "chat.read";
    public bool IsReady => isReadyProvider?.Invoke() ?? false;
    public string Detail => detailProvider?.Invoke() ?? unavailableDetail;
    ```

    **ExecuteAsync 方法：**
    ```csharp
    public ValueTask<ChatLogSnapshot> ExecuteAsync(Request request, OperationContext context)
    {
        ArgumentNullException.ThrowIfNull(request);
        ArgumentNullException.ThrowIfNull(context);
        return executor(request, context.CancellationToken);
    }
    ```

    **Request 类**（MemoryPackable，嵌套在操作类中）：
    ```csharp
    [MemoryPackable]
    [ProtocolOperation("chat.read")]
    [LegacyBridgeRequest("ReadChatLog")]
    public sealed partial class Request
    {
        [Option("channels", Description = "Chat channels to filter by (e.g., Say,Party,System). Empty = all.", Required = false)]
        public string[]? Channels { get; init; }

        [Option("since", Description = "Only return entries after this UTC timestamp (ISO 8601).", Required = false)]
        public DateTimeOffset? Since { get; init; }

        [Option("max-count", Description = "Maximum number of entries to return.", Required = false)]
        public int? MaxCount { get; init; }
    }
    ```

    **TextFormatter**（嵌套类）：
    ```csharp
    public sealed class TextFormatter : IResultFormatter<ChatLogSnapshot>
    {
        public string? FormatText(ChatLogSnapshot result, OperationContext context)
        {
            ArgumentNullException.ThrowIfNull(result);
            ArgumentNullException.ThrowIfNull(context);
            return result.SummaryText;
        }
    }
    ```

    **CreateExecutor 私有方法**：
    ```csharp
    [SupportedOSPlatform("windows")]
    private static Func<Request, CancellationToken, ValueTask<ChatLogSnapshot>> CreateExecutor(
        ChatLogBufferService logBuffer)
    {
        return async (request, cancellationToken) =>
        {
            cancellationToken.ThrowIfCancellationRequested();

            // 解析 Request 参数
            // Channels: string[]? → XivChatType[]? — 如果 request.Channels 有值，进行转换
            // Since: DateTimeOffset? — 直接使用
            // MaxCount: int? — 限制最大 500，否则使用默认值 100

            XivChatType[]? channelFilter = null;
            if (request.Channels is { Length: > 0 })
            {
                var types = new List<XivChatType>(request.Channels.Length);
                foreach (string channelName in request.Channels)
                {
                    if (Enum.TryParse<XivChatType>(channelName, ignoreCase: true, out var parsed))
                        types.Add(parsed);
                    // 静默跳过无法解析的字符串（非致命错误）
                }
                channelFilter = types.Count > 0 ? types.ToArray() : null;
            }

            DateTimeOffset? since = request.Since;
            int maxCount = request.MaxCount is > 0 ? Math.Min(request.MaxCount.Value, 500) : 100;

            IReadOnlyList<ChatLogEntry> entries = logBuffer.GetRecent(channelFilter, since, maxCount);

            return new ChatLogSnapshot(
                DateTimeOffset.UtcNow,
                [.. entries],
                entries.Count,
                $"{entries.Count} log entries returned.");
        };
    }
    ```

    **ChatLogSnapshot**（在文件底部，与 ChatLogReadOperation 同级 namespace）：
    ```csharp
    [MemoryPackable]
    public sealed partial record ChatLogSnapshot(
        DateTimeOffset CapturedAt,
        ChatLogEntry[] Entries,
        int TotalFilteredCount,
        string SummaryText);
    ```

    实现注意事项：
    1. `[SupportedOSPlatform("windows")]` 标记在公共构造函数和 CreateExecutor 上（遵循现有操作模式）
    2. 参数规范化：Channels 字符串数组使用 `Enum.TryParse<XivChatType>(ignoreCase: true)` 进行转换，静默忽略无法解析的值
    3. max-count 上限为 500（安全限制，防止 DoS）
    4. ChatLogSnapshot.Entries 使用 collection expression `[.. entries]`
  </action>
  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj 2>&1 | grep -i "error" | head -5; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\[Operation(' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空（OperationAttribute）
    - `grep -n '\[CliCommand' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `grep -n '\[McpTool' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `grep -n '\[ResultFormatter' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `grep -n 'IOperation<ChatLogReadOperation.Request, ChatLogSnapshot>' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `grep -n 'IPluginReaderStatus' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `grep -n '\[ProtocolOperation' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空（在 Request 类上）
    - `grep -n '\[LegacyBridgeRequest' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `grep -n 'class TextFormatter' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `grep -n 'sealed partial record ChatLogSnapshot' src/DalamudMCP.Plugin/Operations/ChatLogReadOperation.cs` 返回非空
    - `dotnet build` 编译通过（假设 ChatLogBufferService 和 ChatLogEntry 在 Plan 01 中已创建完成）
  </acceptance_criteria>
  <done>ChatLogReadOperation 完整实现，包含 Request、Executor、TextFormatter、ChatLogSnapshot，可在 CLi 和 MCP 中注册</done>
</task>

<task type="auto">
  <name>Task 2: 将 IChatGui 注入 DI 链</name>
  <files>
    src/DalamudMCP.Plugin/PluginCompositionRoot.cs
    src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs
    src/DalamudMCP.Plugin/PluginEntryPoint.cs
  </files>
  <read_first>
    - src/DalamudMCP.Plugin/PluginCompositionRoot.cs（当前状态）
    - src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs（当前状态）
    - src/DalamudMCP.Plugin/PluginEntryPoint.cs（当前状态）
  </read_first>
  <action>
    按以下顺序修改三个文件，将 IChatGui 从 PluginEntryPoint 一路传递到 DI 注册。

    **文件 1: PluginEntryPoint.cs**

    1. 在构造函数参数列表末尾添加 `IChatGui chatGui` 参数（在所有现有参数之后，`ITargetManager targetManager` 之后）：
    ```csharp
    public PluginEntryPoint(
        IDalamudPluginInterface pluginInterface,
        IFramework framework,
        IClientState clientState,
        ICondition condition,
        IObjectTable objectTable,
        IPlayerState playerState,
        IGameInventory gameInventory,
        IFateTable fateTable,
        IDataManager dataManager,
        IGameGui gameGui,
        ITargetManager targetManager,
        IChatGui chatGui)   // ← NEW PARAMETER
    ```

    2. 在构造函数体内添加 null 检查：
    ```csharp
    ArgumentNullException.ThrowIfNull(chatGui);  // 与其他 ArgumentNullException.ThrowIfNull 检查放在一起
    ```

    3. 在 `compositionRoot = PluginCompositionRoot.CreateFromDalamud(` 调用中，在 `targetManager` 之后、`)` 之前添加 `chatGui` 参数：
    ```csharp
    compositionRoot = PluginCompositionRoot.CreateFromDalamud(
        pluginInterface,
        configurationStore,
        framework,
        clientState,
        condition,
        objectTable,
        playerState,
        gameInventory,
        fateTable,
        dataManager,
        gameGui,
        targetManager,
        chatGui);  // ← ADD AFTER targetManager
    ```

    **文件 2: PluginCompositionRoot.cs**

    1. 在 `CreateFromDalamud` 方法签名中，在 `ITargetManager targetManager` 之后添加 `IChatGui chatGui` 参数：
    ```csharp
    public static PluginCompositionRoot CreateFromDalamud(
        IDalamudPluginInterface pluginInterface,
        Configuration.PluginUiConfigurationStore configurationStore,
        IFramework framework,
        IClientState clientState,
        ICondition condition,
        IObjectTable objectTable,
        IPlayerState playerState,
        IGameInventory gameInventory,
        IFateTable fateTable,
        IDataManager dataManager,
        IGameGui gameGui,
        ITargetManager targetManager,
        IChatGui chatGui,     // ← NEW PARAMETER
        string? pipeName = null)
    ```

    2. 添加 null 检查（按字母顺序放在 `ArgumentNullException.ThrowIfNull(targetManager);` 之后）：
    ```csharp
    ArgumentNullException.ThrowIfNull(chatGui);
    ```

    3. 在 `hosting.PluginServiceCollectionExtensions.BuildDalamudServiceProvider(` 调用中，在 `targetManager` 之后添加 `chatGui`：
    ```csharp
    ServiceProvider serviceProvider = Hosting.PluginServiceCollectionExtensions.BuildDalamudServiceProvider(
        pluginInterface,
        configurationStore,
        options,
        framework,
        clientState,
        condition,
        objectTable,
        playerState,
        gameInventory,
        fateTable,
        dataManager,
        gameGui,
        targetManager,
        chatGui);   // ← ADD AFTER targetManager
    ```

    **文件 3: PluginServiceCollectionExtensions.cs**

    1. 在 `BuildDalamudServiceProvider` 方法签名中，在 `ITargetManager targetManager` 之后添加 `IChatGui chatGui` 参数：
    ```csharp
    public static ServiceProvider BuildDalamudServiceProvider(
        IDalamudPluginInterface pluginInterface,
        Configuration.PluginUiConfigurationStore configurationStore,
        PluginRuntimeOptions options,
        IFramework framework,
        IClientState clientState,
        ICondition condition,
        IObjectTable objectTable,
        IPlayerState playerState,
        IGameInventory gameInventory,
        IFateTable fateTable,
        IDataManager dataManager,
        IGameGui gameGui,
        ITargetManager targetManager,
        IChatGui chatGui)   // ← NEW PARAMETER
    ```

    2. 添加 null 检查（与其他检查放在一起）：
    ```csharp
    ArgumentNullException.ThrowIfNull(chatGui);
    ```

    3. 在 `services.AddSingleton(targetManager);` 之后添加以下两行注册：
    ```csharp
    services.AddSingleton(chatGui);
    services.AddSingleton<Services.ChatLogBufferService>();
    ```

    注意：IChatGui 是通过 DI 注册为实例（`services.AddSingleton(chatGui)`）而不是类型（`services.AddSingleton<IChatGui>()`），因为 IChatGui 实例由 Dalamud 提供并注入到 PluginEntryPoint 构造函数中。

    确保 `using Dalamud.Plugin.Services;` 在所有三个文件顶部已存在（PluginCompositionRoot.cs 和 PluginServiceCollectionExtensions.cs 已有此引入，PluginEntryPoint.cs 也已有）。
  </action>
  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj 2>&1 | grep -i "error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - PluginEntryPoint.cs 构造函数包含 `IChatGui chatGui` 参数
    - PluginCompositionRoot.cs `CreateFromDalamud` 包含 `IChatGui chatGui` 参数
    - PluginServiceCollectionExtensions.cs `BuildDalamudServiceProvider` 包含 `IChatGui chatGui` 参数
    - PluginServiceCollectionExtensions.cs 中包含 `services.AddSingleton(chatGui);`
    - PluginServiceCollectionExtensions.cs 中包含 `services.AddSingleton<Services.ChatLogBufferService>();`
    - `dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj` 返回 exit code 0（编译成功）
  </acceptance_criteria>
  <done>DI 链完整连接：IChatGui 从 Dalamud 注入 PluginEntryPoint，经 PluginCompositionRoot 传递到 PluginServiceCollectionExtensions，ChatLogBufferService 在 DI 中注册为 singleton</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/CLI → ChatLogReadOperation | 外部请求通过 Source Generator 生成的分发器进入操作 |
| ChatLogReadOperation → ChatLogBufferService | 操作通过 DI 获取缓冲区服务，只读访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | DoS | max-count 参数过大 | mitigate | Request 规范化中限制 maxCount ≤ 500（`Math.Min(request.MaxCount.Value, 500)`） |
| T-10-05 | Tampering | channels 参数无效枚举值 | mitigate | 使用 `Enum.TryParse<XivChatType>(ignoreCase: true)` 静默忽略无效值 |
| T-10-06 | Tampering | since 参数为未来时间戳 | accept | 未来时间戳不会导致崩溃，只是返回空结果；对日志读取场景无安全影响 |
| T-10-07 | Information Disclosure | 私聊(Tell)内容通过 MCP 暴露 | accept | Tell 内容是用户可见的游戏内信息，属于正常功能范围；通过 PluginOperationExposurePolicy 可在未来控制暴露 |
</threat_model>

<verification>
1. `dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj` 编译通过，exit code 0
2. `grep -c "chatGui" src/DalamudMCP.Plugin/PluginEntryPoint.cs` >= 2（参数声明 + 传递）
3. `grep -c "chatGui" src/DalamudMCP.Plugin/PluginCompositionRoot.cs` >= 2（参数声明 + 传递）
4. `grep -c "chatGui\|ChatLogBufferService" src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs` >= 2
</verification>

<success_criteria>
1. ChatLogReadOperation 完整实现 dual-constructor 模式
2. Operation 属性齐全：Operation("chat.read"), CliCommand("chat", "read"), McpTool("get_chat_log"), ResultFormatter
3. Request 带有 ProtocolOperation("chat.read") 和 LegacyBridgeRequest("ReadChatLog")
4. ExecuteAsync 支持 channels/since/max-count 过滤参数
5. ChatLogSnapshot 为 MemoryPackable record，包含 Entries 数组
6. IChatGui 从 PluginEntryPoint → PluginCompositionRoot → PluginServiceCollectionExtensions 完整传递
7. PluginServiceCollectionExtensions 中注册了 IChatGui 实例和 ChatLogBufferService
8. 整个 Plugin 项目编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-log-reading/10-02-SUMMARY.md`
</output>
