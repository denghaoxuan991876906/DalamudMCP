---
phase: 12-plugin-reload
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeExposedPlugin.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginReloadOperationTests.cs
autonomous: true
requirements:
  - RELOAD-01

must_haves:
  truths:
    - "PluginReloadOperation 在目标插件存在时返回 reload_initiated"
    - "PluginReloadOperation 在目标插件不存在时返回 plugin_not_found"
    - "PluginReloadOperation 在 Reload() 抛异常时返回 reload_failed"
    - "PluginReloadOperation 在请求自身重载时返回 self_reload_blocked"
    - "PluginReloadOperation 在空/空白 plugin_name 时抛出 ArgumentException"
  artifacts:
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeExposedPlugin.cs"
      provides: "IExposedPlugin 测试桩"
      contains: "class FakeExposedPlugin"
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/PluginReloadOperationTests.cs"
      provides: "PluginReloadOperation 单元测试"
      contains: "class PluginReloadOperationTests"
  key_links:
    - from: "PluginReloadOperationTests.cs"
      to: "FakeExposedPlugin.cs"
      via: "using DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc"
    - from: "PluginReloadOperationTests.cs"
      to: "PluginReloadOperation.cs"
      via: "internal 测试构造函数（Func 委托）"
---

<objective>
为 PluginReloadOperation 创建测试桩和单元测试，覆盖所有 4 个状态码路径及输入验证。

Purpose: 确保插件重载逻辑正确，各状态码路径均被测试覆盖。
Output: FakeExposedPlugin.cs（测试桩）、PluginReloadOperationTests.cs（单元测试）
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-plugin-reload/12-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- 本计划需要使用的现有测试基础设施和模式 -->

从 tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs：
```csharp
// 测试桩模式：实现接口，字典驱动，public 类供跨阶段复用
namespace DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;

public sealed class FakeIpcGateway : IPluginIpcGateway
{
    private readonly Dictionary<string, IPluginCallGateSubscriber> subscribers;

    public FakeIpcGateway(params (string Callgate, IPluginCallGateSubscriber Subscriber)[] entries) { ... }

    public bool TryCreate(string callgate, IReadOnlyList<Type> typeArguments, out IPluginCallGateSubscriber? subscriber) { ... }
}
```

从 tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcCallGateSubscriber.cs：
```csharp
// 另一个测试桩，同样遵循 public + 字典驱动模式
namespace DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;

public sealed class FakeIpcCallGateSubscriber : IPluginCallGateSubscriber
{
    public bool HasFunction { get; set; }
    public Func<IReadOnlyList<object?>, object?>? InvokeFuncImpl { get; set; }
    public object? InvokeFunc(IReadOnlyList<object?> arguments) { ... }
}
```

从 tests/DalamudMCP.Plugin.Operations.Tests/PluginIpcGatewayTests.cs（测试模式参考）：
```csharp
// xUnit v3 + NSubstitute 的测试结构
public sealed class PluginIpcGatewayTests
{
    [Fact]
    public void Constructor_RejectsNullInterface() { ... }

    [Fact]
    public void TryCreate_ReturnsSubscriber_WhenCallgateExists() { ... }

    [Fact]
    public void TryCreate_ReturnsFalse_WhenCallgateNotFound() { ... }
}
```

从 12-RESEARCH.md §5：
```csharp
// 需要的测试桩接口
public interface IExposedPlugin  // Dalamud.Plugin
{
    string InternalName { get; }
    string Name { get; }
    void Reload();
}
```

从 12-RESEARCH.md §5.1 测试用例表：
| 测试场景 | 预期结果 |
|----------|---------|
| 正常重载已安装插件 | Success=true, Status=reload_initiated |
| 目标插件未安装 | Success=false, Status=plugin_not_found |
| Reload() 抛出异常 | Success=false, Status=reload_failed, ErrorMessage 包含异常消息 |
| 请求重载自身 | Success=false, Status=self_reload_blocked |
| 空/null 插件名 | ArgumentException |
| InternalName 大小写不敏感匹配 | 正确匹配已安装插件 |

从 src/DalamudMCP.Plugin/Operations/PluginReloadOperation.cs（Plan 01 产物）：
```csharp
// internal 测试构造函数签名
internal PluginReloadOperation(Func<Request, CancellationToken, ValueTask<PluginReloadResult>> executor)

// Request 结构
public sealed partial class Request { public string PluginName { get; init; } = string.Empty; }

// Result 结构
public sealed partial record PluginReloadResult(string PluginName, bool Success, string Status, string? ErrorMessage, string SummaryText);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 FakeExposedPlugin 测试桩</name>
  <files>tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeExposedPlugin.cs</files>
  <read_first>
    - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs（测试桩模式参考）
    - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcCallGateSubscriber.cs（测试桩模式参考）
  </read_first>
  <action>
创建新文件 `tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeExposedPlugin.cs`，实现 Dalamud 的 `IExposedPlugin` 接口作为测试桩。

**类声明和 using：**
```csharp
using Dalamud.Plugin;

namespace DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;

public sealed class FakeExposedPlugin : IExposedPlugin
```

**属性（全部可读写，用于测试配置）：**
```csharp
public string InternalName { get; set; } = string.Empty;
public string Name { get; set; } = string.Empty;
```

**Reload 行为控制：**
```csharp
/// <summary>
/// 设置为 true 使下一次 Reload() 调用抛出 InvalidOperationException。
/// 调用后自动重置为 false。
/// </summary>
public bool ThrowOnReload { get; set; }

/// <summary>
/// Reload() 被调用的次数计数器。
/// </summary>
public int ReloadCallCount { get; private set; }
```

**Reload() 实现：**
```csharp
public void Reload()
{
    ReloadCallCount++;
    if (ThrowOnReload)
    {
        ThrowOnReload = false;
        throw new InvalidOperationException("Simulated reload failure");
    }
}
```

**注意：** `IExposedPlugin` 接口定义在 `Dalamud.Plugin` 命名空间（Dalamud.dll）。测试项目引用了 Dalamud，因此该接口可用。可能需要实现接口的全部成员（除 `InternalName`、`Name`、`Reload()` 外还有 `PluginManifest` 等——通过 IDE 快速修复自动生成其他成员的默认实现：`throw new NotImplementedException()`）。

**代码风格：**
- 显式 public 访问修饰符
- summary XML 文档注释（中文）
- file-scoped namespace
</action>
  <acceptance_criteria>
    - `FakeExposedPlugin.cs` 文件存在于 `tests/.../TestShared/Ipc/` 目录
    - 类实现 `IExposedPlugin` 接口
    - 包含 `InternalName` 属性（可读写 string）
    - 包含 `Name` 属性（可读写 string）
    - 包含 `ThrowOnReload` 属性（bool，调用后自动重置）
    - 包含 `ReloadCallCount` 属性（int，只读）
    - `Reload()` 方法记录调用次数
    - `ThrowOnReload=true` 时 `Reload()` 抛出 `InvalidOperationException`
    - `dotnet build tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj` 0 错误
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --no-restore 2>&1</automated>
  </verify>
  <done>FakeExposedPlugin.cs 编译通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 PluginReloadOperationTests.cs 单元测试</name>
  <files>tests/DalamudMCP.Plugin.Operations.Tests/PluginReloadOperationTests.cs</files>
  <read_first>
    - src/DalamudMCP.Plugin/Operations/PluginReloadOperation.cs（Plan 01 产物，目标实现）
    - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeExposedPlugin.cs（Task 1 产物，测试桩）
    - tests/DalamudMCP.Plugin.Operations.Tests/PluginIpcGatewayTests.cs（测试结构模式参考）
  </read_first>
  <behavior>
    - Test 1: 正常重载已安装插件 → Success=true, Status="reload_initiated", ReloadCallCount=1
    - Test 2: 目标插件未安装 → Success=false, Status="plugin_not_found", ErrorMessage 非空
    - Test 3: Reload() 抛出异常 → Success=false, Status="reload_failed", ErrorMessage 包含 "Simulated reload failure"
    - Test 4: 请求重载自身 → Success=false, Status="self_reload_blocked"
    - Test 5: 空 plugin_name → ArgumentException（通过 ThrowIfNullOrWhiteSpace）
    - Test 6: 大小写不敏感匹配 → InternalName 与请求名称大小写不同时仍成功重载
    - Test 7: 构造函数拒绝 null pluginInterface → ArgumentNullException
    - Test 8: 构造函数拒绝 null framework → ArgumentNullException
  </behavior>
  <action>
创建新文件 `tests/DalamudMCP.Plugin.Operations.Tests/PluginReloadOperationTests.cs`。

**TDD 流程：先 RED（创建测试，预期失败），后 GREEN（确认已有实现通过）。**

由于 PluginReloadOperation.cs 已在 Plan 01 中创建，本测试在已有实现上验证。采用标准测试模式（非严格 TDD RED/GREEN，因为实现已存在）。

**using 语句：**
```csharp
using Dalamud.Plugin;
using Dalamud.Plugin.Services;
using DalamudMCP.Plugin.Operations;
using DalamudMCP.Plugin.Operations.Tests.TestShared.Ipc;
using NSubstitute;
```

**类声明：**
```csharp
namespace DalamudMCP.Plugin.Operations.Tests;

public sealed class PluginReloadOperationTests
```

**辅助方法 `CreateMockFramework`（创建 IFramework mock）：**
```csharp
private static IFramework CreateMockFramework(bool isInFrameworkThread = false)
{
    var framework = Substitute.For<IFramework>();
    framework.IsInFrameworkUpdateThread.Returns(isInFrameworkThread);
    // RunOnFrameworkThread 直接同步执行传入的 Action（简化测试）
    framework.RunOnFrameworkThread(Arg.Any<Action>())
        .Returns(callInfo => { ((Action)callInfo[0])(); return Task.CompletedTask; });
    return framework;
}
```

**辅助方法 `CreateMockPluginInterface`（创建 IDalamudPluginInterface mock）：**
```csharp
private static IDalamudPluginInterface CreateMockPluginInterface(
    IEnumerable<IExposedPlugin>? installedPlugins = null,
    string internalName = "DalamudMCP")
{
    var pluginInterface = Substitute.For<IDalamudPluginInterface>();
    pluginInterface.InternalName.Returns(internalName);
    pluginInterface.InstalledPlugins.Returns(installedPlugins ?? []);
    return pluginInterface;
}
```

**测试用例（共 8 个 [Fact]）：**

**Test 1: ExecuteAsync_ReturnsReloadInitiated_WhenPluginFoundAndReloadSucceeds**
- 创建 FakeExposedPlugin（InternalName = "TargetPlugin"）
- 创建 mock pluginInterface（InstalledPlugins = [fakePlugin]，InternalName = "DalamudMCP"）
- 创建 mock framework（isInFrameworkThread = true）
- 构造 PluginReloadOperation
- 执行 Request { PluginName = "TargetPlugin" }
- 断言：Success = true, Status = "reload_initiated", ReloadCallCount = 1

**Test 2: ExecuteAsync_ReturnsPluginNotFound_WhenPluginNotInstalled**
- 创建 mock pluginInterface（InstalledPlugins 为空列表）
- 创建 mock framework
- 构造 PluginReloadOperation
- 执行 Request { PluginName = "NonExistent" }
- 断言：Success = false, Status = "plugin_not_found", ErrorMessage 包含 "NonExistent"

**Test 3: ExecuteAsync_ReturnsReloadFailed_WhenReloadThrows**
- 创建 FakeExposedPlugin（InternalName = "TargetPlugin"，ThrowOnReload = true）
- 创建完整的 mock 环境
- 执行 Request { PluginName = "TargetPlugin" }
- 断言：Success = false, Status = "reload_failed", ErrorMessage 包含 "Simulated reload failure"

**Test 4: ExecuteAsync_ReturnsSelfReloadBlocked_WhenRequestingOwnPlugin**
- 创建 mock pluginInterface（InternalName = "DalamudMCP"）
- 创建 mock framework
- 构造 PluginReloadOperation
- 执行 Request { PluginName = "DalamudMCP" }
- 断言：Success = false, Status = "self_reload_blocked"

**Test 5: ExecuteAsync_ReturnsSelfReloadBlocked_CaseInsensitive**
- 同上，但使用 PluginName = "dalamudmcp"（小写）
- 断言：Success = false, Status = "self_reload_blocked"

**Test 6: ExecuteAsync_MatchesPluginIgnoreCase**
- 创建 FakeExposedPlugin（InternalName = "TargetPlugin"）
- 执行 Request { PluginName = "targetplugin" }（小写）
- 断言：Success = true, Status = "reload_initiated"

**Test 7: Constructor_RejectsNullPluginInterface**
- Assert.Throws<ArgumentNullException>(() => new PluginReloadOperation(null!, Substitute.For<IFramework>()))

**Test 8: Constructor_RejectsNullFramework**
- Assert.Throws<ArgumentNullException>(() => new PluginReloadOperation(Substitute.For<IDalamudPluginInterface>(), null!))

**额外验证：** Test 5 和 Test 6 覆盖了 RESEARCH.md 中的 "InternalName 大小写不敏感匹配" 和 "请求重载自身" 两个场景。若 `ArgumentException.ThrowIfNullOrWhiteSpace` 对空字符串的行为需要验证，添加 Test 9：

**Test 9: ExecuteAsync_ThrowsArgumentException_WhenPluginNameEmpty**
- 使用 internal 测试构造函数（避免复杂的 mock 设置）
- 构造：`new PluginReloadOperation((req, ct) => throw new ArgumentException("plugin-name is required."))`
- 或更简单：通过 public 构造函数 + mock（返回时抛 ArgumentException）
- 更好的方式：直接测试 public 构造函数的完整路径

**关于 internal 测试构造函数的用法：**
由于 `pluginInterface.InternalName` 和 `InstalledPlugins` 需要通过 NSubstitute mock，所有测试使用 public DI 构造函数。对于单纯参数验证测试，使用 internal 测试构造函数简化设置，或通过 mock 配置。

**测试文件结构：**
- 辅助方法先定义
- [Fact] 测试方法按上述编号排列
- 每个测试独立，无共享状态

**代码风格：**
- xUnit v3 测试方法命名：蛇形命名法（`Method_ExpectedBehavior_WhenCondition`）
- 显式 public 访问修饰符
- file-scoped namespace
</action>
  <acceptance_criteria>
    - `PluginReloadOperationTests.cs` 文件存在
    - 包含至少 8 个 `[Fact]` 测试方法
    - 测试 1 验证 `reload_initiated` 路径
    - 测试 2 验证 `plugin_not_found` 路径
    - 测试 3 验证 `reload_failed` 路径
    - 测试 4 验证 `self_reload_blocked` 路径
    - 测试覆盖大小写不敏感匹配
    - 测试覆盖构造参数 null 验证
    - `dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "FullyQualifiedName~PluginReloadOperationTests"` 全部通过
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --no-build --filter "FullyQualifiedName~PluginReloadOperationTests" 2>&1</automated>
  </verify>
  <done>所有 PluginReloadOperationTests 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 | 无信任边界——测试仅验证逻辑正确性 |

## STRIDE Threat Register

测试代码无安全威胁——无威胁登记。
</threat_model>

<verification>
构建验证：
```bash
dotnet build tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj
```

测试验证：
```bash
dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "FullyQualifiedName~PluginReloadOperationTests"
```

预期：所有测试通过，0 失败。
</verification>

<success_criteria>
- [ ] FakeExposedPlugin.cs 创建并编译通过
- [ ] PluginReloadOperationTests.cs 创建，包含 ≥8 个测试
- [ ] 所有 4 个状态码路径有对应测试覆盖
- [ ] 参数验证（null/空）有测试覆盖
- [ ] 大小写不敏感匹配有测试覆盖
- [ ] 全部测试通过，0 失败
</success_criteria>

<output>
After completion, create `.planning/phases/12-plugin-reload/12-02-SUMMARY.md`
</output>
