---
phase: 06-ipc-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - VAL-03
  - VAL-04
user_setup:
  - "Phase 5 passed: Plugin loaded and operations verified"
  - "CLI binary available at src/DalamudMCP.Cli/bin/Debug/net10.0/"
---

<objective>
Verifikasi IPC bridge dan tiga mode CLI berfungsi dengan API 15.
</objective>

<tasks>

<task type="human-action" tdd="false">
  <name>Task 1: Verifikasi named pipe IPC</name>
  <action>
    1. Pastikan plugin loaded di game
    2. Jalankan CLI dalam mode DirectCli: `dotnet run --project src/DalamudMCP.Cli`
    3. Jalankan beberapa command sederhana (misalnya get_player_info)
    4. Verifikasi response diterima dengan benar melalui named pipe
  </action>
  <acceptance_criteria>
    - CLI dapat terhubung ke plugin via named pipe
    - Response operasi diterima dengan benar
    - Tidak ada error IPC/deserialization
  </acceptance_criteria>
</task>

<task type="human-action" tdd="false">
  <name>Task 2: Verifikasi stdio MCP server mode</name>
  <action>
    1. Konfigurasi Claude Desktop (atau MCP client lain) untuk menggunakan CLI sebagai MCP server
    2. Command: `dotnet run --project src/DalamudMCP.Cli -- serve-mcp`
    3. Uji beberapa MCP tools dari AI client
    4. Verifikasi AI client dapat membaca FFXIV game state
  </action>
  <acceptance_criteria>
    - MCP client dapat terhubung dan menerima daftar tools
    - MCP tools berfungsi dan mengembalikan data aktual dari game
  </acceptance_criteria>
</task>

<task type="human-action" tdd="false">
  <name>Task 3: Verifikasi Streamable HTTP MCP server mode</name>
  <action>
    1. Jalankan: `dotnet run --project src/DalamudMCP.Cli -- serve-http`
    2. Uji endpoint HTTP dengan curl atau browser
    3. Verifikasi MCP protocol via HTTP berfungsi
  </action>
  <acceptance_criteria>
    - HTTP endpoint merespons dengan benar
    - MCP tools dapat diakses via HTTP
  </acceptance_criteria>
</task>

</tasks>
