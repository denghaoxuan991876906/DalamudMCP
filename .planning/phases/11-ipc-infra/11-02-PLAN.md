---
phase: 11-ipc-infra
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs
  - src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "UnsafeInvokePluginIpcOperation 的 public 构造函数通过 DI 注入 IPluginIpcGateway"
    - "IPluginIpcGateway 作为单例服务注册到 DI 容器"
    - "现有 IPC 功能完全无回归 — InvokeUnsafeIpc、ParseKindsCsv 等静态方法正常工作"
  artifacts:
    - path: "src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs"
      provides: "重构后的 IPC 操作类，嵌套类型已移除，public 构造函数使用 DI 注入"
      min_lines: 280
    - path: "src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs"
      provides: "DI 注册入口，新增 IPluginIpcGateway 单例注册"
      min_lines: 78
  key_links:
    - from: "UnsafeInvokePluginIpcOperation public 构造函数"
      to: "IPluginIpcGateway"
      pattern: "IPluginIpcGateway gateway"
    - from: "PluginServiceCollectionExtensions.BuildDalamudServiceProvider()"
      to: "PluginIpcGateway"
      pattern: "services\\.AddSingleton<IPluginIpcGateway, PluginIpcGateway>\\(\\)"
    - from: "CreateDalamudExecutor()"
      to: "IPluginIpcGateway.TryCreate()"
      pattern: "gateway\\.TryCreate\\(callgate, typeArguments"
---

<objective>
重构 `UnsafeInvokePluginIpcOperation.cs` — 移除已提取的嵌套接口和类，修改 public 构造函数以通过 DI 注入 `IPluginIpcGateway`；更新 `PluginServiceCollectionExtensions.cs` 注册 `IPluginIpcGateway` 单例服务。

Purpose: 完成基础设施提取的"连线"工作，使共享 IPC 网关可通过 DI 容器提供给所有操作类使用。
Output: 2 个修改后的现有文件，嵌套类型已移除并替换为独立文件的引用
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ipc-infra/11-CONTEXT.md
@src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs
@src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs
@src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs
@src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs

<interfaces>
<!-- Plan 01 创建的接口，本 plan 需引用 -->
```csharp
// src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs
namespace DalamudMCP.Plugin.Ipc;
public interface IPluginIpcGateway
{
    bool TryCreate(string callgate, IReadOnlyList<Type> typeArguments, out IPluginCallGateSubscriber? subscriber);
}

// src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs
namespace DalamudMCP.Plugin.Ipc;
internal sealed class PluginIpcGateway : IPluginIpcGateway { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 重构 UnsafeInvokePluginIpcOperation.cs — 移除嵌套类型，DI 注入网关</name>
  <read_first>
    - src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs (完整文件)
    - src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs (从 Plan 01 创建)
    - src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs (从 Plan 01 创建)
    - src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs (从 Plan 01 创建)
    - src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs (从 Plan 01 创建)
    - .planning/phases/11-ipc-infra/11-CONTEXT.md (决策 D-04, D-07)
  </read_first>
  <files>src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs</files>
  <action>
对 `UnsafeInvokePluginIpcOperation.cs` 进行以下精确修改：

**步骤 1: 添加 using 指令**
在文件顶部的 using 块中添加：
```csharp
using DalamudMCP.Plugin.Ipc;
```

**步骤 2: 修改 public 构造函数（第 26-34 行）**
将：
```csharp
public UnsafeInvokePluginIpcOperation(
    IDalamudPluginInterface pluginInterface,
    IFramework framework)
{
    ArgumentNullException.ThrowIfNull(pluginInterface);
    ArgumentNullException.ThrowIfNull(framework);

    executor = CreateDalamudExecutor(new PluginIpcGateway(pluginInterface), framework);
}
```
改为：
```csharp
public UnsafeInvokePluginIpcOperation(
    IPluginIpcGateway gateway,
    IFramework framework)
{
    ArgumentNullException.ThrowIfNull(gateway);
    ArgumentNullException.ThrowIfNull(framework);

    executor = CreateDalamudExecutor(gateway, framework);
}
```

**步骤 3: 删除嵌套类型（删除第 293-369 行）**
删除以下嵌套类型定义（这些已提取到 Plan 01 的文件中）：
- `internal interface IPluginIpcGateway`（第 293-296 行）
- `internal interface IPluginCallGateSubscriber`（第 298-303 行）
- `internal sealed class PluginIpcGateway : IPluginIpcGateway`（第 305-343 行）
- `internal sealed class ReflectionPluginCallGateSubscriber : IPluginCallGateSubscriber`（第 346-369 行）

**保留不变的部分：**
- `UnsafeInvokePluginIpcOperation` 类的所有其他成员（internal 构造函数、ExecuteAsync、Request、TextFormatter、CreateDalamudExecutor、InvokeUnsafeIpc、ParseKindsCsv、ParseKind、BuildTypeArguments、ParseArguments、ParseElement 等静态方法）
- 文件末尾的 `PluginIpcValueKind` 枚举（第 372-382 行）
- 文件末尾的 `UnsafeInvokePluginIpcResult` record（第 384-391 行）
- 所有 `[Operation]`、`[MemoryPackable]` 等属性

**关键约束：**
- 不修改 internal 构造函数（`internal UnsafeInvokePluginIpcOperation(Func<...> executor)`）— 它继续按原样工作
- `CreateDalamudExecutor` 方法签名仍接受 `IPluginIpcGateway gateway`（现在引用 Plan 01 的 public 接口）
- `InvokeUnsafeIpc` 方法参数类型 `IPluginIpcGateway gateway` 自动解析为新位置
  </action>
  <verify>
    <automated>grep -c "internal interface IPluginIpcGateway" "src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs" | xargs -I{} sh -c 'test {} -eq 0' && echo "PASS: 嵌套接口已移除"</automated>
  </verify>
  <acceptance_criteria>
    - `UnsafeInvokePluginIpcOperation.cs` 不再包含 `internal interface IPluginIpcGateway` 定义
    - `UnsafeInvokePluginIpcOperation.cs` 不再包含 `internal interface IPluginCallGateSubscriber` 定义
    - `UnsafeInvokePluginIpcOperation.cs` 不再包含 `internal sealed class PluginIpcGateway` 定义
    - `UnsafeInvokePluginIpcOperation.cs` 不再包含 `internal sealed class ReflectionPluginCallGateSubscriber` 定义
    - public 构造函数签名改为 `UnsafeInvokePluginIpcOperation(IPluginIpcGateway gateway, IFramework framework)`（per D-07）
    - 文件顶部包含 `using DalamudMCP.Plugin.Ipc;`
    - `PluginIpcValueKind` 枚举和 `UnsafeInvokePluginIpcResult` record 保留在原位置
    - internal 测试构造函数保持不变
  </acceptance_criteria>
  <done>UnsafeInvokePluginIpcOperation 的嵌套类型已移除，public 构造函数通过 DI 注入 IPluginIpcGateway</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 PluginServiceCollectionExtensions 中注册 IPluginIpcGateway 单例</name>
  <read_first>
    - src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs (完整文件)
    - src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs (接口)
    - src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs (实现)
    - .planning/phases/11-ipc-infra/11-CONTEXT.md (决策 D-05: 手动 DI 注册)
  </read_first>
  <files>src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs</files>
  <action>
在 `PluginServiceCollectionExtensions.cs` 中进行以下精确修改：

**步骤 1: 添加 using 指令**
在 using 块中添加（如果不存在）：
```csharp
using DalamudMCP.Plugin.Ipc;
```

**步骤 2: 添加 IPluginIpcGateway 单例注册**
在 `BuildDalamudServiceProvider` 方法体中，按 D-05 决策，在 `services.AddSingleton(configurationStore);`（第 49 行）之后、`services.AddGeneratedPluginOperations();`（第 64 行）之前插入：

```csharp
services.AddSingleton<IPluginIpcGateway, PluginIpcGateway>();
```

**插入位置说明：** 注册到 `pluginInterface` 单例注册之后、操作类注册之前，确保 `IPluginIpcGateway` 在 DI 容器可用时操作类能解析它。

**不修改的部分：**
- 方法签名不变
- 所有其他 `AddSingleton` 注册不变
- `services.AddGeneratedPluginOperations()` 和后续注册不变
  </action>
  <verify>
    <automated>grep -n "AddSingleton<IPluginIpcGateway, PluginIpcGateway>" "src/DalamudMCP.Plugin/Hosting/PluginServiceCollectionExtensions.cs"</automated>
  </verify>
  <acceptance_criteria>
    - `PluginServiceCollectionExtensions.cs` 包含 `using DalamudMCP.Plugin.Ipc;`
    - `BuildDalamudServiceProvider` 方法体中包含 `services.AddSingleton<IPluginIpcGateway, PluginIpcGateway>()`
    - 注册位置在 `pluginInterface` 注册之后、操作类注册之前
    - 所有现有注册保持不变
  </acceptance_criteria>
  <done>IPluginIpcGateway 已作为单例服务注册到 DI 容器</done>
</task>

</tasks>

<verification>
整体验证：
1. `UnsafeInvokePluginIpcOperation.cs` 中无嵌套接口/类定义
2. `UnsafeInvokePluginIpcOperation` public 构造函数接受 `IPluginIpcGateway gateway`
3. `PluginServiceCollectionExtensions.cs` 中注册了 `IPluginIpcGateway`
4. 执行 `./build/build.ps1` 编译通过（确认无类型引用错误）
</verification>

<success_criteria>
- 项目编译成功：`./build/build.ps1` 无错误
- `UnsafeInvokePluginIpcOperation` 的 public 构造函数改为 DI 注入 `IPluginIpcGateway`
- DI 容器中 `IPluginIpcGateway` 作为单例可用
- `PluginIpcValueKind` 枚举和 `UnsafeInvokePluginIpcResult` record 位置不变
</success_criteria>

<output>
执行完成后创建 `.planning/phases/11-ipc-infra/11-02-SUMMARY.md`
</output>
