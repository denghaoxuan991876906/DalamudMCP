---
phase: 11-ipc-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs
  - src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs
  - src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs
  - src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "IPluginIpcGateway 可作为 public 类型被 DI 容器注入使用"
    - "IPluginCallGateSubscriber 可作为 public 类型在其他项目中引用"
    - "PluginIpcGateway 实现可通过反射调用 IDalamudPluginInterface.GetIpcSubscriber()"
    - "ReflectionPluginCallGateSubscriber 可封装任意 IPC 订阅者的 HasFunction 和 InvokeFunc"
  artifacts:
    - path: "src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs"
      provides: "IPluginIpcGateway public 接口，定义 TryCreate 方法"
      min_lines: 10
    - path: "src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs"
      provides: "IPluginCallGateSubscriber public 接口，定义 HasFunction 和 InvokeFunc"
      min_lines: 12
    - path: "src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs"
      provides: "PluginIpcGateway internal 类，基于 IDalamudPluginInterface 反射订阅 IPC"
      min_lines: 50
    - path: "src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs"
      provides: "ReflectionPluginCallGateSubscriber internal 类，反射封装 CallGateSubscriber"
      min_lines: 30
  key_links:
    - from: "IPluginIpcGateway.TryCreate()"
      to: "IDalamudPluginInterface.GetIpcSubscriber()"
      pattern: "method\\.MakeGenericMethod\\(typeArguments\\)\\.Invoke\\(pluginInterface"
    - from: "ReflectionPluginCallGateSubscriber"
      to: "IPluginCallGateSubscriber.HasFunction"
      pattern: "hasFunctionProperty\\.GetValue\\(subscriber\\)"
    - from: "ReflectionPluginCallGateSubscriber"
      to: "IPluginCallGateSubscriber.InvokeFunc"
      pattern: "invokeFuncMethod\\.Invoke\\(subscriber, arguments\\.ToArray\\(\\)\\)"
---

<objective>
从 `UnsafeInvokePluginIpcOperation.cs` 中提取 `IPluginIpcGateway` 和 `IPluginCallGateSubscriber` 接口及其实现类为 `src/DalamudMCP.Plugin/Ipc/` 目录下的独立文件，接口提升为 `public`，实现类保持 `internal`。

Purpose: 为后续 Phase 12/14/15 的跨插件操作提供可注入的共享 IPC 网关服务。
Output: 4 个新文件：2 个 public 接口 + 2 个 internal 实现类
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ipc-infra/11-CONTEXT.md
@src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs

<interfaces>
<!-- 从 UnsafeInvokePluginIpcOperation.cs 提取的嵌套接口和类的完整代码 -->
<!-- 执行者应原样提取，不进行任何语义修改 -->

当前嵌套接口（第 293-303 行）:
```csharp
internal interface IPluginIpcGateway
{
    public bool TryCreate(string callgate, IReadOnlyList<Type> typeArguments, out IPluginCallGateSubscriber? subscriber);
}

internal interface IPluginCallGateSubscriber
{
    public bool HasFunction { get; }

    public object? InvokeFunc(IReadOnlyList<object?> arguments);
}
```

当前嵌套实现类（第 305-369 行）:
- PluginIpcGateway: 通过反射调用 IDalamudPluginInterface.GetIpcSubscriber<T1,T2,...>(callgate)
- ReflectionPluginCallGateSubscriber: 通过反射封装 IP 订阅者对象的 HasFunction / InvokeFunc
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 public 接口文件 IPluginIpcGateway.cs 和 IPluginCallGateSubscriber.cs</name>
  <read_first>
    - src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs (第 293-303 行：当前嵌套接口定义)
    - .planning/phases/11-ipc-infra/11-CONTEXT.md (决策 D-01: 接口放 DalamudMCP.Plugin 顶层，D-03: Ipc/ 子目录)
  </read_first>
  <files>src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs, src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs</files>
  <action>
创建 `src/DalamudMCP.Plugin/Ipc/` 目录，然后创建两个 public 接口文件：

**文件 1: `src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs`**
- 命名空间：`DalamudMCP.Plugin.Ipc`
- 接口：`public interface IPluginIpcGateway`
- 方法签名（原样从嵌套接口复制）：`bool TryCreate(string callgate, IReadOnlyList<Type> typeArguments, out IPluginCallGateSubscriber? subscriber)`

**文件 2: `src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs`**
- 命名空间：`DalamudMCP.Plugin.Ipc`
- 接口：`public interface IPluginCallGateSubscriber`
- 属性：`bool HasFunction { get; }`
- 方法：`object? InvokeFunc(IReadOnlyList<object?> arguments)`

两个文件在接口级别保持与嵌套接口完全相同的成员签名，仅将 `internal` 改为 `public`。
  </action>
  <verify>
    <automated>test -f "src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs" && test -f "src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs" && echo "PASS: 接口文件已创建"</automated>
  </verify>
  <acceptance_criteria>
    - `src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs` 存在，包含 `namespace DalamudMCP.Plugin.Ipc` 和 `public interface IPluginIpcGateway`
    - `src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs` 存在，包含 `namespace DalamudMCP.Plugin.Ipc` 和 `public interface IPluginCallGateSubscriber`
    - `IPluginIpcGateway.TryCreate` 签名与原嵌套接口完全一致：`bool TryCreate(string callgate, IReadOnlyList<Type> typeArguments, out IPluginCallGateSubscriber? subscriber)`
  </acceptance_criteria>
  <done>两个 public 接口文件已创建，接口签名与嵌套版本完全一致</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 internal 实现类文件 PluginIpcGateway.cs 和 ReflectionPluginCallGateSubscriber.cs</name>
  <read_first>
    - src/DalamudMCP.Plugin/Operations/UnsafeInvokePluginIpcOperation.cs (第 305-369 行：PluginIpcGateway 和 ReflectionPluginCallGateSubscriber 完整实现)
    - src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs (Task 1 创建的接口)
    - src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs (Task 1 创建的接口)
  </read_first>
  <files>src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs, src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs</files>
  <action>
创建两个 internal 实现类文件：

**文件 1: `src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs`**
- 命名空间：`DalamudMCP.Plugin.Ipc`
- 类：`internal sealed class PluginIpcGateway : IPluginIpcGateway`
- 原样提取 `UnsafeInvokePluginIpcOperation.cs` 第 305-343 行的完整实现，包括：
  - 静态 `GetSubscriberMethods` 缓存（`MethodInfo[]`，从 `IDalamudPluginInterface` 枚举所有 `GetIpcSubscriber` 泛型方法重载）
  - `private readonly IDalamudPluginInterface pluginInterface` 字段
  - 构造函数 `public PluginIpcGateway(IDalamudPluginInterface pluginInterface)`
  - `TryCreate` 方法：通过 `GetSubscriberMethods` 找到匹配泛型参数数量的重载，反射调用 `MakeGenericMethod().Invoke()` 创建订阅者，包装为 `ReflectionPluginCallGateSubscriber`
- 添加 `using System.Reflection;` 和 `using Dalamud.Plugin;`

**文件 2: `src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs`**
- 命名空间：`DalamudMCP.Plugin.Ipc`
- 类：`internal sealed class ReflectionPluginCallGateSubscriber : IPluginCallGateSubscriber`
- 原样提取 `UnsafeInvokePluginIpcOperation.cs` 第 346-369 行的完整实现，包括：
  - `private readonly object subscriber` 字段
  - `hasFunctionProperty` 和 `invokeFuncMethod` 的反射缓存
  - 构造函数：`public ReflectionPluginCallGateSubscriber(object subscriber)` — 通过 `GetProperty("HasFunction")` 和 `GetMethod("InvokeFunc")` 缓存反射成员
  - `HasFunction` 属性：`(bool?)hasFunctionProperty.GetValue(subscriber) ?? false`
  - `InvokeFunc` 方法：`invokeFuncMethod.Invoke(subscriber, arguments.ToArray())`
- 添加 `using System.Reflection;`

**关键约束：** 不修改任何逻辑，仅从嵌套类提取为独立文件。内部实现代码完全与第 305-369 行一致。
  </action>
  <verify>
    <automated>test -f "src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs" && test -f "src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs" && echo "PASS: 实现类文件已创建"</automated>
  </verify>
  <acceptance_criteria>
    - `src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs` 存在，包含 `internal sealed class PluginIpcGateway : IPluginIpcGateway`
    - `PluginIpcGateway` 构造函数接受 `IDalamudPluginInterface pluginInterface` 参数
    - `TryCreate` 方法通过反射调用 `IDalamudPluginInterface.GetIpcSubscriber<T...>()`
    - `src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs` 存在，包含 `internal sealed class ReflectionPluginCallGateSubscriber : IPluginCallGateSubscriber`
    - `ReflectionPluginCallGateSubscriber` 构造函数缓存了 `HasFunction` 属性和 `InvokeFunc` 方法
    - 两个实现类的行为与原嵌套类完全一致（代码一字不差提取）
  </acceptance_criteria>
  <done>两个 internal 实现类文件已创建，代码与原嵌套类完全一致</done>
</task>

</tasks>

<verification>
整体验证：
1. 4 个文件全部存在于 `src/DalamudMCP.Plugin/Ipc/` 目录
2. 接口文件包含 `public interface` 声明
3. 实现文件包含 `internal sealed class` 声明
4. 所有代码从 UnsafeInvokePluginIpcOperation.cs 原样提取，无逻辑修改
</verification>

<success_criteria>
- `src/DalamudMCP.Plugin/Ipc/IPluginIpcGateway.cs` 存在，`IPluginIpcGateway` 为 public 接口
- `src/DalamudMCP.Plugin/Ipc/IPluginCallGateSubscriber.cs` 存在，`IPluginCallGateSubscriber` 为 public 接口
- `src/DalamudMCP.Plugin/Ipc/PluginIpcGateway.cs` 存在，`PluginIpcGateway` 为 internal 类
- `src/DalamudMCP.Plugin/Ipc/ReflectionPluginCallGateSubscriber.cs` 存在，`ReflectionPluginCallGateSubscriber` 为 internal 类
- 所有提取的代码与 UnsafeInvokePluginIpcOperation.cs 中嵌套接口/类的语义完全一致
</success_criteria>

<output>
执行完成后创建 `.planning/phases/11-ipc-infra/11-01-SUMMARY.md`
</output>
