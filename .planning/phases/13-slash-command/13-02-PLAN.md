---
phase: 13-slash-command
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - tests/DalamudMCP.Plugin.Operations.Tests/SlashCommandOperationTests.cs
autonomous: true
requirements:
  - SLASH-01

must_haves:
  truths:
    - "所有 10 个单元测试通过（覆盖验证失败路径、成功路径、构造参数、线程编排、边界值）"
    - "测试复用 Phase 12 建立的 NSubstitute mock 模式（IFramework、ICommandManager）"
    - "无需新建测试桩文件——两个依赖接口均可通过 NSubstitute 直接 mock"
  artifacts:
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/SlashCommandOperationTests.cs"
      provides: "斜杠命令操作单元测试（10 个测试覆盖全部路径）"
      min_lines: 180
  key_links:
    - from: "SlashCommandOperationTests.cs"
      to: "SlashCommandOperation.cs"
      via: "new SlashCommandOperation(framework, commandManager)"
      pattern: "new SlashCommandOperation\\("
    - from: "SlashCommandOperationTests.cs"
      to: "IFramework mock"
      via: "CreateFramework() 工厂方法"
      pattern: "Substitute\\.For<IFramework>\\(\\)"
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-slash-command/13-01-PLAN.md（依赖：SlashCommandOperation 已实现）

<interfaces>
<!-- 执行器需要的现有接口。从代码库中提取。 -->

从 tests/DalamudMCP.Plugin.Operations.Tests/PluginReloadOperationTests.cs（Phase 12 测试模式）:

```csharp
using Dalamud.Plugin;
using Dalamud.Plugin.Services;
using DalamudMCP.Framework;
using NSubstitute;

namespace DalamudMCP.Plugin.Operations.Tests;

public sealed class PluginReloadOperationTests
{
    // Mock 工厂方法（在 Task 1 中复制到新测试文件）
    private static IFramework CreateFramework(bool isInFrameworkThread = false)
    {
        var framework = Substitute.For<IFramework>();
        framework.IsInFrameworkUpdateThread.Returns(isInFrameworkThread);
        framework.RunOnFrameworkThread(Arg.Any<Action>())
            .Returns(callInfo =>
            {
                ((Action)callInfo[0])();
                return Task.CompletedTask;
            });
        return framework;
    }

    private static ICommandManager CreateCommandManager()
    {
        return Substitute.For<ICommandManager>();
    }
}

// 测试模式示例：
// - 操作构造：new SlashCommandOperation(framework, commandManager)
// - 请求创建：new SlashCommandOperation.Request { Command = "/echo hello" }
// - 上下文创建：OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken)
// - 断言：Assert.True(result.Success)、Assert.Equal("command_sent", result.Status)
// - mock 验证：commandManager.Received(1).ProcessCommand("/echo hello")
// - null 构造验证：Assert.Throws<ArgumentNullException>(() => new SlashCommandOperation(null!, commandManager))
```

从 .planning/phases/13-slash-command/13-RESEARCH.md（§6.2 测试用例表）:
- 7 个输入验证测试（有效命令、不以 / 开头、空字符串、恰好 256、超过 256、仅 /、含换行）
- 2 个线程测试（已在 Framework 线程、不在 Framework 线程）
- 3 个构造参数验证（null framework、null commandManager、null request）
- 总计：12 个测试 → 精简为 10 个（null request 已由 ExecuteAsync 框架自动处理，不作为独立测试；Framework 线程异常处理合并）
</interfaces>

<context>
从 tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj 测试项目引用:
```xml
<PackageReference Include="NSubstitute" Version="5.3.0" />
<PackageReference Include="xunit.v3.mtp-v2" Version="3.2.2" />
<ProjectReference Include="..\..\src\DalamudMCP.Plugin\DalamudMCP.Plugin.csproj" />
<ProjectReference Include="..\..\src\DalamudMCP.Framework\DalamudMCP.Framework.csproj" />
<Using Include="Xunit" />
```
</context>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 SlashCommandOperationTests.cs（10 个测试）</name>
  <files>tests/DalamudMCP.Plugin.Operations.Tests/SlashCommandOperationTests.cs</files>
  <read_first>
    - src/DalamudMCP.Plugin/Operations/SlashCommandOperation.cs（被测试的操作类——验证逻辑、状态码、Record 结构）
    - tests/DalamudMCP.Plugin.Operations.Tests/PluginReloadOperationTests.cs（Phase 12 测试模式——mock 工厂、测试结构、断言风格）
    - .planning/phases/13-slash-command/13-RESEARCH.md（§6.2 测试用例表）
  </read_first>
  <behavior>
    - Test 1: ExecuteAsync_ReturnsCommandSent_WhenCommandStartsWithSlash → 命令 "/echo hello" → Success=true, Status="command_sent", ProcessCommand 被调用 1 次
    - Test 2: ExecuteAsync_ReturnsValidationFailed_WhenCommandNotStartingWithSlash → 命令 "hello" → Success=false, Status="validation_failed", ProcessCommand 未被调用
    - Test 3: ExecuteAsync_ReturnsValidationFailed_WhenCommandEmpty → 命令 "" → Success=false, Status="validation_failed"
    - Test 4: ExecuteAsync_ReturnsValidationFailed_WhenCommandExceedsMaxLength → 命令为 257 个字符（以 / 开头） → Success=false, Status="validation_failed"
    - Test 5: ExecuteAsync_ReturnsCommandSent_WhenCommandAtMaxLength → 命令为恰好 256 个字符（以 / 开头） → Success=true, Status="command_sent"
    - Test 6: ExecuteAsync_ReturnsCommandSent_WhenCommandIsOnlySlash → 命令 "/" → Success=true, Status="command_sent"（D-02 不预检查有效性）
    - Test 7: ExecuteAsync_ReturnsCommandSent_WithSpecialCharacters → 命令 "/echo\r\nhello" → Success=true, Status="command_sent"（D-02 不过滤特殊字符）
    - Test 8: ExecuteAsync_ReturnsCommandSent_OnFrameworkThread → framework.IsInFrameworkUpdateThread=true → 直接调用 ProcessCommand，不使用 RunOnFrameworkThread
    - Test 9: ExecuteAsync_ReturnsCommandSent_RunOnFrameworkThread → framework.IsInFrameworkUpdateThread=false → 使用 RunOnFrameworkThread 编排
    - Test 10: Constructor_RejectsNullFramework + Constructor_RejectsNullCommandManager → 各抛 ArgumentNullException 并指定正确参数名
  </behavior>
  <action>
创建新文件 `tests/DalamudMCP.Plugin.Operations.Tests/SlashCommandOperationTests.cs`，包含以下内容：

**导入和命名空间：**
```csharp
using Dalamud.Plugin.Services;
using DalamudMCP.Framework;
using NSubstitute;

namespace DalamudMCP.Plugin.Operations.Tests;
```

**类声明和 Mock 辅助方法（从 PluginReloadOperationTests 复制并精简）：**
```csharp
public sealed class SlashCommandOperationTests
{
    private static IFramework CreateFramework(bool isInFrameworkThread = false)
    {
        var framework = Substitute.For<IFramework>();
        framework.IsInFrameworkUpdateThread.Returns(isInFrameworkThread);
        framework.RunOnFrameworkThread(Arg.Any<Action>())
            .Returns(callInfo =>
            {
                ((Action)callInfo[0])();
                return Task.CompletedTask;
            });
        return framework;
    }

    private static ICommandManager CreateCommandManager()
    {
        return Substitute.For<ICommandManager>();
    }
}
```

**10 个测试方法：**

**测试 1：有效命令—command_sent**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsCommandSent_WhenCommandStartsWithSlash()
{
    var framework = CreateFramework();
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var request = new SlashCommandOperation.Request { Command = "/echo hello" };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.True(result.Success);
    Assert.Equal("command_sent", result.Status);
    Assert.Contains("命令已发送", result.SummaryText);
    commandManager.Received(1).ProcessCommand("/echo hello");
}
```

**测试 2：不以 / 开头—validation_failed**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsValidationFailed_WhenCommandNotStartingWithSlash()
{
    var framework = CreateFramework();
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var request = new SlashCommandOperation.Request { Command = "hello" };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.False(result.Success);
    Assert.Equal("validation_failed", result.Status);
    commandManager.DidNotReceive().ProcessCommand(Arg.Any<string>());
}
```

**测试 3：空命令—validation_failed**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsValidationFailed_WhenCommandEmpty()
{
    var framework = CreateFramework();
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var request = new SlashCommandOperation.Request { Command = "" };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.False(result.Success);
    Assert.Equal("validation_failed", result.Status);
}
```

**测试 4：超过 256 字符—validation_failed**
使用 `new string("x", 257)` 生成恰好 257 个字符的字符串，并在前面加上 /：
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsValidationFailed_WhenCommandExceedsMaxLength()
{
    var framework = CreateFramework();
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var longCommand = "/" + new string('x', 256); // 257 chars total
    var request = new SlashCommandOperation.Request { Command = longCommand };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.False(result.Success);
    Assert.Equal("validation_failed", result.Status);
    commandManager.DidNotReceive().ProcessCommand(Arg.Any<string>());
}
```

**测试 5：恰好 256 字符—command_sent**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsCommandSent_WhenCommandAtMaxLength()
{
    var framework = CreateFramework();
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var maxCommand = "/" + new string('x', 255); // exactly 256 chars
    var request = new SlashCommandOperation.Request { Command = maxCommand };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.True(result.Success);
    Assert.Equal("command_sent", result.Status);
}
```

**测试 6：仅 / 无参数—command_sent**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsCommandSent_WhenCommandIsOnlySlash()
{
    var framework = CreateFramework();
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var request = new SlashCommandOperation.Request { Command = "/" };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.True(result.Success);
    Assert.Equal("command_sent", result.Status);
}
```

**测试 7：特殊字符—command_sent（D-02 策略）**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsCommandSent_WithSpecialCharacters()
{
    var framework = CreateFramework();
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var request = new SlashCommandOperation.Request { Command = "/echo\r\nhello" };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.True(result.Success);
    Assert.Equal("command_sent", result.Status);
}
```

**测试 8：已在 Framework 线程—直接调用 ProcessCommand**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsCommandSent_OnFrameworkThread()
{
    var framework = CreateFramework(isInFrameworkThread: true);
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var request = new SlashCommandOperation.Request { Command = "/echo hello" };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.True(result.Success);
    Assert.Equal("command_sent", result.Status);
    commandManager.Received(1).ProcessCommand("/echo hello");
    await framework.DidNotReceive().RunOnFrameworkThread(Arg.Any<Action>());
}
```

**测试 9：不在 Framework 线程—使用 RunOnFrameworkThread**
```csharp
[Fact]
public async Task ExecuteAsync_ReturnsCommandSent_RunOnFrameworkThread()
{
    var framework = CreateFramework(isInFrameworkThread: false);
    var commandManager = CreateCommandManager();
    var operation = new SlashCommandOperation(framework, commandManager);
    var request = new SlashCommandOperation.Request { Command = "/echo hello" };
    var context = OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken);

    var result = await operation.ExecuteAsync(request, context);

    Assert.True(result.Success);
    Assert.Equal("command_sent", result.Status);
    await framework.Received(1).RunOnFrameworkThread(Arg.Any<Action>());
}
```

**测试 10：构造 null 参数验证**
1. null framework → `Assert.Equal("framework", ex.ParamName)`
2. null commandManager → `Assert.Equal("commandManager", ex.ParamName)`
```csharp
[Fact]
public void Constructor_RejectsNullFramework()
{
    var commandManager = CreateCommandManager();
    var ex = Assert.Throws<ArgumentNullException>(
        () => new SlashCommandOperation(null!, commandManager));
    Assert.Equal("framework", ex.ParamName);
}

[Fact]
public void Constructor_RejectsNullCommandManager()
{
    var framework = CreateFramework();
    var ex = Assert.Throws<ArgumentNullException>(
        () => new SlashCommandOperation(framework, null!));
    Assert.Equal("commandManager", ex.ParamName);
}
```
  </action>
  <verify>
    <automated>dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "FullyQualifiedName~SlashCommandOperationTests" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - SlashCommandOperationTests.cs 文件存在，包含至少 10 个 [Fact] 测试方法
    - 所有 10 个测试通过（dotnet test 输出：passed: 10, failed: 0）
    - 测试 #1：commandManager.Received(1).ProcessCommand("/echo hello") 断言
    - 测试 #2：commandManager.DidNotReceive().ProcessCommand() 断言
    - 测试 #3：空字符串 → validation_failed
    - 测试 #4：#5：长度边界测试（256 OK，257 拒绝）
    - 测试 #6：仅 "/" → command_sent
    - 测试 #7：控制字符 → command_sent（不拒绝）
    - 测试 #8：framework.DidNotReceive().RunOnFrameworkThread 断言（已在 Framework 线程）
    - 测试 #9：framework.Received(1).RunOnFrameworkThread 断言（需要编排）
    - 测试 #10：ArgumentNullException 验证（framework + commandManager 两个参数名）
  </acceptance_criteria>
  <done>10 个单元测试全部通过，覆盖：验证失败（3 个测试）、成功路径（3 个测试）、线程模型（2 个测试）、null 构造（2 个测试）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → SlashCommandOperation | 测试输入使用 NSubstitute mock，不触发真实的 ICommandManager 或 Framework 线程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | 测试断言逻辑错误 | accept | 测试代码本身不进入生产环境。验证通过 `dotnet test` 确认 10/10 通过 |
| T-13-08 | Information Disclosure | 测试覆盖间隙 | accept | 测试覆盖 7 条验证路径 + 2 条线程路径 + 2 条构造路径。ProcessCommand 异常处理合并到 test #1 的 Success=true 断言中（fire-and-forget 语义下异常不改变状态码） |
</threat_model>

<verification>
1. `dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "FullyQualifiedName~SlashCommandOperationTests"` → passed: 10, failed: 0
2. 确认没有新建 `Fake*` 测试桩文件（两个接口均可通过 NSubstitute 直接 mock）
</verification>

<success_criteria>
- SlashCommandOperationTests.cs 存在，包含 10 个 [Fact] 测试方法
- 所有 10 个测试通过，无失败，无跳过
- 测试覆盖全部输入验证路径（D-01/D-02 策略验证）
- 测试覆盖两种线程模型（IsInFrameworkUpdateThread true/false）
- 测试覆盖构造参数 null 守卫
- 测试文件位于现有测试项目 `tests/DalamudMCP.Plugin.Operations.Tests/`
</success_criteria>

<output>
After completion, create `.planning/phases/13-slash-command/13-02-SUMMARY.md`
</output>
