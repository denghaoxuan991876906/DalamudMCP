---
phase: 15-data-relay
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs
  - src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs
  - src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs
  - src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Relay/FakePluginDataRelayService.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginDataSubscribeOperationTests.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginDataUnsubscribeOperationTests.cs
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginDataPollOperationTests.cs
autonomous: true
requirements:
  - RELAY-01

must_haves:
  truths:
    - "MCP plugin_data_subscribe 工具可用——接受 plugin-name 和 channel 参数，成功返回 subscribe_success"
    - "对已存在的通道重复订阅返回 already_subscribed（幂等）"
    - "MCP plugin_data_unsubscribe 工具可用——接受 channel 参数（完整通道名），成功返回 unsubscribe_success"
    - "MCP plugin_data_poll 工具可用——接受 channel 参数，返回 data_available（有数据）/ no_data（无数据）/ channel_not_found（未订阅）"
    - "plugin_data_poll 支持 max-items 参数限制返回条目数"
    - "所有三个操作归入 unsafe 暴露策略，受 UI 安全开关控制"
  artifacts:
    - path: "src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs"
      provides: "MCP: plugin_data_subscribe 操作"
      min_lines: 100
    - path: "src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs"
      provides: "MCP: plugin_data_unsubscribe 操作"
      min_lines: 80
    - path: "src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs"
      provides: "MCP: plugin_data_poll 操作"
      min_lines: 120
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Relay/FakePluginDataRelayService.cs"
      provides: "可控的 IPluginDataRelayService 测试桩"
      min_lines: 40
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/PluginDataSubscribeOperationTests.cs"
      provides: "8 个订阅操作测试"
      min_lines: 160
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/PluginDataUnsubscribeOperationTests.cs"
      provides: "5 个退订操作测试"
      min_lines: 100
    - path: "tests/DalamudMCP.Plugin.Operations.Tests/PluginDataPollOperationTests.cs"
      provides: "8 个轮询操作测试"
      min_lines: 160
  key_links:
    - from: "三个操作类的 public 构造函数"
      to: "IPluginDataRelayService"
      via: "DI 构造注入"
      pattern: "IPluginDataRelayService relay"
    - from: "PluginOperationExposurePolicy.UnsafeOperationIds"
      to: "三个新操作 ID"
      via: "HashSet 包含: plugin.data.subscribe, plugin.data.unsubscribe, plugin.data.poll"
      pattern: '"plugin\.data\.(subscribe|unsubscribe|poll)"'
    - from: "FakePluginDataRelayService"
      to: "IPluginDataRelayService"
      via: "接口实现"
      pattern: "class FakePluginDataRelayService : IPluginDataRelayService"
---

<objective>
创建 3 个 MCP 工具操作类（subscribe/unsubscribe/poll）+ 暴露策略注册 + 单元测试（~21 个测试覆盖所有状态码和路径）。

Purpose: AI 客户端通过 MCP 工具管理数据回传通道的完整生命周期——订阅通道让目标插件开始推送数据、退订通道停止推送并清理资源、轮询通道获取已缓存数据。
Output: 新建 7 个文件（3 操作类 + 1 测试桩 + 3 测试文件），修改 1 个文件（暴露策略新增 3 行）。
</objective>

<execution_context>
@C:/Users/嗨呀/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/嗨呀/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-data-relay/15-RESEARCH.md

<interfaces>
<!-- 从 Plan 01 创建的接口——执行器需要知道这些契约 -->
<!-- 注意：Plan 01 先执行，这些类型在执行 Plan 02 时已存在 -->

来自 src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs:
```csharp
public interface IPluginDataRelayService
{
    bool Subscribe(string pluginName, string channelName, int capacity = 1000);
    bool Unsubscribe(string fullChannelName);
    bool TryPoll(string fullChannelName, out IReadOnlyList<string> data);
    bool IsSubscribed(string fullChannelName);
    IReadOnlyCollection<string> ActiveChannels { get; }
}
```

<!-- 操作类模式来自已完成阶段——执行器完全按此模式创建 -->
来自 src/DalamudMCP.Plugin/Operations/SafeInvokePluginIpcOperation.cs — 操作类模板:
```csharp
// 完整模式：sealed partial class + IOperation<TReq, TRes> + executor 委托 + 双构造函数 + ExecuteAsync + Request 嵌套类 + TextFormatter
// public 构造注入 DI 服务 → executor = CreateDalamudExecutor(...)
// internal 构造接受 Func<Request, CancellationToken, ValueTask<TRes>> 用于测试
// ExecuteAsync: return executor(request, context.CancellationToken)
// Request: [MemoryPackable] + [ProtocolOperation("id")] + [Option] 属性
// TextFormatter: return result.SummaryText
// Result: [MemoryPackable] sealed partial record
```

来自 src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs — 当前 UnsafeOperationIds:
```csharp
private static readonly HashSet<string> UnsafeOperationIds =
[
    "unsafe.invoke.plugin-ipc",
    "plugin.reload",
    "command.slash",
    "plugin.ipc"
];
// 新增: "plugin.data.subscribe", "plugin.data.unsubscribe", "plugin.data.poll"
```

来自 tests/DalamudMCP.Plugin.Operations.Tests/SlashCommandOperationTests.cs — 测试模式:
```csharp
// xUnit v3: [Fact] + async Task
// NSubstitute: Substitute.For<IFramework>()
// framework.IsInFrameworkUpdateThread.Returns(false)
// framework.RunOnFrameworkThread(Arg.Any<Action>()).Returns(callInfo => { ((Action)callInfo[0])(); return Task.CompletedTask; });
// OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken)
```

来自 tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs — 测试桩模式:
```csharp
// 实现接口，内部用 Dictionary/ConcurrentDictionary，公开构造函数接受预定义条目
// Phase 15 新增: FakePluginDataRelayService 遵循相同模式
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 3 个操作类（Subscribe/Unsubscribe/Poll）+ Result records + 暴露策略注册</name>
  <files>
    src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs,
    src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs,
    src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs,
    src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs
  </files>

  <read_first>
  - src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs（新文件）
  - src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs（新文件）
  - src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs（新文件）
  - src/DalamudMCP.Plugin/Operations/SafeInvokePluginIpcOperation.cs（操作类模式——属性、构造函数、ExecuteAsync、Request、TextFormatter、CreateDalamudExecutor）
  - src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs（服务接口——Subscribe/Unsubscribe/TryPoll 签名）
  - src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs（当前 UnsafeOperationIds 列表）
  - .planning/phases/15-data-relay/15-RESEARCH.md §4（操作类设计）、§5（Response/Result 模型设计）、§6（3 个操作详细设计——包括 MCP 描述文本、状态码矩阵）
  </read_first>

  <action>
创建 3 个操作类文件，每个文件包含操作类 + 嵌套 Request 类 + 嵌套 TextFormatter + 命名空间级 Result record。然后修改暴露策略文件。

---

**文件 1: `src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs`**

命名空间: `DalamudMCP.Plugin.Operations`

操作属性:
- `[Operation("plugin.data.subscribe", Description = "订阅目标插件的数据回传通道。注册 IPC 端点使目标插件可通过 Dalamud IPC 向 DalamudMCP 推送数据。通道命名约定：IPC CallGate = DalamudMCP.Relay.{plugin-name}.{channel}。目标插件使用 GetIpcSubscriber<string,object>(callGate).InvokeAction(jsonData) 推送 JSON 字符串数据。成功订阅后，使用 plugin_data_poll 轮询获取数据。", Summary = "Subscribes to a plugin data relay channel.")]`
- `[ResultFormatter(typeof(PluginDataSubscribeOperation.TextFormatter))]`
- `[CliCommand("plugin", "data", "subscribe")]`
- `[McpTool("plugin_data_subscribe")]`

类声明: `public sealed partial class PluginDataSubscribeOperation : IOperation<PluginDataSubscribeOperation.Request, PluginDataSubscribeResult>`

字段: `private readonly Func<Request, CancellationToken, ValueTask<PluginDataSubscribeResult>> executor;`

Public 构造函数 (DI 注入):
- 参数: `IPluginDataRelayService relay, IFramework framework`
- `ArgumentNullException.ThrowIfNull` 两个参数
- `executor = CreateDalamudExecutor(relay, framework)`
- 添加 `[SupportedOSPlatform("windows")]`

Internal 构造函数 (测试):
- 参数: `Func<Request, CancellationToken, ValueTask<PluginDataSubscribeResult>> executor`
- `this.executor = executor ?? throw new ArgumentNullException(nameof(executor))`

ExecuteAsync: 验证 request/context 非 null，返回 `executor(request, context.CancellationToken)`

Request 嵌套类:
```csharp
[MemoryPackable]
[ProtocolOperation("plugin.data.subscribe")]
public sealed partial class Request
{
    [Option("plugin-name", Description = "目标插件的内部名称（InternalName），用于构造 IPC CallGate。")]
    public string PluginName { get; init; } = string.Empty;

    [Option("channel", Description = "回传通道名称（不含插件名前缀）。完整通道名将为 {plugin-name}.{channel}。")]
    public string Channel { get; init; } = string.Empty;
}
```

TextFormatter 嵌套类: `public sealed class TextFormatter : IResultFormatter<PluginDataSubscribeResult>` — FormatText 返回 `result.SummaryText`

CreateDalamudExecutor (private static, [SupportedOSPlatform("windows")]):
- 返回 `Func<Request, CancellationToken, ValueTask<PluginDataSubscribeResult>>`
- lambda: `ct.ThrowIfCancellationRequested()` + 验证 PluginName/Channel 非空（`ArgumentException.ThrowIfNullOrWhiteSpace`）
- 如果 `framework.IsInFrameworkUpdateThread` → 直接 `return ExecuteSubscribe(relay, request)`
- 否则 → `await framework.RunOnFrameworkThread(() => ExecuteSubscribe(relay, request)).ConfigureAwait(false)`（使用局部变量模式，同 SafeInvokePluginIpcOperation 行 88-93）

ExecuteSubscribe (internal static):
- 返回 `PluginDataSubscribeResult`
- 参数: `IPluginDataRelayService relay, Request request`
- 调用 `relay.Subscribe(request.PluginName.Trim(), request.Channel.Trim(), capacity: 1000)`
- 返回 true → `new PluginDataSubscribeResult(FullChannelName: $"{request.PluginName.Trim()}.{request.Channel.Trim()}", PluginName: request.PluginName.Trim(), Success: true, Status: "subscribe_success", ErrorMessage: null, SummaryText: $"订阅成功：通道 '{request.PluginName}.{request.Channel}' 已创建。目标插件可通过 IPC CallGate 'DalamudMCP.Relay.{request.PluginName}.{request.Channel}' 推送 JSON 数据。")`
- 返回 false → `new PluginDataSubscribeResult(..., Success: true, Status: "already_subscribed", ErrorMessage: null, SummaryText: $"通道 '{request.PluginName}.{request.Channel}' 已存在订阅。")`
- 异常捕获（整个 ExecuteSubscribe 方法体用 try-catch）：catch (Exception ex) → `new PluginDataSubscribeResult(..., Success: false, Status: "subscribe_failed", ErrorMessage: ex.Message, SummaryText: $"订阅失败：{ex.Message}")`

Result record (文件末尾，命名空间级别):
```csharp
[MemoryPackable]
public sealed partial record PluginDataSubscribeResult(
    string FullChannelName,
    string PluginName,
    bool Success,
    string Status,
    string? ErrorMessage,
    string SummaryText);
```

Using: `System.Runtime.Versioning`, `Dalamud.Plugin.Services`, `DalamudMCP.Framework`, `DalamudMCP.Protocol`, `DalamudMCP.Plugin.Relay`, `MemoryPack`

---

**文件 2: `src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs`**

命名空间: `DalamudMCP.Plugin.Operations`

操作属性:
- `[Operation("plugin.data.unsubscribe", Description = "退订数据回传通道。注销 IPC 端点，关闭有界缓冲区，释放相关资源。退订后目标插件无法再向该通道推送数据，plugin_data_poll 将返回 channel_not_found。", Summary = "Unsubscribes from a plugin data relay channel.")]`
- `[ResultFormatter(typeof(PluginDataUnsubscribeOperation.TextFormatter))]`
- `[CliCommand("plugin", "data", "unsubscribe")]`
- `[McpTool("plugin_data_unsubscribe")]`

类声明: `public sealed partial class PluginDataUnsubscribeOperation : IOperation<PluginDataUnsubscribeOperation.Request, PluginDataUnsubscribeResult>`

Request 嵌套类:
```csharp
[MemoryPackable]
[ProtocolOperation("plugin.data.unsubscribe")]
public sealed partial class Request
{
    [Option("channel", Description = "完整通道名（{plugin-name}.{channel}，如 'MyPlugin.status'），即订阅时返回的 full_channel_name。")]
    public string Channel { get; init; } = string.Empty;
}
```

TextFormatter 与 Subscribe 相同模式。

CreateDalamudExecutor: 与 Subscribe 相同模式，但调用 `ExecuteUnsubscribe`。

ExecuteUnsubscribe (internal static):
- 返回 `PluginDataUnsubscribeResult`
- `relay.Unsubscribe(request.Channel.Trim())`
- true → `new PluginDataUnsubscribeResult(FullChannelName: request.Channel.Trim(), Success: true, Status: "unsubscribe_success", ErrorMessage: null, SummaryText: $"退订成功：通道 '{request.Channel}' 已关闭并释放资源。")`
- false → `new PluginDataUnsubscribeResult(..., Success: true, Status: "not_subscribed", ErrorMessage: null, SummaryText: $"通道 '{request.Channel}' 不存在或已退订。")`
- try-catch Exception → `new PluginDataUnsubscribeResult(..., Success: false, Status: "unsubscribe_failed", ErrorMessage: ex.Message, SummaryText: $"退订失败：{ex.Message}")`

Result record:
```csharp
[MemoryPackable]
public sealed partial record PluginDataUnsubscribeResult(
    string FullChannelName,
    bool Success,
    string Status,
    string? ErrorMessage,
    string SummaryText);
```

Using 与 Subscribe 相同。

---

**文件 3: `src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs`**

命名空间: `DalamudMCP.Plugin.Operations`

操作属性:
- `[Operation("plugin.data.poll", Description = "轮询指定数据回传通道中已缓存的数据。非阻塞读取：返回通道中当前所有可用数据。max-items 参数可限制返回条目数（默认所有可用，上限 10000）。目标插件卸载时，对应通道自动退订，此时 poll 将返回 channel_not_found。", Summary = "Polls cached data from a plugin data relay channel.")]`
- `[ResultFormatter(typeof(PluginDataPollOperation.TextFormatter))]`
- `[CliCommand("plugin", "data", "poll")]`
- `[McpTool("plugin_data_poll")]`

类声明: `public sealed partial class PluginDataPollOperation : IOperation<PluginDataPollOperation.Request, PluginDataPollResult>`

Request 嵌套类:
```csharp
[MemoryPackable]
[ProtocolOperation("plugin.data.poll")]
public sealed partial class Request
{
    [Option("channel", Description = "完整通道名（{plugin-name}.{channel}，如 'MyPlugin.status'）。")]
    public string Channel { get; init; } = string.Empty;

    [Option("max-items", Description = "最大返回条目数（1-10000）。不指定时返回所有可用数据。", Required = false)]
    public int? MaxItems { get; init; }
}
```

CreateDalamudExecutor: 在 lambda 中额外验证 MaxItems（如果指定且 <= 0 → 抛出 `ArgumentException`，如果 > 10000 → 钳位到 10000）。

ExecutePoll (internal static):
- 返回 `PluginDataPollResult`
- `relay.TryPoll(request.Channel.Trim(), out IReadOnlyList<string> data)`
- false (通道不存在) → `new PluginDataPollResult(FullChannelName: request.Channel.Trim(), Success: true, Status: "channel_not_found", ItemCount: 0, Items: [], ErrorMessage: null, SummaryText: $"通道 '{request.Channel}' 不存在。请先使用 plugin_data_subscribe 订阅。")`
- true, data.Count == 0 → `new PluginDataPollResult(..., Status: "no_data", ItemCount: 0, Items: [], ..., SummaryText: $"通道 '{request.Channel}' 无新数据。")`
- true, data.Count > 0 → 如果 `request.MaxItems` 有值且 < data.Count → 取前 MaxItems 项；否则取全部
  - Items 转为 `string[]` 数组
  - `new PluginDataPollResult(..., Status: "data_available", ItemCount: finalItems.Length, Items: finalItems, ..., SummaryText: $"读取 {finalItems.Length} 条数据（共 {data.Count} 条可用）。")`
- try-catch Exception → `new PluginDataPollResult(..., Success: false, Status: "poll_failed", ItemCount: 0, Items: [], ErrorMessage: ex.Message, SummaryText: $"轮询失败：{ex.Message}")`

Result record:
```csharp
[MemoryPackable]
public sealed partial record PluginDataPollResult(
    string FullChannelName,
    bool Success,
    string Status,
    int ItemCount,
    string[] Items,
    string? ErrorMessage,
    string SummaryText);
```

---

**文件 4: `src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs` 修改**

在 `UnsafeOperationIds` HashSet 中添加三个新操作 ID，紧接 `"plugin.ipc"` 之后:

将现状:
```csharp
    private static readonly HashSet<string> UnsafeOperationIds =
    [
        "unsafe.invoke.plugin-ipc",
        "plugin.reload",
        "command.slash",
        "plugin.ipc"
    ];
```

精确修改为:
```csharp
    private static readonly HashSet<string> UnsafeOperationIds =
    [
        "unsafe.invoke.plugin-ipc",
        "plugin.reload",
        "command.slash",
        "plugin.ipc",
        "plugin.data.subscribe",
        "plugin.data.unsubscribe",
        "plugin.data.poll"
    ];
```

文件其余部分不作任何更改。

---

所有文件编码: UTF-8 with BOM
  </action>

  <verify>
    <automated>dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj -c Debug 2>&1 | Select-String "Error" -NotMatch "warning"</automated>
  </verify>

  <acceptance_criteria>
    <criterion value="1">文件 PluginDataSubscribeOperation.cs 存在</criterion>
    <criterion value="2">文件 PluginDataUnsubscribeOperation.cs 存在</criterion>
    <criterion value="3">文件 PluginDataPollOperation.cs 存在</criterion>
    <criterion value="4">grep -c "\[Operation(\"plugin.data.subscribe\"" → 输出 >= 1</criterion>
    <criterion value="5">grep -c "\[McpTool(\"plugin_data_subscribe\")" → 输出 >= 1</criterion>
    <criterion value="6">grep -c "\[Operation(\"plugin.data.unsubscribe\"" → 输出 >= 1</criterion>
    <criterion value="7">grep -c "\[McpTool(\"plugin_data_unsubscribe\")" → 输出 >= 1</criterion>
    <criterion value="8">grep -c "\[Operation(\"plugin.data.poll\"" → 输出 >= 1</criterion>
    <criterion value="9">grep -c "\[McpTool(\"plugin_data_poll\")" → 输出 >= 1</criterion>
    <criterion value="10">grep -c "subscribe_success" src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs → 输出 >= 1</criterion>
    <criterion value="11">grep -c "already_subscribed" src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs → 输出 >= 1</criterion>
    <criterion value="12">grep -c "subscribe_failed" src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs → 输出 >= 1</criterion>
    <criterion value="13">grep -c "unsubscribe_success" src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs → 输出 >= 1</criterion>
    <criterion value="14">grep -c "not_subscribed" src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs → 输出 >= 1</criterion>
    <criterion value="15">grep -c "data_available" src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs → 输出 >= 1</criterion>
    <criterion value="16">grep -c "no_data" src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs → 输出 >= 1</criterion>
    <criterion value="17">grep -c "channel_not_found" src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs → 输出 >= 1</criterion>
    <criterion value="18">grep -c "MaxItems" src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs → 输出 >= 1</criterion>
    <criterion value="19">grep -c "plugin\.data\.subscribe" src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs → 输出 >= 1</criterion>
    <criterion value="20">grep -c "plugin\.data\.unsubscribe" src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs → 输出 >= 1</criterion>
    <criterion value="21">grep -c "plugin\.data\.poll" src/DalamudMCP.Plugin/Hosting/PluginOperationExposurePolicy.cs → 输出 >= 1</criterion>
    <criterion value="22">dotnet build 零错误</criterion>
  </acceptance_criteria>

  <done>3 个操作类编译通过，3 个操作 ID 已在 UnsafeOperationIds 中注册，源生成器自动注册到 DI</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 FakePluginDataRelayService 测试桩 + 3 个测试文件（~21 个测试）</name>
  <files>
    tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Relay/FakePluginDataRelayService.cs,
    tests/DalamudMCP.Plugin.Operations.Tests/PluginDataSubscribeOperationTests.cs,
    tests/DalamudMCP.Plugin.Operations.Tests/PluginDataUnsubscribeOperationTests.cs,
    tests/DalamudMCP.Plugin.Operations.Tests/PluginDataPollOperationTests.cs
  </files>

  <read_first>
  - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Relay/FakePluginDataRelayService.cs（新文件）
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginDataSubscribeOperationTests.cs（新文件）
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginDataUnsubscribeOperationTests.cs（新文件）
  - tests/DalamudMCP.Plugin.Operations.Tests/PluginDataPollOperationTests.cs（新文件）
  - tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Ipc/FakeIpcGateway.cs（测试桩模式——实现接口，ConcurrentDictionary，构造函数接受预定义条目）
  - tests/DalamudMCP.Plugin.Operations.Tests/SlashCommandOperationTests.cs（测试模式——CreateFramework helper、[Fact] async Task、Substitute、OperationContext.ForMcp）
  - src/DalamudMCP.Plugin/Relay/IPluginDataRelayService.cs（接口——Subscribe/Unsubscribe/TryPoll 签名）
  - src/DalamudMCP.Plugin/Operations/PluginDataSubscribeOperation.cs（Task 1 创建的操作类——internal 测试构造函数签名）
  - src/DalamudMCP.Plugin/Operations/PluginDataUnsubscribeOperation.cs
  - src/DalamudMCP.Plugin/Operations/PluginDataPollOperation.cs
  - .planning/phases/15-data-relay/15-RESEARCH.md §7 测试策略（测试用例矩阵——Subscribe 8 个、Unsubscribe 5 个、Poll 8 个、Fake 设计）
  </read_first>

  <action>
创建 `tests/DalamudMCP.Plugin.Operations.Tests/TestShared/Relay/` 目录（如不存在），然后创建以下 4 个文件。

---

**文件 1: `tests/.../TestShared/Relay/FakePluginDataRelayService.cs`**

命名空间: `DalamudMCP.Plugin.Operations.Tests.TestShared.Relay`

实现 `IPluginDataRelayService`，用 `ConcurrentDictionary` 管理内存中的通道。关键设计：不涉及真实 Dalamud IPC——纯内存实现，提供可控行为供操作类测试使用。

```csharp
using System.Collections.Concurrent;
using System.Threading.Channels;
using DalamudMCP.Plugin.Relay;

namespace DalamudMCP.Plugin.Operations.Tests.TestShared.Relay;

public sealed class FakePluginDataRelayService : IPluginDataRelayService
{
    // 不使用真实 RelayChannel（避免依赖 IDisposable IPC Provider），改用 ConcurrentDictionary 管理 Channel<string> + 订阅状态
    private readonly ConcurrentDictionary<string, (Channel<string> Channel, bool Subscribed)> channels = new(StringComparer.OrdinalIgnoreCase);

    public bool Subscribe(string pluginName, string channelName, int capacity = 1000)
    {
        string fullName = $"{pluginName}.{channelName}";
        // 幂等——已存在返回 false（不覆盖）
        if (channels.ContainsKey(fullName))
            return false;

        BoundedChannelOptions options = new(capacity) { FullMode = BoundedChannelFullMode.DropOldest };
        Channel<string> channel = Channel.CreateBounded<string>(options);
        channels[fullName] = (channel, true);
        return true;
    }

    public bool Unsubscribe(string fullChannelName)
    {
        if (!channels.TryRemove(fullChannelName, out var entry))
            return false;
        entry.Channel.Writer.TryComplete();
        return true;
    }

    public bool TryPoll(string fullChannelName, out IReadOnlyList<string> data)
    {
        if (!channels.TryGetValue(fullChannelName, out var entry))
        {
            data = [];
            return false;
        }

        List<string> items = [];
        while (entry.Channel.Reader.TryRead(out string? item))
            items.Add(item);

        data = items;
        return true;
    }

    public bool IsSubscribed(string fullChannelName)
    {
        return channels.ContainsKey(fullChannelName);
    }

    public IReadOnlyCollection<string> ActiveChannels => channels.Keys.ToArray();

    /// <summary>
    /// 测试辅助方法——向指定通道直接写入数据（模拟目标插件通过 IPC 推送）。
    /// 使用 TryWrite 确保非阻塞。
    /// </summary>
    public bool WriteData(string fullChannelName, string jsonData)
    {
        if (!channels.TryGetValue(fullChannelName, out var entry))
            return false;
        return entry.Channel.Writer.TryWrite(jsonData);
    }

    /// <summary>
    /// 测试辅助方法——获取通道当前缓冲数据条数（不消费）。
    /// </summary>
    public int GetBufferedCount(string fullChannelName)
    {
        if (!channels.TryGetValue(fullChannelName, out var entry))
            return 0;
        return entry.Channel.Reader.Count;
    }
}
```

---

**文件 2: `tests/.../PluginDataSubscribeOperationTests.cs`（8 个测试）**

命名空间: `DalamudMCP.Plugin.Operations.Tests`

Helper 方法:
```csharp
private static IFramework CreateFramework(bool isInFrameworkThread = false)
{
    var framework = Substitute.For<IFramework>();
    framework.IsInFrameworkUpdateThread.Returns(isInFrameworkThread);
    framework.RunOnFrameworkThread(Arg.Any<Action>())
        .Returns(callInfo =>
        {
            ((Action)callInfo[0])();
            return Task.CompletedTask;
        });
    return framework;
}

private static PluginDataSubscribeOperation CreateOperation(FakePluginDataRelayService relay, IFramework framework)
{
    return new PluginDataSubscribeOperation(relay, framework);
}
```

测试列表（每个用 `[Fact]` + `async Task`）：

1. **ExecuteAsync_ReturnsSubscribeSuccess_WhenValidChannel**
   - `relay = new FakePluginDataRelayService()`, framework = `CreateFramework()`
   - request: `PluginName = "MyPlugin"`, `Channel = "status"`
   - result → `Assert.True(result.Success)` + `Assert.Equal("subscribe_success", result.Status)` + `Assert.Equal("MyPlugin.status", result.FullChannelName)` + `Assert.Contains("订阅成功", result.SummaryText)`
   - relay 侧验证: `Assert.True(relay.IsSubscribed("MyPlugin.status"))`

2. **ExecuteAsync_ReturnsAlreadySubscribed_WhenChannelExists**
   - relay 预先 subscribe → 再次 subscribe 同一通道
   - result → `Assert.True(result.Success)` + `Assert.Equal("already_subscribed", result.Status)` + `Assert.Contains("已存在", result.SummaryText)`

3. **ExecuteAsync_ReturnsSubscribeFailed_WhenCapacityInvalid**（可选——如负容量导致的异常）
   - 使用 internal 构造模拟 relay 抛异常 → `Assert.False(result.Success)` + `Assert.Equal("subscribe_failed", result.Status)`

4. **Constructor_RejectsNullRelayService**
   - `Assert.Throws<ArgumentNullException>(() => new PluginDataSubscribeOperation(null!, CreateFramework()))`

5. **Constructor_RejectsNullFramework**
   - `Assert.Throws<ArgumentNullException>(() => new PluginDataSubscribeOperation(new FakePluginDataRelayService(), null!))`

6. **ExecuteAsync_ThrowsArgumentException_WhenPluginNameEmpty**
   - request: `PluginName = ""`, `Channel = "status"` → 通过 internal 构造验证：executor 委托中 `ThrowIfNullOrWhiteSpace` → 预期抛出 `ArgumentException`

7. **ExecuteAsync_ThrowsArgumentException_WhenChannelEmpty**
   - request: `PluginName = "MyPlugin"`, `Channel = ""` → 预期抛出 `ArgumentException`

8. **ExecuteAsync_DefaultCapacityIs1000**
   - subscribe 后通过 Fake 验证 channel 容量：写入 1000 条 → 再写第 1001 条 → 第一条应被丢弃（验证 `ReadAvailableAsync` 返回 1000 条且不包含第一条）
   - 实现方式：循环 `relay.WriteData("MyPlugin.status", $"data-{i}")` 1001 次 → 轮询出所有数据 → `Assert.Equal(1000, data.Count)` + `Assert.DoesNotContain("data-0", data[0])`（或者更简单的检查数量）

---

**文件 3: `tests/.../PluginDataUnsubscribeOperationTests.cs`（5 个测试）**

命名空间: `DalamudMCP.Plugin.Operations.Tests`

Helper 方法: 与 Subscribe 测试相同模式（`CreateFramework` + `CreateOperation`）。

测试：

1. **ExecuteAsync_ReturnsUnsubscribeSuccess_WhenChannelExists**
   - relay subscribe 一个通道 → 然后 unsubscribe
   - result → `Assert.True(result.Success)` + `Assert.Equal("unsubscribe_success", result.Status)` + `Assert.Contains("退订成功", result.SummaryText)`
   - relay 侧验证: `Assert.False(relay.IsSubscribed("MyPlugin.status"))`

2. **ExecuteAsync_ReturnsNotSubscribed_WhenChannelNotFound**
   - relay 无任何订阅 → 直接 unsubscribe
   - result → `Assert.True(result.Success)` + `Assert.Equal("not_subscribed", result.Status)` + `Assert.Contains("不存在", result.SummaryText)`

3. **Constructor_RejectsNullRelayService**
   - `Assert.Throws<ArgumentNullException>(() => new PluginDataUnsubscribeOperation(null!, CreateFramework()))`

4. **ExecuteAsync_ThrowsArgumentException_WhenChannelEmpty**
   - request: `Channel = ""` → 预期抛出 `ArgumentException`

5. **ExecuteAsync_UnsubscribeClearsData**
   - relay subscribe → `relay.WriteData("MyPlugin.status", "test-data")` → relay unsubscribe → relay TryPoll → `Assert.False(success)` (通道已不存在) + `Assert.Empty(data)`

---

**文件 4: `tests/.../PluginDataPollOperationTests.cs`（8 个测试）**

命名空间: `DalamudMCP.Plugin.Operations.Tests`

Helper 方法: 与上面相同模式。

测试：

1. **ExecuteAsync_ReturnsDataAvailable_WhenChannelHasItems**
   - relay subscribe → `relay.WriteData("MyPlugin.status", "data-1")` → `relay.WriteData("MyPlugin.status", "data-2")`
   - poll → `Assert.True(result.Success)` + `Assert.Equal("data_available", result.Status)` + `Assert.Equal(2, result.ItemCount)` + `Assert.Equal(["data-1", "data-2"], result.Items)` + `Assert.Contains("读取 2 条", result.SummaryText)`

2. **ExecuteAsync_ReturnsNoData_WhenChannelIsEmpty**
   - relay subscribe 但无数据写入 → poll
   - result → `Assert.True(result.Success)` + `Assert.Equal("no_data", result.Status)` + `Assert.Equal(0, result.ItemCount)` + `Assert.Empty(result.Items)` + `Assert.Contains("无新数据", result.SummaryText)`

3. **ExecuteAsync_ReturnsChannelNotFound_WhenNotSubscribed**
   - 直接 poll 不存在的通道
   - result → `Assert.True(result.Success)` + `Assert.Equal("channel_not_found", result.Status)` + `Assert.Equal(0, result.ItemCount)` + `Assert.Contains("不存在", result.SummaryText)`

4. **ExecuteAsync_RespectsMaxItemsParameter**
   - relay subscribe → 写入 5 条数据 (data-0 到 data-4)
   - poll with MaxItems=3 → `Assert.Equal(3, result.ItemCount)` + `Assert.Equal(["data-0", "data-1", "data-2"], result.Items)`

5. **ExecuteAsync_ReturnsAllItems_WhenMaxItemsExceedsAvailable**
   - relay subscribe → 写入 2 条数据 → poll with MaxItems=10
   - `Assert.Equal(2, result.ItemCount)` — 不限制

6. **Constructor_RejectsNullRelayService**
   - `Assert.Throws<ArgumentNullException>(() => new PluginDataPollOperation(null!, CreateFramework()))`

7. **Constructor_RejectsNullFramework**
   - `Assert.Throws<ArgumentNullException>(() => new PluginDataPollOperation(new FakePluginDataRelayService(), null!))`

8. **ExecuteAsync_ThrowsArgumentException_WhenChannelEmpty**
   - request: `Channel = ""` → 预期抛出 `ArgumentException`

---

所有测试文件使用:
- `using Dalamud.Plugin.Services;`（IFramework 的 NSubstitute mock）
- `using DalamudMCP.Framework;`（OperationContext）
- `using NSubstitute;`
- `using DalamudMCP.Plugin.Operations.Tests.TestShared.Relay;`（FakePluginDataRelayService）

使用 `OperationContext.ForMcp(cancellationToken: TestContext.Current.CancellationToken)` 创建 context。

文件编码: UTF-8 with BOM
  </action>

  <verify>
    <automated>dotnet test tests/DalamudMCP.Plugin.Operations.Tests/DalamudMCP.Plugin.Operations.Tests.csproj --filter "FullyQualifiedName~PluginData" --no-restore 2>&1</automated>
  </verify>

  <acceptance_criteria>
    <criterion value="1">文件 FakePluginDataRelayService.cs 存在</criterion>
    <criterion value="2">文件 PluginDataSubscribeOperationTests.cs 存在且包含 >= 8 个 [Fact] 方法</criterion>
    <criterion value="3">文件 PluginDataUnsubscribeOperationTests.cs 存在且包含 >= 5 个 [Fact] 方法</criterion>
    <criterion value="4">文件 PluginDataPollOperationTests.cs 存在且包含 >= 8 个 [Fact] 方法</criterion>
    <criterion value="5">dotnet test --filter "PluginDataSubscribe" 全部通过（8/8）</criterion>
    <criterion value="6">dotnet test --filter "PluginDataUnsubscribe" 全部通过（5/5）</criterion>
    <criterion value="7">dotnet test --filter "PluginDataPoll" 全部通过（8/8）</criterion>
  </acceptance_criteria>

  <done>21 个测试全部通过，FakePluginDataRelayService 可用，覆盖 subscribe/unsubscribe/poll 的全部状态码路径和构造验证</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI MCP 客户端 → CLI 桥接 | 不可信输入（plugin-name、channel、max-items）从 AI 端进入 CLI 进程 |
| CLI 桥接 → Plugin 命名管道 | 序列化操作通过本地命名管道传输 |
| Plugin → IPluginDataRelayService | 操作类通过 DI 注入调用 relay 服务——进程内调用，无信任问题 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | plugin-name / channel 参数为空 | mitigate | `ArgumentException.ThrowIfNullOrWhiteSpace` 拒绝空白输入——在任何通道操作前验证 |
| T-15-08 | Denial of Service | max-items 过大导致 MCP 响应超限 | mitigate | Poll 操作中 `max-items` 钳位到 10000；test #7 验证构造拒绝 null framework，确保类型安全 |
| T-15-09 | Information Disclosure | 未 subscribe 时 poll 不应返回数据 | mitigate | `channel_not_found` 状态码明确区分"未订阅"和"无数据"——不泄露其他通道信息 |
| T-15-10 | Elevation of Privilege | 所有三个 relay MCP 工具仅通过 unsafe 路径暴露 | mitigate | 操作 ID 加入 `UnsafeOperationIds`；MCP 工具仅在 `enableUnsafeOperations=true` 时暴露给 AI |

**注意：** 测试覆盖率目标 ≥ 90%（符合项目 CoverageThreshold=90 的设置，见 .csproj line 8）。
</threat_model>

<verification>
- `dotnet test tests/DalamudMCP.Plugin.Operations.Tests --filter "FullyQualifiedName~PluginData"` 全部 21 个测试通过
- `dotnet build src/DalamudMCP.Plugin/DalamudMCP.Plugin.csproj -c Debug` 零错误
- `grep -c "plugin\.data\.(subscribe|unsubscribe|poll)" PluginOperationExposurePolicy.cs` 输出 >= 3
</verification>

<success_criteria>
1. 3 个操作类编译通过，通过 DI 构造注入 `IPluginDataRelayService` + `IFramework`
2. `plugin_data_subscribe` 返回 subscribe_success / already_subscribed / subscribe_failed
3. `plugin_data_unsubscribe` 返回 unsubscribe_success / not_subscribed
4. `plugin_data_poll` 返回 data_available / no_data / channel_not_found，支持 max-items 限制
5. 所有三个操作归类为 unsafe，受 `PluginOperationExposurePolicy` 的 UI 安全开关控制
6. 源生成器通过 `[Operation]` 属性自动注册到 DI
7. FakePluginDataRelayService 提供纯内存实现，支持 WriteData 测试辅助方法
8. 21 个测试覆盖：Subscribe 8 个（含空验证/幂等/构造/容量）、Unsubscribe 5 个（含清理验证）、Poll 8 个（含 max-items/无数据/不存在/构造）
9. dotnet test 零失败，dotnet build 零错误
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-relay/15-02-SUMMARY.md`
</output>
